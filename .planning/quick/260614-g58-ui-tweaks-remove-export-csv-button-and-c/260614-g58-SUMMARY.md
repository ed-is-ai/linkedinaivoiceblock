---
quick_id: 260614-g58
status: complete
date: 2026-06-14
commits: [61ef66f, b229376]
files_modified:
  - src/dashboard/dataManagement.ts
  - src/dashboard/dataManagement.test.ts
  - src/dashboard/index.tsx
  - src/dashboard/SelectorView.tsx
---

# Quick Task 260614-g58: Dashboard UI tweaks

**One-liner:** Removed the account-level "Export CSV" button + code, and enlarged the Selector Health status dot.

## What changed

**Task 1 — remove Export CSV (`61ef66f`)**
- Deleted `buildCsvExport` from `dataManagement.ts` (kept `csvEscape`, still used by `buildPostsCsvExport`).
- Removed the `buildCsvExport` import and its `describe('buildCsvExport')` block (8 tests) from `dataManagement.test.ts`.
- Dropped `buildCsvExport` from the `./dataManagement` import, deleted `handleExportCsv`, and removed the `<button>Export CSV</button>` in `index.tsx`.
- Export JSON / Export Posts CSV / Export Traces are untouched.

**Task 2 — enlarge Selector Health dot (`b229376`)**
- `trafficLight` style bumped 10×10 → 16×16 with a subtle `boxShadow` ring in `SelectorView.tsx`, so the green/yellow/red status reads clearly beside the heading.

## Verification
- `npx tsc --noEmit` clean.
- `npx vitest run` — 242 tests pass (was 250; the 8 removed Export CSV tests account for the drop).
- `npx vite build` succeeds.
- grep confirms no remaining `buildCsvExport` / `handleExportCsv` / "Export CSV" references in `src/`.
