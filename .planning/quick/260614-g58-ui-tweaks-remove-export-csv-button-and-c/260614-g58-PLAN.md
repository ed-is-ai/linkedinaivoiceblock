---
quick_id: 260614-g58
type: quick
date: 2026-06-14
description: "UI tweaks — remove the account-level Export CSV button + code, and enlarge the Selector Health green dot for visibility"
files_modified:
  - src/dashboard/dataManagement.ts
  - src/dashboard/dataManagement.test.ts
  - src/dashboard/index.tsx
  - src/dashboard/SelectorView.tsx
---

<objective>
Two small dashboard UI tweaks:
1. Remove the account-level "Export CSV" button and its supporting code (`buildCsvExport` + `handleExportCsv` + tests). Leave "Export Posts CSV", "Export JSON", and "Export Traces" untouched. `csvEscape` stays (still used by `buildPostsCsvExport`).
2. Enlarge the "Selector Health" traffic-light dot so it is more visible (currently 10×10).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove Export CSV button + buildCsvExport + tests</name>
  <files>src/dashboard/dataManagement.ts, src/dashboard/dataManagement.test.ts, src/dashboard/index.tsx</files>
  <action>
    - dataManagement.ts: delete the `buildCsvExport` function. Keep `csvEscape` (shared with buildPostsCsvExport).
    - dataManagement.test.ts: remove the `buildCsvExport` import and its `describe('buildCsvExport', ...)` block.
    - index.tsx: drop `buildCsvExport` from the `./dataManagement` import, delete `handleExportCsv`, and remove the `<button ...>Export CSV</button>` from the export row.
  </action>
  <verify>npx tsc --noEmit && npx vitest run src/dashboard/dataManagement.test.ts</verify>
  <done>No `buildCsvExport`/`handleExportCsv`/"Export CSV" references remain; Posts CSV / JSON / Traces exports unchanged; tsc + tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enlarge Selector Health traffic-light dot</name>
  <files>src/dashboard/SelectorView.tsx</files>
  <action>Increase the `trafficLight` style from 10×10 to 16×16 and add a subtle ring (box-shadow) so the status colour reads clearly next to the heading.</action>
  <verify>npx tsc --noEmit && npx vite build</verify>
  <done>The Selector Health status dot renders larger/more visible; build clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npx vitest run` — full suite passes (Export CSV tests removed, no orphan references).
- `npx vite build` succeeds.
- grep confirms no remaining `buildCsvExport` / `handleExportCsv` / `Export CSV` (non-Posts) references in src/.
</verification>

<output>
Create .planning/quick/260614-g58-ui-tweaks-remove-export-csv-button-and-c/260614-g58-SUMMARY.md when done.
</output>
