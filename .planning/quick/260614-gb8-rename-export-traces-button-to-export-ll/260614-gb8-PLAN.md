---
quick_id: 260614-gb8
type: quick
date: 2026-06-14
description: 'Rename the "Export Traces" button to "Export LLM call traces"'
files_modified:
  - src/dashboard/index.tsx
---

<objective>
Rename the dashboard export button label from "Export Traces" to "Export LLM call traces" (the count suffix `(N)` is preserved). Behavior unchanged.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rename the Export Traces button label</name>
  <files>src/dashboard/index.tsx</files>
  <action>Change the button text `Export Traces{...}` to `Export LLM call traces{...}`, keeping the `(N)` count suffix.</action>
  <verify>npx tsc --noEmit && npx vite build</verify>
  <done>Button reads "Export LLM call traces" (with count when traces exist); tsc + build clean; label present in dist bundle.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npx vite build` succeeds; `grep "Export LLM call traces" dist/dashboard/index.js` matches.
</verification>

<output>
Create .planning/quick/260614-gb8-rename-export-traces-button-to-export-ll/260614-gb8-SUMMARY.md when done.
</output>
