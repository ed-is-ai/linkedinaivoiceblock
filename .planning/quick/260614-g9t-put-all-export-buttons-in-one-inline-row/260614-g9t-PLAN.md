---
quick_id: 260614-g9t
type: quick
date: 2026-06-14
description: "Put all Data-management export buttons on a single inline row"
files_modified:
  - src/dashboard/index.tsx
---

<objective>
The export buttons rendered across two flex rows (Export JSON + Export Posts CSV in one account-gated row; Export Traces in its own always-rendered row), so they wrapped onto two lines. Merge them into one wrapping flex row so all export buttons sit inline.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Merge export buttons into one inline flex row</name>
  <files>src/dashboard/index.tsx</files>
  <action>
    Replace the two separate export `<div style={{display:'flex'...}}>` rows with a single `flex` container (`flexWrap: 'wrap'`) holding all three buttons. Keep the account-gating per-button (Export JSON + Export Posts CSV render only when `accounts.length > 0`); keep Export Traces always rendered + disabled when `traces.length === 0`. Move the "No flagged accounts yet" note above the button row so it shows when empty.
  </action>
  <verify>npx tsc --noEmit && npx vite build</verify>
  <done>All export buttons render on one inline (wrapping) row; account-gating and the Traces disabled-when-empty behavior preserved; tsc + build clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `npx vite build` succeeds.
- Single flex container now wraps Export JSON / Export Posts CSV / Export Traces.
</verification>

<output>
Create .planning/quick/260614-g9t-put-all-export-buttons-in-one-inline-row/260614-g9t-SUMMARY.md when done.
</output>
