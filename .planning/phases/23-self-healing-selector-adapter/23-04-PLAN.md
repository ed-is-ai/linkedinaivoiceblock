---
phase: 23-self-healing-selector-adapter
plan: 04
type: execute
wave: 3
depends_on: ["23-02", "23-03"]
files_modified:
  - src/content/observer.ts
  - src/content/selector/heal.ts
  - src/content/selector/heal.test.ts
  - src/content/selector/__fixtures__/feed-healthy.html
  - src/content/selector/__fixtures__/feed-broken-classrot.html
  - src/content/selector/__fixtures__/feed-skeleton.html
  - src/content/selector/__fixtures__/feed-loggedout.html
  - src/content/selector/__fixtures__/feed-empty.html
  - src/content/selector/__fixtures__/feed-jobcards.html
  - src/content/selector/__fixtures__/feed-promoted.html
  - src/content/selector/__fixtures__/feed-abvariant-a.html
  - src/content/selector/__fixtures__/feed-abvariant-b.html
  - src/content/selector/__fixtures__/feed-pii-rich.html
autonomous: true
requirements: [ADAPT-01, ADAPT-09]
must_haves:
  truths:
    - "Breakage detection does NOT fire on logged-out, skeleton-loader, non-feed-URL, or genuinely-empty-feed fixtures (Core-4 guards suppress it)"
    - "On a class-rot active feed with >=3 prior posts seen and a 30s zero-match window, the heal orchestrator runs heuristics, validates, and writes a healed candidate via insertCandidate — without an API call when heuristics succeed"
    - "The LLM fallback is invoked only when heuristics yield no validated candidate and an API key is present, sending only the sanitized skeleton"
    - "Breakage state resets on SPA navigation (reinit)"
  artifacts:
    - path: "src/content/selector/heal.ts"
      provides: "Core-4 guards + triggerHeal orchestrator (heuristic → validate → insert; LLM fallback → validate → insert)"
      exports: ["triggerHeal", "isFeedUrl", "hasFeedContainer"]
    - path: "src/content/observer.ts"
      provides: "breakage-window state, session post counter, zero-match detection wired into the MutationObserver + reinit reset"
      contains: "triggerHeal"
    - path: "src/content/selector/heal.test.ts"
      provides: "D7 false-positive suppression + D3 write-gate + heal-to-wrong-element rejection + reset round-trip assertions"
    - path: "src/content/selector/__fixtures__/feed-broken-classrot.html"
      provides: "class-rot heal fixture"
  key_links:
    - from: "src/content/observer.ts MutationObserver"
      to: "triggerHeal"
      via: "fired after 30s zero-match window when Core-4 guards pass"
      pattern: "triggerHeal"
    - from: "src/content/selector/heal.ts"
      to: "validateCandidate then insertCandidate"
      via: "no write until validation passes"
      pattern: "validateCandidate"
---

<objective>
Wire breakage detection into `observer.ts` (Core-4 guards + 30s rolling debounce + session-activity counter + zero-match window, reset on SPA navigation) and build the `heal.ts` orchestrator that runs the full self-healing loop: heuristics first (validate each candidate, insert the first that passes), then — only if no heuristic candidate validates and an API key is configured — the LLM fallback via `LLMRederiver` on a sanitized skeleton (validate each returned candidate, insert the first that passes). Add the fixture-DOM test suite (ADAPT-09).

Purpose: This is the integration plan that connects every prior artifact (23-01 sanitizer + validator, 23-02 heuristic + insertCandidate, 23-03 rederiver) into the live observer, and delivers ADAPT-01 (guarded breakage detection) + ADAPT-09 (fixture tests). It depends on 23-02 and 23-03 and runs in Wave 3.
Output: heal.ts orchestrator + observer.ts wiring + 10 fixture files + heal.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-self-healing-selector-adapter/23-RESEARCH.md
@.planning/phases/23-self-healing-selector-adapter/23-PATTERNS.md
@.planning/phases/23-self-healing-selector-adapter/23-AI-SPEC.md
@CLAUDE.md
@.planning/phases/23-self-healing-selector-adapter/23-02-SUMMARY.md
@.planning/phases/23-self-healing-selector-adapter/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture DOM files for the heal test harness</name>
  <files>src/content/selector/__fixtures__/feed-healthy.html, src/content/selector/__fixtures__/feed-broken-classrot.html, src/content/selector/__fixtures__/feed-skeleton.html, src/content/selector/__fixtures__/feed-loggedout.html, src/content/selector/__fixtures__/feed-empty.html, src/content/selector/__fixtures__/feed-jobcards.html, src/content/selector/__fixtures__/feed-promoted.html, src/content/selector/__fixtures__/feed-abvariant-a.html, src/content/selector/__fixtures__/feed-abvariant-b.html, src/content/selector/__fixtures__/feed-pii-rich.html</files>
  <read_first>
    - src/content/selectors.ts (the seed selector values: what FEED_CONTAINER, POST_CARD `div[componentkey]`, POST_BODY_TEXT `span[data-testid="expandable-text-box"]`, POST_AUTHOR_LINK look like — fixtures must match/break these realistically)
    - 23-RESEARCH.md "Test Harness" fixture table (the 10 fixtures and which D-dimension each covers)
    - 23-AI-SPEC.md Section 5 "Reference Dataset" composition list
  </read_first>
  <action>
    Create 10 minimal-but-realistic LinkedIn-feed HTML snippet files in src/content/selector/__fixtures__/. Each is a feed-container element with child post cards (or the relevant non-feed state). Use ONLY data-*/role/aria/semantic structure consistent with selectors.ts seeds — never invent CSS class selectors as the target hook. Contents: feed-healthy (5-15 `div[componentkey]` cards each with `a[href*="/in/"]` author link + `span[data-testid="expandable-text-box"]` body text — validates a good candidate); feed-broken-classrot (cards present but `componentkey` replaced with `data-urn`, so the seed POST_CARD matches 0 and a heuristic data-urn candidate should heal); feed-skeleton (placeholder cards, no text, no author links — must fail D5 and must not trigger heal); feed-loggedout (login wall, no feed container); feed-empty (feed container present, zero post cards); feed-jobcards (job-recommendation cards with non-/in/ links — heal-to-wrong-element, must fail D5 ratio); feed-promoted (organic + Promoted cards via `aria-label*="Promoted"` — D9 advisory); feed-abvariant-a (cards using `data-activity-urn`); feed-abvariant-b (cards using a different data-* attr name); feed-pii-rich (cards with real-name text, `/in/john-doe` hrefs, `media.licdn.com` srcs, author `aria-label`s — the sanitizer strip target for D4 cross-check). Keep each file small (a handful of cards).
  </action>
  <verify>
    <automated>cd C:/Git/linkedin.blocker && node -e "const fs=require('fs');const d='src/content/selector/__fixtures__';const want=['feed-healthy','feed-broken-classrot','feed-skeleton','feed-loggedout','feed-empty','feed-jobcards','feed-promoted','feed-abvariant-a','feed-abvariant-b','feed-pii-rich'];const miss=want.filter(n=>!fs.existsSync(d+'/'+n+'.html'));if(miss.length){console.error('MISSING',miss);process.exit(1)}console.log('all 10 fixtures present')"