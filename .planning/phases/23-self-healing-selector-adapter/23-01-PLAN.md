---
phase: 23-self-healing-selector-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/types.ts
  - src/content/selector/sanitizer.ts
  - src/content/selector/sanitizer.test.ts
  - src/content/selector/validator.ts
  - src/content/selector/validator.test.ts
autonomous: true
requirements: [ADAPT-04, ADAPT-06, ADAPT-07, ADAPT-08, ADAPT-10]
must_haves:
  truths:
    - "Running `npm test` executes the Vitest suite and all sanitizer + validator tests pass"
    - "The PII sanitizer output for a PII-rich fixture contains no text-node content, no href/src/aria-label/title/alt values"
    - "The validator rejects body/html/*/:root, dangerous-token strings, and CSS class/id selectors before any DOM query runs"
    - "The validator rejects selectors matching <2 or >50 elements and selectors with author-link ratio <=50% or no post-body text"
  artifacts:
    - path: "src/content/selector/sanitizer.ts"
      provides: "buildDomSkeleton(container) — PII-stripped structural DOM serializer"
      exports: ["buildDomSkeleton"]
    - path: "src/content/selector/validator.ts"
      provides: "validateCandidate(selector, container) — 3-gate validation gate plus injection/class guards"
      exports: ["validateCandidate", "ValidationResult"]
    - path: "src/content/selector/sanitizer.test.ts"
      provides: "D4 PII-boundary assertions"
    - path: "src/content/selector/validator.test.ts"
      provides: "D1/D2/D5/D6 validation assertions incl. adversarial-string rejection"
    - path: "src/shared/types.ts"
      provides: "RateLimitState storage keys on StorageSchema"
      contains: "llbRederiveInFlight"
    - path: "package.json"
      provides: "test/test:watch/test:coverage npm scripts"
      contains: "vitest run"
  key_links:
    - from: "src/content/selector/validator.ts"
      to: "container.querySelectorAll"
      via: "plain string argument inside try/catch — never eval"
      pattern: "querySelectorAll\\(selector\\)"
    - from: "src/content/selector/sanitizer.ts"
      to: "Element.cloneNode(true)"
      via: "clone-before-strip — live DOM never mutated"
      pattern: "cloneNode\\(true\\)"
---

<objective>
Establish the Phase 23 test harness and build the two privacy/safety-critical pure utilities the entire self-healing loop depends on: the PII sanitizer (`sanitizer.ts`) and the candidate validation gate (`validator.ts`). Also add the four rate-limit storage keys to `StorageSchema` so the service-worker plan (23-03) compiles against typed storage.

This plan deliberately front-loads the two highest-stakes guardrails (D4 PII boundary, D2 injection safety) and their deterministic tests, per the AI-SPEC principle "the validation gate IS the eval." No DOM writes, no API calls, no observer wiring — those land in later plans that consume these contracts.

Purpose: Lock the privacy boundary and the write-gate contract before any code can call them, so 23-02 (heuristic) and 23-03 (service worker) build against fixed, tested interfaces.
Output: `npm test` runs; `sanitizer.ts` + `validator.ts` exist with full test coverage; `StorageSchema` carries the rate-limit keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-self-healing-selector-adapter/23-AI-SPEC.md
@.planning/phases/23-self-healing-selector-adapter/23-RESEARCH.md
@.planning/phases/23-self-healing-selector-adapter/23-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test scripts and rate-limit storage keys</name>
  <files>package.json, src/shared/types.ts</files>
  <read_first>
    - package.json (no `test` script currently exists; vitest, jsdom, @vitest/coverage-v8 are already in devDependencies)
    - vitest.config.ts (already configured: environment 'jsdom', include 'src/**/*.test.ts')
    - src/shared/types.ts:275-299 (the StorageSchema interface — extend it; do NOT remove existing keys)
    - 23-PATTERNS.md "src/shared/types.ts (modify ...)" section (StorageSchema extension pattern)
  </read_first>
  <action>
    Add three scripts to package.json scripts block: `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:coverage": "vitest run --coverage"`. Do not add or change any dependency — vitest/jsdom/@vitest/coverage-v8 are already declared.
    In src/shared/types.ts, append four OPTIONAL keys to the existing StorageSchema interface (do not create a new interface; do not delete `selectorRegistry` or `selectorSessionMisses`): `llbRederiveLastCallMs?: number` (epoch ms of most recent LLM call, for the 5-min cool-off), `llbRederiveCallsToday?: number` (count since UTC midnight, for the daily cap), `llbRederiveDateKey?: string` ('YYYY-MM-DD' UTC, for date-rollover reset), `llbRederiveInFlight?: boolean` (single-flight latch). Use the exact key spelling `llbRederiveDateKey` (the RESEARCH.md draft has a typo `llbRedeiveDateKey` — do NOT replicate the typo; use `llbRederiveDateKey` consistently everywhere). Add a one-line JSDoc comment above each key referencing ADAPT-05.
  </action>
  <verify>
    <automated>cd C:/Git/linkedin.blocker && npx vitest run 2>&1 | head -20 && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` is a valid script that invokes `vitest run` (grep package.json for `"test": "vitest run"`).
    - `npm run type-check` exits 0 with the four new keys present.
    - StorageSchema contains the literal token `llbRederiveInFlight` and `llbRederiveDateKey` (NOT `llbRedeiveDateKey`).
    - No existing StorageSchema key was removed (grep confirms `selectorRegistry?` and `selectorSessionMisses?` still present).
  </acceptance_criteria>
  <done>package.json has the three test scripts; StorageSchema has the four optional rate-limit keys with correct spelling; type-check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PII sanitizer (buildDomSkeleton) with D4 tests</name>
  <files>src/content/selector/sanitizer.ts, src/content/selector/sanitizer.test.ts</files>
  <read_first>
    - src/content/detector/language.ts (the closest analog: pure function, module-scope constants header, JSDoc, no DOM writes — mirror this file's structure)
    - src/content/exclusions.test.ts:1-60 (Vitest header + jsdom usage pattern to mirror in sanitizer.test.ts)
    - 23-RESEARCH.md "PII Sanitizer" section (serialization strategy, MAX_DEPTH/MAX_SIBLINGS/MAX_CHARS constants, clone-before-strip)
    - 23-AI-SPEC.md Section 1 + Dimension "PII boundary" + Section 4 "Context Window Strategy" (the strip list and 4000-char cap are authoritative)
    - 23-PATTERNS.md "src/content/selector/sanitizer.ts" section (analog: language.ts)
  </read_first>
  <behavior>
    - Stripping a fixture containing `<a href="/in/john-doe">John Doe</a>`: skeleton contains neither `/in/` nor `John`.
    - Stripping `<div aria-label="Post by Jane Roe">`: skeleton contains neither `aria-label` value nor `Jane`.
    - Stripping `<img src="https://media.licdn.com/x.jpg">`: skeleton contains neither `media.licdn.com` nor `src=`.
    - Stripping `title`/`alt` attributes: their values are absent from the skeleton.
    - `data-*` attribute names AND values, `role`, tag names, and nesting are PRESERVED in the skeleton.
    - Output never exceeds 4000 chars; an over-long input is truncated at the last complete `>` with a `<!-- skeleton truncated -->` marker appended.
    - The live DOM passed in is NOT mutated (assert the original element still has its href after calling buildDomSkeleton).
  </behavior>
  <action>
    Write `buildDomSkeleton(container: Element): string` as a named-export pure function following the language.ts file shape: JSDoc header stating exactly what is stripped and that it operates on a CLONE, module-scope constants `PII_ATTRS_TO_REMOVE = ['href','src','aria-label','title','alt','action','data-src']`, `MAX_DEPTH = 6`, `MAX_SIBLINGS = 3`, `MAX_CHARS = 4000`. Clone the container with `cloneNode(true)`, recursively strip PII attributes and blank text-node content on the clone (depth-capped at MAX_DEPTH, siblings capped at MAX_SIBLINGS — remove excess siblings from the clone, never the live node), serialize with a manual recursive serializer (tag + remaining attributes + children), then enforce MAX_CHARS truncation. Strip `aria-label` per the RESOLVED SPEC CONFLICT — ADAPT-04 + ROADMAP success criterion #3 + AI-SPEC override the CONTEXT.md prose; aria-label values must NOT leave the browser. Reference ADAPT-04 in the JSDoc. Write sanitizer.test.ts using jsdom + `document.body.innerHTML = ...` covering every bullet in <behavior>, with each PII assertion using `expect(skeleton).not.toContain(...)`.
  </action>
  <verify>
    <automated>cd C:/Git/linkedin.blocker && npx vitest run src/content/selector/sanitizer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All sanitizer.test.ts cases pass (D4 dimension).
    - Source assertion: sanitizer.ts contains `cloneNode(true)` and a `PII_ATTRS_TO_REMOVE` array including the literals `'href'`, `'src'`, `'aria-label'`, `'title'`, `'alt'`.
    - Source assertion: sanitizer.ts contains no `.innerHTML =` write on the input container (grep `\.innerHTML\s*=` returns no match in sanitizer.ts).
    - A test asserts the input element retains its `href` after the call (live DOM untouched).
    - A test asserts a >4000-char input produces output ending with `<!-- skeleton truncated -->` and length <= 4000 + marker length.
  </acceptance_criteria>
  <done>buildDomSkeleton strips all PII attrs and text, preserves data-*/role/structure, caps at 4000 chars, never mutates the live DOM; D4 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validation gate (validateCandidate) with D1/D2/D5/D6 tests</name>
  <files>src/content/selector/validator.ts, src/content/selector/validator.test.ts</files>
  <read_first>
    - src/content/exclusions.ts:29-84 (analog: typed result interface + sequential early-return gate pattern to mirror)
    - src/content/exclusions.test.ts:1-119 (Vitest describe/it structure and adversarial loop pattern)
    - 23-RESEARCH.md "Candidate Validation Gate" section (the 3 gates, BLOCKLIST_EXACT, DANGEROUS_TOKEN_RE, CLASS_ID_RE, author-link ratio, post-text presence, advisory sponsored warning)
    - 23-AI-SPEC.md Section 5 dimensions D1, D2, D5, D6 + Section 6 guardrails (these are the authoritative pass/fail rubrics)
    - 23-PATTERNS.md "src/content/selector/validator.ts" section (analog: exclusions.ts)
  </read_first>
  <behavior>
    - `validateCandidate('body', container)` → pass=false (blocklisted exact); same for `'html'`, `'*'`, `':root'`.
    - `validateCandidate('");alert(1)//', container)` and `'<script>alert(1)</script>'` → pass=false (dangerous token), before any querySelectorAll.
    - `validateCandidate('.feed-shared-update-v2', container)` and `'#main-content'` → pass=false (CSS class/id forbidden, CLAUDE.md constraint #1, D6).
    - A syntactically invalid selector → pass=false reason 'invalid CSS selector syntax' (caught from querySelectorAll throw).
    - A selector matching 1 element → pass=false (too few, D8/ADAPT-08); matching >50 → pass=false (too broad, ADAPT-07).
    - A selector matching 2-50 elements where >50% contain `a[href*="/in/"]` AND at least one has post-body text → pass=true, matchCount correct (D5).
    - A selector matching skeleton/job-card elements (in 2-50 range but author-link ratio <=50% or no text) → pass=false (D5).
    - A passing match set containing a Promoted marker logs a console.warn but still returns pass=true (D9 advisory, non-blocking per CONTEXT.md).
  </behavior>
  <action>
    Write `validateCandidate(selector: string, container: Element): ValidationResult` and export `interface ValidationResult { pass: boolean; reason: string; matchCount: number }`, mirroring the sequential early-return gate style of checkExclusions. Gate order: (0) BLOCKLIST_EXACT Set of `['body','html','*',':root']` reject; (0a) DANGEROUS_TOKEN_RE `/eval|function\s*\(|javascript:|<|>|\bwindow\b|\bdocument\b/i` reject (ADAPT-06); (0b) CLASS_ID_RE reject any `.`/`#` token (CLAUDE.md constraint #1, D6); then `container.querySelectorAll(selector)` inside try/catch — the selector string is ONLY ever passed to querySelectorAll, NEVER to eval/new Function/innerHTML; (1) match count <2 or >50 reject (ADAPT-07/08); (2) author-link ratio: fraction of matches containing `a[href*="/in/"]` must be >0.5 else reject; (3) post-text presence: at least one match has trimmed textContent length >10 else reject. Add the advisory-only sponsored-contamination `console.warn` (matches containing `[aria-label*="Promoted"]` / `[aria-label*="Sponsored"]`) — log only, never a write-blocker (CONTEXT.md decision). Write validator.test.ts with jsdom fixtures and an `ADVERSARIAL_SELECTORS` array driving a reject-loop covering every <behavior> bullet. Cite ADAPT-06/07/08 in the JSDoc.
  </action>
  <verify>
    <automated>cd C:/Git/linkedin.blocker && npx vitest run src/content/selector/validator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All validator.test.ts cases pass including the full adversarial reject-loop (D2/D6).
    - Source assertion: validator.ts contains exactly one `querySelectorAll` call and contains NO `eval(`, `new Function(`, or `.innerHTML` (grep `eval\(|new Function|\.innerHTML` in validator.ts returns no match).
    - Source assertion: validator.ts contains the literal match bounds `< 2` and `> 50` (or `count < 2` / `count > 50`).
    - A test asserts a Promoted-contaminated but otherwise-valid match set returns pass=true AND triggers console.warn (spied).
  </acceptance_criteria>
  <done>validateCandidate enforces injection/class guards then 2-50 count + >50% author ratio + post-text gates, treats the selector only as a querySelectorAll argument, and logs sponsored contamination without blocking; all D1/D2/D5/D6 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn page DOM → content-script utilities | Untrusted page content enters sanitizer (to be serialized) and validator (selector run against it) |
| content-script → service-worker (future, set up here) | The skeleton this plan's sanitizer produces is what crosses to the SW in 23-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `sanitizer.buildDomSkeleton` | mitigate | Strip text nodes + href/src/aria-label/title/alt on a clone before serialization; D4 tests assert no `/in/`, `media.licdn.com`, or author-name token survives (ADAPT-04). HIGH. |
| T-23-02 | Elevation of Privilege | `validator.validateCandidate` | mitigate | Selector string passed only to `querySelectorAll`; reject `eval`/`function(`/`javascript:`/`<`/`>` tokens and body/html/*/:root before query (ADAPT-06). HIGH. |
| T-23-03 | Tampering | `validator` write-gate | mitigate | 2-50 match bound + author-link ratio + post-text gates return pass=false on bad candidates so 23-02/23-04 never write them (ADAPT-07/08). MEDIUM/HIGH. |
| T-23-SC | Tampering | npm installs | accept | No new packages installed this plan (vitest/jsdom already declared); audit table not required per RESEARCH.md. |
</threat_model>

<verification>
- `npx vitest run` executes and the sanitizer + validator suites pass.
- `npm run type-check` clean.
- `npm run lint` clean for the two new files.
- Grep gate: `validator.ts` and `sanitizer.ts` contain no `eval(`, `new Function(`, or `.innerHTML` write.
</verification>

<success_criteria>
- `npm test` script exists and runs the Vitest suite green.
- `buildDomSkeleton` strips all PII (D4) and caps the skeleton at 4000 chars without mutating the live DOM.
- `validateCandidate` enforces injection/class guards + the three locked validation gates (D1/D2/D5/D6).
- StorageSchema carries `llbRederiveLastCallMs`, `llbRederiveCallsToday`, `llbRederiveDateKey`, `llbRederiveInFlight`.
</success_criteria>

<output>
Create `.planning/phases/23-self-healing-selector-adapter/23-01-SUMMARY.md` when done.
</output>
