---
phase: 23-self-healing-selector-adapter
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/background/index.ts
  - src/background/ratelimit.test.ts
  - src/content/detector/rederiver.ts
autonomous: true
requirements: [ADAPT-03, ADAPT-05, ADAPT-06]
must_haves:
  truths:
    - "A REDERIVE_SELECTOR message reaching the service worker triggers an Anthropic fetch to claude-haiku-4-5-20251001 with max_tokens 256, only after the rate-limit check passes"
    - "While the single-flight latch is held, a second REDERIVE_SELECTOR does not call fetch"
    - "A second call within the 5-minute cool-off is refused; calls beyond the per-day cap of 5 are refused; all state survives a simulated service-worker restart (read back from chrome.storage.local)"
    - "The LLM response is parsed through isRederiveModelOutput and the selector strings are returned to the content script unevaluated (never eval/Function/innerHTML)"
  artifacts:
    - path: "src/background/index.ts"
      provides: "rederiveSelector() + checkRateLimit/acquire/release + REDERIVE_SELECTOR onMessage branch + REDERIVE_SYSTEM_PROMPT"
      contains: "REDERIVE_SELECTOR"
    - path: "src/content/detector/rederiver.ts"
      provides: "LLMRederiver.rederive(target, domSkeleton) — content-script message sender"
      exports: ["LLMRederiver"]
    - path: "src/background/ratelimit.test.ts"
      provides: "D8 rate-bounding assertions with mocked fetch + chrome.storage + fake timers"
  key_links:
    - from: "src/content/detector/rederiver.ts"
      to: "chrome.runtime.sendMessage({ type: 'REDERIVE_SELECTOR' })"
      via: "Promise-wrapped message — mirrors LLMDetector"
      pattern: "REDERIVE_SELECTOR"
    - from: "src/background/index.ts rederiveSelector"
      to: "api.anthropic.com/v1/messages"
      via: "fetch with model claude-haiku-4-5-20251001, gated by checkRateLimit"
      pattern: "claude-haiku-4-5-20251001"
---

<objective>
Add the service-worker LLM fallback for selector re-derivation: `rederiveSelector()` (mirrors the existing `scorePost` fetch pattern exactly, but with model `claude-haiku-4-5-20251001`, `max_tokens: 256`, the `REDERIVE_SYSTEM_PROMPT`, and `isRederiveModelOutput` validation), the persisted rate-limit guard (single-flight latch + 5-min cool-off + per-day cap of 5, all in `chrome.storage.local`), the `REDERIVE_SELECTOR` onMessage branch, and the content-script `LLMRederiver` sender (mirrors `LLMDetector`).

Purpose: Deliver ADAPT-03 (LLM proposes candidates from a sanitized skeleton — sanitizer lives in 23-01, called by 23-04), ADAPT-05 (rate-bounding persisted across SW restarts), and ADAPT-06 (response strictly validated, selector never evaluated). This plan is file-disjoint from 23-02 so both run in Wave 2.
Output: `rederiveSelector` + rate-limit functions + REDERIVE_SELECTOR handler in background/index.ts; `LLMRederiver` in rederiver.ts; rate-limit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-self-healing-selector-adapter/23-AI-SPEC.md
@.planning/phases/23-self-healing-selector-adapter/23-RESEARCH.md
@.planning/phases/23-self-healing-selector-adapter/23-PATTERNS.md
@CLAUDE.md
@.planning/phases/23-self-healing-selector-adapter/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rederiveSelector + rate-limit guard + REDERIVE_SELECTOR handler</name>
  <files>src/background/index.ts, src/background/ratelimit.test.ts</files>
  <read_first>
    - src/background/index.ts:97-155 (scorePost fetch pattern + SCORE_POST onMessage branch with `return true` — the exact structure to replicate)
    - 23-AI-SPEC.md Section 3 (rederiveSelector is specified VERBATIM — use the REDERIVE_SYSTEM_PROMPT and request body as written) + Section 4 (model config) + Section 4b.1 (isRederiveModelOutput type guard + 2-attempt retry) + Section 4b.5 (cap rationale)
    - 23-RESEARCH.md "Rate-Bounding State" section (storage keys, checkRateLimit/acquireRateLimitLatch/releaseRateLimitLatch, 5-min cool-off, daily cap 5, latch-before-fetch rule) + "Common Pitfalls" 1 and 6
    - 23-PATTERNS.md "src/background/index.ts (modify ...)" + "chrome.storage.local direct reads in service worker" + "return true in async onMessage branch" sections
    - src/shared/types.ts StorageSchema (the four llbRederive* keys added in 23-01)
  </read_first>
  <behavior>
    - With a configured apiKey and a passing rate-limit check, rederiveSelector('POST_CARD', skeleton) calls fetch once to https://api.anthropic.com/v1/messages with model 'claude-haiku-4-5-20251001' and max_tokens 256, and returns `{ candidates: [{selector, rationale}, ...] }`.
    - With no apiKey configured, rederiveSelector throws 'No API key configured' before any fetch.
    - checkRateLimit returns allowed=false when llbRederiveInFlight is true (single-flight).
    - checkRateLimit returns allowed=false when now - llbRederiveLastCallMs < 5*60*1000 (cool-off).
    - checkRateLimit returns allowed=false when llbRederiveCallsToday >= 5 for today's UTC date key (daily cap); a new date key resets the count to 0.
    - acquireRateLimitLatch writes llbRederiveInFlight=true + lastCallMs + incremented count + date key to storage BEFORE fetch; releaseRateLimitLatch sets inFlight=false (called in finally).
    - Simulated SW restart: after acquire writes to the mock storage, a fresh checkRateLimit reading that same mock storage sees inFlight=true and refuses (state survived).
    - A malformed JSON / schema-invalid response is retried once (2 attempts total) then throws; the handler responds with `{ error }` and leaves storage's broken selector untouched.
    - The REDERIVE_SELECTOR onMessage branch returns true (channel kept open) and on rate-limit rejection responds `{ error: 'rate-limited: <reason>' }` without calling fetch.
  </behavior>
  <action>
    In src/background/index.ts add `REDERIVE_SYSTEM_PROMPT` (copy verbatim from AI-SPEC Section 3, including the GOOD/BAD response examples from 4b.3) and `async function rederiveSelector(target, domSkeleton)` replicating scorePost's structure: read apiKey from chrome.storage.local (throw if absent), fetch /v1/messages with the same headers (including `anthropic-dangerous-direct-browser-access: true` and the prompt-caching beta header), `model: 'claude-haiku-4-5-20251001'`, `max_tokens: 256`, system block with REDERIVE_SYSTEM_PROMPT, user content `Target: ${target}\n\nDOM skeleton:\n${domSkeleton}`, `stop_sequences: ['\n\n\n']`; HTTP error check; markdown-fence strip; JSON.parse; validate with an `isRederiveModelOutput(value): value is {candidates:{selector,rationale}[]}` type guard (AI-SPEC 4b.1); retry up to 2 attempts on parse/schema failure (do NOT retry on HTTP 401/429); return `{ candidates }`. Add rate-limit constants `REDERIVE_COOLOFF_MS = 5*60*1000` and `REDERIVE_DAILY_CAP = 5`, and `checkRateLimit()`, `acquireRateLimitLatch(todayKey, callsToday)`, `releaseRateLimitLatch()` using direct `chrome.storage.local.get/set` on the keys `llbRederiveInFlight`, `llbRederiveLastCallMs`, `llbRederiveCallsToday`, `llbRederiveDateKey` (consistent spelling — NOT `llbRedeive...`). Add the `REDERIVE_SELECTOR` branch to the onMessage listener: call checkRateLimit; if not allowed respond `{ error: 'rate-limited: ' + reason }`; else acquire latch (writing state BEFORE the fetch — Pitfall 1), call rederiveSelector, respond `{ result: candidates }`, `.catch` → `{ error }`, `.finally` → releaseRateLimitLatch; `return true` (Pitfall 6 — never omit). Write src/background/ratelimit.test.ts with a vi.stubGlobal in-memory chrome.storage mock (PATTERNS.md pattern), a mocked global fetch, and fake timers, covering every <behavior> bullet, asserting fetch call counts for latch/cool-off/cap/restart scenarios. Cite ADAPT-05/ADAPT-06 in comments.
  </action>
  <verify>
    <automated>cd C:/Git/linkedin.blocker && npx vitest run src/background/ratelimit.test.ts && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - All ratelimit.test.ts cases pass (D8): latch-held → 0 fetch calls; within-cool-off → refused; at-cap → refused; new-date → count resets; restart-survival → refused after acquire (ADAPT-05).
    - Source assertion: background/index.ts contains the literal `claude-haiku-4-5-20251001` and `max_tokens: 256` and `REDERIVE_SELECTOR` and a `return true` inside that branch.
    - Source assertion: background/index.ts contains no `eval(`, `new Function(`, or `.innerHTML`; the parsed selector strings are returned as data, never executed (ADAPT-06).
    - Source assertion: the latch write (`llbRederiveInFlight: true`) happens in acquireRateLimitLatch which is called BEFORE rederiveSelector in the handler (Pitfall 1).
    - Storage keys use the spelling `llbRederiveDateKey` everywhere (grep returns no `llbRedeiveDateKey`).
  </acceptance_criteria>
  <done>rederiveSelector calls Haiku 4.5 with max_tokens 256 only after a persisted rate-limit check passes; latch/cool-off/cap survive SW restart; response is schema-validated and returned unevaluated; handler returns true.</done>
</task>

<task type="auto">
  <name>Task 2: LLMRederiver content-script sender</name>
  <files>src/content/detector/rederiver.ts</files>
  <read_first>
    - src/content/detector/llm.ts:11-35 (LLMDetector — copy the class + sendMessage Promise structure verbatim, swapping SCORE_POST → REDERIVE_SELECTOR)
    - 23-AI-SPEC.md Section 4 "Core Pattern" (LLMRederiver shape) + Common Pitfall 3 (must run from SW, content script only sends the message)
    - 23-PATTERNS.md "src/content/detector/rederiver.ts" section
  </read_first>
  <action>
    Write `export class LLMRederiver` with `async rederive(target: string, domSkeleton: string): Promise<Array<{ selector: string; rationale: string }>>` that returns a Promise wrapping `chrome.runtime.sendMessage({ type: 'REDERIVE_SELECTOR', target, domSkeleton }, cb)`: reject on `chrome.runtime.lastError`, reject on `response?.error`, else resolve `response.result`. Mirror LLMDetector's error-handling shape exactly. Do NOT call fetch here — the CORS-restricted Anthropic call lives in the service worker (Pitfall 3). The caller in 23-04 wraps this in a try/catch that releases the content-side heal lock on failure. No new imports beyond what the message shape needs. Reference ADAPT-03/ADAPT-06 in the JSDoc.
  </action>
  <verify>
    <automated>cd C:/Git/linkedin.blocker && npm run type-check && npm run lint -- src/content/detector/rederiver.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: rederiver.ts sends `type: 'REDERIVE_SELECTOR'` and contains no `fetch(` call (the API call is in the SW).
    - Source assertion: rederiver.ts rejects on both `chrome.runtime.lastError` and `response?.error` (mirrors LLMDetector).
    - `npm run type-check` clean.
  </acceptance_criteria>
  <done>LLMRederiver sends REDERIVE_SELECTOR and resolves the candidate array, mirroring LLMDetector; no fetch in the content script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content-script → service-worker | REDERIVE_SELECTOR message carries the sanitized skeleton (sanitized in 23-01/23-04) |
| service-worker → api.anthropic.com | Sanitized skeleton + user's API key leave the browser |
| Anthropic response → content-script | Model-proposed selector strings re-enter the extension as untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Information Disclosure | API key handling | mitigate | apiKey read from chrome.storage.local in the SW only, sent only in the `x-api-key` header, never logged, never returned to the page (ADAPT-05 supporting). MEDIUM. |
| T-23-07 | Denial of Service | runaway API spend | mitigate | Single-flight latch + 5-min cool-off + per-day cap of 5, persisted to storage and re-read each invocation so they survive SW restart; latch written before fetch (ADAPT-05). MEDIUM. |
| T-23-02 | Elevation of Privilege | model selector string | mitigate | Response parsed by isRederiveModelOutput and returned as data; selector is only ever a querySelectorAll argument in the 23-01 validator; never eval/Function/innerHTML in the SW (ADAPT-06). HIGH. |
| T-23-SC | Tampering | npm installs | accept | No new packages (direct fetch, no SDK per AI-SPEC Section 2); audit not required. |
</threat_model>

<verification>
- `npx vitest run src/background/ratelimit.test.ts` green; fetch call-count assertions hold for latch/cool-off/cap/restart.
- `npm run type-check` clean.
- Grep gate: background/index.ts and rederiver.ts contain no `eval(`, `new Function(`, `.innerHTML`.
- Grep gate: storage keys spelled `llbRederiveDateKey` (no `llbRedeiveDateKey`).
- The REDERIVE_SELECTOR onMessage branch ends with `return true`.
</verification>

<success_criteria>
- rederiveSelector calls Haiku 4.5 (max_tokens 256) only when rate-limit allows, validates the response, returns candidates unevaluated (ADAPT-03/ADAPT-06).
- Rate-bounding (single-flight + 5-min cool-off + daily cap 5) is persisted and survives SW restart (ADAPT-05).
- LLMRederiver sends REDERIVE_SELECTOR mirroring LLMDetector.
</success_criteria>

<output>
Create `.planning/phases/23-self-healing-selector-adapter/23-03-SUMMARY.md` when done.
</output>
