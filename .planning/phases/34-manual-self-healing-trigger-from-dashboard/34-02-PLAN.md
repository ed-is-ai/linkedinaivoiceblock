---
phase: 34-manual-self-healing-trigger-from-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/heal-messages.ts
  - src/tools/library/dom-selector-rederive/heal.ts
autonomous: true
requirements: [HEAL-03, HEAL-05]

must_haves:
  truths:
    - "triggerHeal accepts the live container and heals every currently-stale, DOM-healable target, not only POST_CARD"
    - "Card-shaped targets route to the heuristic deriver; sub-element targets route to the LLM rederiver"
    - "When no API key is configured, sub-element targets report 'unchanged' instead of erroring"
    - "COMPANY_PAGE_MARKER and attribute-name targets are excluded from the heal set"
    - "Every written candidate passes validateCandidate before insertCandidate; no second write path exists"
    - "triggerHeal returns a per-target outcome list"
  artifacts:
    - path: "src/shared/heal-messages.ts"
      provides: "Single pinned TRIGGER_HEAL request/response envelope + HealOutcome type shared by content and dashboard"
      exports: ["TRIGGER_HEAL", "HealOutcome", "TriggerHealMessage", "TriggerHealResponse"]
    - path: "src/tools/library/dom-selector-rederive/heal.ts"
      provides: "Generalized triggerHeal(container) that heals all stale targets and returns HealOutcome[]"
      contains: "HealOutcome"
  key_links:
    - from: "src/tools/library/dom-selector-rederive/heal.ts"
      to: "src/content/selector-registry.ts"
      via: "validateCandidate then insertCandidate (the only write path)"
      pattern: "insertCandidate"
    - from: "src/tools/library/dom-selector-rederive/heal.ts"
      to: "src/shared/heal-messages.ts"
      via: "imports HealOutcome return type"
      pattern: "HealOutcome"
---

<objective>
Generalize the heal pipeline from `POST_CARD`-only to all currently-stale DOM-healable targets (HEAL-03 / D-03, D-04, D-05), and pin the `TRIGGER_HEAL` request/response message contract in ONE shared module so the content-side listener (Plan 03) and the dashboard sender (Plan 04) agree without divergence.

`triggerHeal` is hardcoded today: `deriveHeuristicCandidates('POST_CARD', container)`, `insertCandidate('POST_CARD', …)`, `rederiver.rederive('POST_CARD', skeleton)`. Generalize it to iterate an internal stale-target list, route each target by shape, and return a per-target outcome — while preserving the validate-before-write gate (HEAL-05 / D-07) untouched.

Purpose: This is the core enabling change. The content listener and dashboard UI both depend on the generalized signature and the pinned envelope.
Output: New `src/shared/heal-messages.ts` (contract); generalized `heal.ts` returning `HealOutcome[]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-manual-self-healing-trigger-from-dashboard/34-CONTEXT.md
@.planning/phases/34-manual-self-healing-trigger-from-dashboard/34-PATTERNS.md
@CLAUDE.md
@src/tools/library/dom-selector-rederive/heal.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the TRIGGER_HEAL message contract + HealOutcome type</name>
  <read_first>
    - src/shared/types.ts (SelectorTarget union — import the type; do NOT modify it here, Plan 01 owns it)
    - src/background/index.ts (lines 199-222 — the typed message envelope precedent the contract must match: `message?.type` discrimination, `{ result }` / `{ error }` two-shape response)
    - src/tools/library/dom-selector-rederive/rederiver.ts (lines 26-49 — the promise-wrapped sender shape that consumes `{ result }` / `{ error }`)
  </read_first>
  <action>
    Create `src/shared/heal-messages.ts` as the single source of truth for the dashboard↔content heal contract:
    - Export a `TRIGGER_HEAL` string-literal constant (value `'TRIGGER_HEAL'`) used as the message `type` discriminator on both sides.
    - Export `HealResult` as a string-literal union `'healed' | 'unchanged' | 'failed'` (D-06).
    - Export `HealOutcome` as `{ target: SelectorTarget; result: HealResult }` importing `SelectorTarget` from `../shared/types` (relative path from this module's location).
    - Export `TriggerHealMessage` as `{ type: typeof TRIGGER_HEAL }` (the request envelope; no payload — the content side resolves its own live container per D-09).
    - Export `TriggerHealResponse` as `{ result: HealOutcome[] } | { error: string }` matching the existing two-shape `{ result } | { error }` contract.

    Do NOT add a `chrome.tabs` call here — this module is pure types/constants, importable by both an extension page (dashboard) and the content script.
  </action>
  <verify>
    <automated>cd c:/Git/linkedin.blocker && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/shared/heal-messages.ts` exports `TRIGGER_HEAL`, `HealResult`, `HealOutcome`, `TriggerHealMessage`, `TriggerHealResponse`.
    - Source assertion: `HealOutcome` references `SelectorTarget` imported from `../shared/types` (no inline duplicate union).
    - Source assertion: file contains NO `chrome.` API calls (pure contract module).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>The heal message contract exists in one place and type-checks; both Plan 03 and Plan 04 can import it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize triggerHeal to heal all stale targets with shape-based routing</name>
  <read_first>
    - src/tools/library/dom-selector-rederive/heal.ts (lines 60-101 — the current hardcoded-POST_CARD body to generalize in place)
    - src/tools/library/dom-selector-rederive/heuristic.ts (lines 83-86 — `deriveHeuristicCandidates(target: 'POST_CARD' | 'POST_BODY_TEXT', container)` signature; do NOT widen this signature — card-shaped heal set is these two only)
    - src/tools/library/dom-selector-rederive/rederiver.ts (line 26 — `LLMRederiver.rederive(target: string, domSkeleton)` already generic; requires API key via the SW)
    - src/content/selector-registry.ts (insertCandidate near line 337, validateCandidate usage — the only sanctioned write path; recordMiss / selectorSessionMisses near lines 394-401 for the stale signal)
    - src/shared/heal-messages.ts (HealOutcome — the return element type, created in Task 1)
    - src/content/selectors.ts (COMPANY_PAGE_MARKER and POST_URN_ATTR values — confirm which targets are URL-substring / attribute-name strings to EXCLUDE)
  </read_first>
  <behavior>
    - Given a stale card-shaped target (POST_CARD or POST_BODY_TEXT) and a container where the heuristic deriver yields a candidate that passes validateCandidate, triggerHeal calls insertCandidate(target, …) and records that target's outcome as 'healed'.
    - Given a stale sub-element target (e.g. SPONSORED_MARKER) with NO API key configured, triggerHeal records 'unchanged' for that target and does NOT throw.
    - Given a stale sub-element target WITH an API key, triggerHeal calls rederiver.rederive(target, skeleton); a validated candidate yields 'healed', exhausted/invalid candidates yield 'failed'.
    - COMPANY_PAGE_MARKER (URL substring) and attribute-name targets (POST_URN_ATTR) never appear in the returned outcome list (excluded from heal set, D-05).
    - When heuristic candidates all fail validation for a card-shaped target and no LLM route applies, that target's outcome is 'failed'.
    - Every insertCandidate call is preceded by a passing validateCandidate (no candidate written unvalidated, D-07).
  </behavior>
  <action>
    Generalize `triggerHeal(container: Element)` in place to return `Promise<HealOutcome[]>` (imported from `src/shared/heal-messages`). Per D-08 discretion, keep an INTERNAL stale-target list so the whole heal stays inside one guarded invocation (the caller's single-flight latch wraps the entire promise — Plan 03 owns that guard).

    Build the heal set: derive currently-stale targets from the existing staleness signal (`selectorSessionMisses` in `chrome.storage.local`, written by `recordMiss`), intersected with the DOM-healable target set. Do NOT invent a new staleness metric. Exclude `COMPANY_PAGE_MARKER` (URL substring) and attribute-name targets such as `POST_URN_ATTR` (not querySelector inputs) per D-05.

    Route each target by shape (D-04):
    - Card-shaped (`POST_CARD`, `POST_BODY_TEXT`) → `deriveHeuristicCandidates(target, container)`; loop candidates, `validateCandidate(candidate.selector, container)`, and on the first pass call `insertCandidate(target, candidate.selector, 'heuristic')` and record `'healed'`. If none validate, record `'failed'`.
    - Sub-element (`SPONSORED_MARKER`, `AUTHOR_HEADLINE`, `CONNECTION_DEGREE`, `COMMENT_EXPAND_BUTTON`, `COMMENT_TEXT`, `OPEN_TO_WORK_MARKER`) → require the configured API key via `storageGet(['anthropicApiKey'])`. If absent, record `'unchanged'` and continue (degrade gracefully, D-04). If present, `buildDomSkeleton(container)`, `new LLMRederiver().rederive(target, skeleton)`, then validate-then-insert with source `'llm'`; record `'healed'` / `'failed'` accordingly. Catch rederive errors per target and record `'failed'` — never let one target's failure abort the others.

    Preserve the existing validate-before-write loop structure exactly; `insertCandidate` remains the ONLY write surface (CLAUDE.md #1, D-07). Do NOT widen the `deriveHeuristicCandidates` signature. Update the file's doc comment to describe the generalized multi-target behavior. Add or update co-located unit tests proving the behaviors above (mock the registry write path and `storageGet`).
  </action>
  <verify>
    <automated>cd c:/Git/linkedin.blocker && npm run type-check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `triggerHeal` return type is `Promise<HealOutcome[]>` and the body contains no literal `'POST_CARD'` hardcoding in the heuristic/insert/rederive calls (target is parameterized per iteration).
    - Source assertion: COMPANY_PAGE_MARKER and POST_URN_ATTR do not appear as arguments to `deriveHeuristicCandidates`, `rederive`, or `insertCandidate`.
    - Source assertion: every `insertCandidate(` call site is guarded by a preceding `validateCandidate(` whose `.pass` is checked.
    - Behavior assertion (test): no-API-key sub-element target yields `'unchanged'`, not a thrown error.
    - Behavior assertion (test): a validated heuristic candidate for a card target yields `'healed'` and calls insertCandidate exactly once for that target.
    - `npm run type-check` and `npm test` exit 0.
  </acceptance_criteria>
  <done>triggerHeal heals all stale DOM-healable targets via shape-based routing, returns HealOutcome[], preserves the validate-before-write gate, and degrades gracefully with no API key; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-proposed selector string → registry | LLM/heuristic candidate strings are untrusted data crossing into stored selectors |
| service-worker LLM call | rederive routes through the SW, which holds the network/API-key boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | model/heuristic candidate written without validation | mitigate | Every candidate passes `validateCandidate` before `insertCandidate`; `insertCandidate` is the only write surface (CLAUDE.md #1, D-07); candidate strings are passed only to querySelectorAll, never eval'd (ADAPT-06) |
| T-34-04 | Information Disclosure | DOM skeleton sent to the LLM | mitigate | `buildDomSkeleton` PII-strips before send (existing); reused unchanged |
| T-34-05 | Denial of Service | one target's heal error aborting the whole set | mitigate | Per-target try/catch records `'failed'` and continues; pipeline-level errors only for missing container (handled by caller) |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- `npm run type-check` and `npm test` green.
- Heal set excludes `COMPANY_PAGE_MARKER` and attribute-name targets.
- No second selector-write path introduced.
</verification>

<success_criteria>
- `triggerHeal(container)` returns `HealOutcome[]`, heals all stale DOM-healable targets with heuristic/LLM routing, degrades gracefully without an API key.
- The `TRIGGER_HEAL` contract lives in one shared module imported by both downstream sides.
- validate-before-write gate preserved; no new write surface.
</success_criteria>

<output>
Create `.planning/phases/34-manual-self-healing-trigger-from-dashboard/34-02-SUMMARY.md` when done
</output>
