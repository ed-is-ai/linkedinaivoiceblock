---
phase: 34-manual-self-healing-trigger-from-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/selectors.ts
  - src/content/selector-registry.ts
  - src/shared/types.ts
autonomous: true
requirements: [HEAL-06]

must_haves:
  truths:
    - "POST_AUTHOR_NAME and POST_URN_ATTR_FALLBACK no longer appear as Selector Health rows"
    - "npm test stays green after removal"
    - "npm run type-check stays green after removal"
    - "The seven other blank last-matched selectors remain present and untouched"
  artifacts:
    - path: "src/content/selectors.ts"
      provides: "Seed selector strings with the two dead exports removed"
      contains: "POST_AUTHOR_LINK"
    - path: "src/content/selector-registry.ts"
      provides: "SEED_MAP exhaustive over the trimmed SelectorTarget union"
      contains: "SEED_MAP"
    - path: "src/shared/types.ts"
      provides: "SelectorTarget union with the two dead members removed"
      contains: "SelectorTarget"
  key_links:
    - from: "src/content/selector-registry.ts"
      to: "src/shared/types.ts"
      via: "SEED_MAP typed Record<SelectorTarget, string> must stay exhaustive"
      pattern: "Record<SelectorTarget"
---

<objective>
Remove the two genuinely-dead selectors `POST_AUTHOR_NAME` and `POST_URN_ATTR_FALLBACK` (zero `resolve()` consumers anywhere; their logic is covered by `POST_AUTHOR_LINK` and `POST_URN_ATTR` respectively) from all three sites so they no longer clutter the dashboard Selector Health tab as rows that can never match (HEAL-06 / D-10).

This is a NARROW removal. The other `last matched = —` rows (`SPONSORED_MARKER`, `OPEN_TO_WORK_MARKER`, `AUTHOR_HEADLINE`, `CONNECTION_DEGREE`, `COMMENT_EXPAND_BUTTON`, `COMMENT_TEXT`, `RESHARE_INDICATOR`) are all LIVE and MUST be kept — their blank cells are an instrumentation artifact, not dead code (D-11).

Purpose: Reduce Selector Health noise before the heal-all-stale feature ships, so the manual heal set is computed over only real, healable targets.
Output: Trimmed `selectors.ts`, `selector-registry.ts`, and `SelectorTarget` union; green test + type-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-manual-self-healing-trigger-from-dashboard/34-CONTEXT.md
@.planning/phases/34-manual-self-healing-trigger-from-dashboard/34-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm both selectors are dead, then remove from all three sites</name>
  <read_first>
    - src/content/selectors.ts (the two `export const`s at lines 62 and 73; the verification comment at line 98 that names POST_AUTHOR_NAME)
    - src/content/selector-registry.ts (the import block lines 31/33 and SEED_MAP entries lines 66/68; the resolve() doc comment near line 211 that mentions POST_URN_ATTR_FALLBACK)
    - src/shared/types.ts (the SelectorTarget union members at lines 346 and 348; the note comment near line 337 naming POST_URN_ATTR_FALLBACK)
    - src/content/observer.ts (lines 91-104 and line 150 — confirm POST_AUTHOR_LINK anchor reads the author name and POST_URN_ATTR is the only URN attr resolved)
  </read_first>
  <action>
    First, prove deadness: run a repo-wide search for any `resolve('POST_AUTHOR_NAME')`, `resolve('POST_URN_ATTR_FALLBACK')`, or any other consumer reference (bare token usage) across `src/`. If ANY `resolve()` consumer exists for either name, STOP and report — do not delete. Expect zero consumers (author name comes from the `POST_AUTHOR_LINK` anchor via `authorAnchor.querySelector('strong')` plus span/text fallbacks in `observer.ts`; `POST_URN_ATTR` is the only URN attribute resolved).

    Then remove each of `POST_AUTHOR_NAME` and `POST_URN_ATTR_FALLBACK` from three sites, mirroring how every other member appears:
    1. `src/content/selectors.ts` — delete the two `export const` declarations (`POST_URN_ATTR_FALLBACK = 'componentkey'` and `POST_AUTHOR_NAME = 'a[href*="/in/"]:has(strong) strong'`). Update the verification comment that names `POST_AUTHOR_NAME` so it no longer references a removed export.
    2. `src/content/selector-registry.ts` — remove both names from the import block AND both entries from `SEED_MAP` (typed `Record<SelectorTarget, string>`, so it must stay exhaustive over the trimmed union). Update the `resolve()` doc comment that mentions `POST_URN_ATTR_FALLBACK`.
    3. `src/shared/types.ts` — remove the two members from the `SelectorTarget` union and drop `POST_URN_ATTR_FALLBACK` from the note comment near the union.

    Keep `POST_URN_ATTR`, `POST_AUTHOR_LINK`, and `COMPANY_PAGE_MARKER` untouched. Do NOT touch the seven live blank-last-matched selectors named in D-11.
  </action>
  <verify>
    <automated>cd c:/Git/linkedin.blocker && npm run type-check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -rn "POST_AUTHOR_NAME\|POST_URN_ATTR_FALLBACK" src/` returns NO matches in `selectors.ts`, `selector-registry.ts`, or `types.ts` (and no `resolve()` consumer anywhere).
    - Source assertion: `POST_URN_ATTR`, `POST_AUTHOR_LINK`, and all seven D-11 live selectors (`SPONSORED_MARKER`, `OPEN_TO_WORK_MARKER`, `AUTHOR_HEADLINE`, `CONNECTION_DEGREE`, `COMMENT_EXPAND_BUTTON`, `COMMENT_TEXT`, `RESHARE_INDICATOR`) still present in all relevant sites.
    - `npm run type-check` exits 0 (proves `SEED_MAP` stays exhaustive over the trimmed `SelectorTarget` union).
    - `npm test` exits 0 with no reduction in passing-test count attributable to this change.
  </acceptance_criteria>
  <done>Both dead selectors removed from all three sites; the seven live blank-last-matched selectors kept; type-check and tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time source → runtime registry | `SEED_MAP` seeds the registry; an inexhaustive map would break selector resolution at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | SEED_MAP / SelectorTarget union drift | mitigate | `Record<SelectorTarget, string>` typing + `npm run type-check` gate forces map and union to change together; removal is atomic across all three sites |
| T-34-02 | Denial of Service | accidental removal of a live selector | mitigate | Pre-removal `resolve()`-consumer grep gate; D-11 live-selector retention asserted in acceptance criteria |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; nothing to audit |
</threat_model>

<verification>
- `npm run type-check` and `npm test` both green.
- Selector Health tab (manual, optional) shows two fewer rows; the seven live blank rows remain.
</verification>

<success_criteria>
- Exactly two dead selectors removed from `selectors.ts`, `selector-registry.ts` (import + `SEED_MAP`), and the `SelectorTarget` union in `types.ts`.
- No `resolve()` consumer broken; no live selector removed.
- Tests and type-check pass green.
</success_criteria>

<output>
Create `.planning/phases/34-manual-self-healing-trigger-from-dashboard/34-01-SUMMARY.md` when done
</output>
