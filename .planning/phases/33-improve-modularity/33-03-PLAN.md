---
phase: 33-improve-modularity
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - src/shared/tool-registry.ts
  - src/content/generated-skill-registry.ts
  - src/shared/generated-tool-registry.ts
autonomous: true
requirements: [MOD-03]

must_haves:
  truths:
    - "A single codegen mechanism (scripts/generate-skill-registry.ts) produces BOTH the skill registry module and the tool registry module in one invocation"
    - "SkillRegistry and ToolRegistry remain distinct runtime contracts (not collapsed)"
    - "Both generated registry files are clean after all Track 1/2/4 file moves (stale-checks pass)"
    - "The stale documentation comment referencing src/skills/library/<name>/ for tools is corrected to src/tools/library/<name>/"
  artifacts:
    - path: "scripts/generate-skill-registry.ts"
      provides: "Unified codegen emitting both generated modules in one run (already satisfies D-05)"
    - path: "src/content/generated-skill-registry.ts"
      provides: "Generated SkillRegistry seed module (regenerated clean)"
    - path: "src/shared/generated-tool-registry.ts"
      provides: "Generated ToolRegistry seed module (regenerated clean)"
  key_links:
    - from: "scripts/generate-skill-registry.ts"
      to: "src/content/generated-skill-registry.ts"
      via: "fs.writeFileSync (skill output)"
      pattern: "generated-skill-registry"
    - from: "scripts/generate-skill-registry.ts"
      to: "src/shared/generated-tool-registry.ts"
      via: "fs.writeFileSync (tool output)"
      pattern: "generated-tool-registry"
---

<objective>
Track 3 (VERIFICATION-ONLY): confirm MOD-03 / D-05 is satisfied. The single script `scripts/generate-skill-registry.ts` already emits BOTH `src/content/generated-skill-registry.ts` and `src/shared/generated-tool-registry.ts` in one invocation (RESEARCH § Track 3: lines 268 and 327). No code change to the codegen is required. This plan regenerates both files after the file moves in plans 33-01 and 33-02, confirms the stale-check / CI guard passes, and fixes one stale documentation comment in `tool-registry.ts` that still references `src/skills/library/<name>/` for tools (tools moved to `src/tools/library/` in the post-32 refactor).

Purpose: Implements MOD-03 (one shared codegen produces both generated modules; `SkillRegistry` and `ToolRegistry` stay distinct). Honors D-05.
Output: Verified-unified codegen, clean generated files, corrected comment; zero behavior change.

This plan is sequenced after 33-01 and 33-02 because the generated files' import paths reference skill/tool file locations that those plans rewrite; regenerating here proves the codegen tracks the new locations cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-improve-modularity/33-CONTEXT.md
@.planning/phases/33-improve-modularity/33-RESEARCH.md
@.planning/phases/33-improve-modularity/33-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify unified codegen emits both modules; regenerate; confirm distinct registries</name>
  <files>scripts/generate-skill-registry.ts, src/content/generated-skill-registry.ts, src/shared/generated-tool-registry.ts</files>
  <read_first>
    - scripts/generate-skill-registry.ts (confirm a single run writes BOTH outputs — the two fs.writeFileSync calls for skill output and tool output; RESEARCH § Track 3 cites lines 268 and 327)
    - src/shared/tool-registry.ts (the ToolRegistry runtime contract — confirm it is distinct from SkillRegistry, NOT collapsed)
    - src/content/skill-registry.ts (the SkillRegistry runtime contract — confirm distinct)
    - 33-RESEARCH.md § Track 3 (Status: Already Done) and 33-PATTERNS.md § Track 3
  </read_first>
  <action>
    Confirm by reading `scripts/generate-skill-registry.ts` that one invocation writes both `src/content/generated-skill-registry.ts` and `src/shared/generated-tool-registry.ts` (no separate tool-generation script exists) — this is the D-05 unification, already in place. Confirm `SkillRegistry` (`src/content/skill-registry.ts`) and `ToolRegistry` (`src/shared/tool-registry.ts`) remain two separate runtime contracts. Make NO change to the codegen logic. Run `npm run generate-skill-registry` to regenerate both committed files reflecting the new skill/tool/shared locations from plans 33-01 and 33-02, then commit any regenerated output.
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm run generate-skill-registry && npm run check-skill-registry && npm run check-tool-registry</automated>
  </verify>
  <done>
    Single codegen run regenerates both files; both stale-checks exit 0; `SkillRegistry` and `ToolRegistry` confirmed distinct.
  </done>
  <acceptance_criteria>
    - `scripts/generate-skill-registry.ts` is unmodified in codegen logic (a single script still writes both generated modules).
    - `src/content/generated-skill-registry.ts` and `src/shared/generated-tool-registry.ts` both regenerate with no uncommitted diff.
    - `npm run check-skill-registry` exits 0 and `npm run check-tool-registry` exits 0.
    - `SkillRegistry` and `ToolRegistry` are still two separate runtime contracts (D-05: registries not collapsed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix the stale tools-folder comment in tool-registry.ts; run the zero-behavior-change gate</name>
  <files>src/shared/tool-registry.ts</files>
  <read_first>
    - src/shared/tool-registry.ts (locate the comment that references `src/skills/library/<name>/` for tools — RESEARCH § Track 3 notes it around the AUTHORING comment near line 19)
    - 33-RESEARCH.md § Track 3 ("The AUTHORING.md comment ... still reads src/skills/library/<name>/ (stale)") and 33-PATTERNS.md § Track 3 (stale comment to fix)
  </read_first>
  <action>
    Edit only the stale comment text in `src/shared/tool-registry.ts`: change any reference to the tools location from `src/skills/library/<name>/` to `src/tools/library/<name>/` (tools were relocated in the post-32 refactor). This is a comment-only change — do not alter any code, types, or exports. Then run the full zero-behavior-change gate.
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm test && npm run type-check && npm run check-tool-registry</automated>
  </verify>
  <done>
    The comment references `src/tools/library/<name>/`; full test suite + type-check + tool stale-check pass; no code changed.
  </done>
  <acceptance_criteria>
    - `src/shared/tool-registry.ts` no longer contains a `src/skills/library/<name>/` reference for the tools folder; it reads `src/tools/library/<name>/`.
    - The change is comment-only (no diff to any code line, type, or export).
    - `npm test`, `npm run type-check`, `npm run check-tool-registry` all exit 0.
    - Detection golden-score snapshot + exclusion parity byte-identical (asserted by `npm test`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Verification + comment-only change. No new inputs, no new network calls, no new trust boundaries. The codegen is unchanged.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Tampering | Generated registries become stale after the Track 1/2/4 moves | mitigate | Regenerate via the single codegen script and gate on `check-skill-registry` + `check-tool-registry` (git diff --exit-code). |
| T-33-08 | Repudiation | Comment edit accidentally touches code | mitigate | Acceptance criteria assert the change is comment-only; `npm test` + `npm run type-check` confirm no behavior/type change. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. No install tasks exist. |
</threat_model>

<verification>
```
npm run generate-skill-registry
npm run check-skill-registry && npm run check-tool-registry
npm test && npm run type-check
```
All exit 0. This is a verification + doc-comment plan; land as one small atomic commit.
</verification>

<success_criteria>
- One codegen mechanism produces both generated modules in a single run; `SkillRegistry`/`ToolRegistry` remain distinct (MOD-03, D-05).
- Both generated files regenerate clean after all prior-plan moves.
- Stale tools-folder comment corrected.
- `npm test`, `npm run type-check`, both stale-checks green.
</success_criteria>

<output>
Create `.planning/phases/33-improve-modularity/33-03-SUMMARY.md` when done.
</output>
