---
phase: 33-improve-modularity
plan: 04
type: execute
wave: 4
depends_on: [33-01, 33-02, 33-03]
files_modified:
  - src/dashboard/index.html
  - src/dashboard/index.tsx
  - src/dashboard/SelectorView.tsx
  - src/dashboard/dataManagement.ts
  - src/dashboard/dataManagement.test.ts
  - src/dashboard/evals.html
  - src/dashboard/evals.tsx
  - src/dashboard/evalsLabeling.ts
  - src/dashboard/evalsRunEngine.ts
  - src/dashboard/evals.test.ts
  - src/popup/index.html
  - src/popup/index.tsx
  - src/popup/AccountRow.tsx
  - src/popup/BatchBlockBar.tsx
  - src/modules/dashboard/index.html
  - src/modules/dashboard/index.tsx
  - src/modules/dashboard/SelectorView.tsx
  - src/modules/dashboard/dataManagement.ts
  - src/modules/dashboard/dataManagement.test.ts
  - src/modules/evals/evals.html
  - src/modules/evals/evals.tsx
  - src/modules/evals/evalsLabeling.ts
  - src/modules/evals/evalsRunEngine.ts
  - src/modules/evals/evals.test.ts
  - src/modules/popup/index.html
  - src/modules/popup/index.tsx
  - src/modules/popup/AccountRow.tsx
  - src/modules/popup/BatchBlockBar.tsx
  - src/manifest.json
  - vite.config.ts
autonomous: false
requirements: [MOD-05]

must_haves:
  truths:
    - "src/modules/dashboard/, src/modules/evals/, and src/modules/popup/ exist as self-contained peer modules with their own entry points"
    - "src/dashboard/ and src/popup/ no longer exist (all files relocated under src/modules/)"
    - "manifest.json and vite.config.ts resolve all three module HTML entry points; the built extension loads popup, dashboard, and evals pages"
    - "No modules/common/ shared UX folder is created (D-08: nothing shared)"
    - "Full test suite stays green and detection golden-score snapshot + exclusion parity are byte-identical after this track"
  artifacts:
    - path: "src/modules/dashboard/index.html"
      provides: "Dashboard module entry point"
    - path: "src/modules/evals/evals.html"
      provides: "Evals module entry point"
    - path: "src/modules/popup/index.html"
      provides: "Popup module entry point"
    - path: "src/manifest.json"
      provides: "HTML entry points repointed to modules/ paths"
      contains: "modules/popup/index.html"
    - path: "vite.config.ts"
      provides: "additionalInputs repointed to modules/evals/evals.html"
      contains: "modules/evals/evals.html"
  key_links:
    - from: "src/manifest.json"
      to: "src/modules/popup/index.html"
      via: "action.default_popup"
      pattern: "modules/popup/index.html"
    - from: "src/manifest.json"
      to: "src/modules/dashboard/index.html"
      via: "options_ui.page"
      pattern: "modules/dashboard/index.html"
    - from: "vite.config.ts"
      to: "src/modules/evals/evals.html"
      via: "additionalInputs"
      pattern: "modules/evals/evals.html"
---

<objective>
Track 5 (HIGHEST RISK): split the bundled `src/dashboard/` (which holds both the dashboard app and the evals app) and `src/popup/` into three self-contained peer modules under a new `src/modules/` folder — `dashboard/`, `evals/`, `popup/`. Move each module's files, repoint internal `../shared/...` imports to `../../shared/...` (combining the Track 1 / Track 4 path changes already landed in plans 33-01 and 33-02), and repoint the three `manifest.json` HTML entry fields plus `vite.config.ts` `additionalInputs` simultaneously. Then verify the built extension loads all three pages.

Purpose: Implements MOD-05 (`src/modules/{dashboard,evals,popup}/` exist as self-contained peer modules; build config resolves all three). Honors D-07 (three peer modules), D-08 (each module owns its internals; no `modules/common/`), D-09 (build + manifest repoint, verify the built extension loads).
Output: Three peer UX modules; zero behavior change; built extension loads all three pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-improve-modularity/33-CONTEXT.md
@.planning/phases/33-improve-modularity/33-RESEARCH.md
@.planning/phases/33-improve-modularity/33-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move dashboard, evals, and popup files into src/modules/ and repoint all internal imports</name>
  <files>src/dashboard/index.html, src/dashboard/index.tsx, src/dashboard/SelectorView.tsx, src/dashboard/dataManagement.ts, src/dashboard/dataManagement.test.ts, src/dashboard/evals.html, src/dashboard/evals.tsx, src/dashboard/evalsLabeling.ts, src/dashboard/evalsRunEngine.ts, src/dashboard/evals.test.ts, src/popup/index.html, src/popup/index.tsx, src/popup/AccountRow.tsx, src/popup/BatchBlockBar.tsx, src/modules/dashboard/, src/modules/evals/, src/modules/popup/</files>
  <read_first>
    - src/dashboard/index.tsx (imports: ../shared/types, ./dataManagement, ./SelectorView, ../content/selector-registry, storage path now ../shared/memory/storage after plan 33-02)
    - src/dashboard/dataManagement.ts and SelectorView.tsx (../shared/types imports)
    - src/dashboard/evals.tsx (most complex — combines Track 1 skill import + Track 4 pricing/postStore paths + Track 5 depth; see 33-PATTERNS.md § Track 5 full before/after block)
    - src/dashboard/evalsLabeling.ts (postStore import — now ../shared/memory/postStore after plan 33-02) and evalsRunEngine.ts (../shared/eval/index)
    - src/dashboard/evals.test.ts (postStore import)
    - src/popup/index.tsx (../shared/types, ./AccountRow, ./BatchBlockBar) and AccountRow.tsx (../shared/types)
    - src/dashboard/index.html, evals.html, src/popup/index.html (script src is `./index.tsx` / `./evals.tsx` — co-located relative, unchanged after move)
    - 33-RESEARCH.md § Track 5 (File-to-Module Mapping + Internal Import Path Updates) and 33-PATTERNS.md § Track 5 (exact before/after import strings, target folder shapes)
    - NOTE: plans 33-01 (skill paths) and 33-02 (shared/memory + shared/llm paths) already landed, so the CURRENT import strings in these files already reflect those changes; this task only increases the relative depth by one segment (`../shared` -> `../../shared`, `../content` -> `../../content`, `../skills` -> `../../skills`).
  </read_first>
  <action>
    Create `src/modules/dashboard/`, `src/modules/evals/`, `src/modules/popup/` and move files per 33-RESEARCH.md § Track 5 File-to-Module Mapping (filenames preserved):
    - dashboard module: `index.html`, `index.tsx`, `SelectorView.tsx`, `dataManagement.ts`, `dataManagement.test.ts` from `src/dashboard/`.
    - evals module: `evals.html`, `evals.tsx`, `evalsLabeling.ts`, `evalsRunEngine.ts`, `evals.test.ts` from `src/dashboard/`.
    - popup module: `index.html`, `index.tsx`, `AccountRow.tsx`, `BatchBlockBar.tsx` from `src/popup/`.
    After moving, `src/dashboard/` and `src/popup/` must no longer exist. Do NOT create a `modules/common/` folder (D-08).
    Repoint every cross-folder import by adding one `../` segment (siblings via `./` stay unchanged), using the EXACT target strings in 33-PATTERNS.md § Track 5:
    - All `../shared/...` -> `../../shared/...` (e.g. `../shared/types` -> `../../shared/types`; storage/postStore already at `../shared/memory/...` -> `../../shared/memory/...`; pricing already at `../shared/llm/pricing` -> `../../shared/llm/pricing`).
    - `../content/selector-registry` -> `../../content/selector-registry` (dashboard/index.tsx).
    - In `evals.tsx`, the HeuristicDetector import (already pointed at the skill folder by plan 33-01) deepens to `../../skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill`.
    - HTML `<script src="./index.tsx">` / `<script src="./evals.tsx">` stay relative — the `.tsx` is co-located in the same new folder; no change.
    Do NOT touch `manifest.json` or `vite.config.ts` in this task (Task 2 does the build wiring).
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm run type-check && npm test</automated>
  </verify>
  <done>
    Three peer modules exist under `src/modules/`; `src/dashboard/` and `src/popup/` are gone; no `modules/common/`; type-check + full test suite pass.
  </done>
  <acceptance_criteria>
    - `src/modules/dashboard/` contains exactly: `index.html`, `index.tsx`, `SelectorView.tsx`, `dataManagement.ts`, `dataManagement.test.ts`.
    - `src/modules/evals/` contains exactly: `evals.html`, `evals.tsx`, `evalsLabeling.ts`, `evalsRunEngine.ts`, `evals.test.ts`.
    - `src/modules/popup/` contains exactly: `index.html`, `index.tsx`, `AccountRow.tsx`, `BatchBlockBar.tsx`.
    - `src/dashboard/` and `src/popup/` no longer exist; no `src/modules/common/` exists.
    - No grep hit for `from '../shared/` (single-dot-shared) within `src/modules/` files; depth is `../../shared/`.
    - `npm run type-check` exits 0 and `npm test` exits 0 (golden-score + exclusion parity byte-identical).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repoint manifest.json + vite.config.ts to the module paths and build the extension</name>
  <files>src/manifest.json, vite.config.ts</files>
  <read_first>
    - src/manifest.json (action.default_popup = "popup/index.html"; options_ui.page = "dashboard/index.html"; web_accessible_resources[0].resources[0] = "dashboard/evals.html")
    - vite.config.ts (root: 'src'; additionalInputs: ['dashboard/evals.html']; paths are relative to root)
    - 33-RESEARCH.md § Track 5 Vite + manifest repoint tables (assumption A1: additionalInputs relative to root 'src') and 33-PATTERNS.md § Track 5 (exact manifest + vite before/after)
    - 33-RESEARCH.md Pitfall 3 (stale manifest/vite path = page fails to load) and the build verification step
  </read_first>
  <action>
    Repoint all three HTML entry points in `src/manifest.json` (paths relative to `src/`, no leading slash) per 33-PATTERNS.md § Track 5:
    - `action.default_popup`: `popup/index.html` -> `modules/popup/index.html`.
    - `options_ui.page`: `dashboard/index.html` -> `modules/dashboard/index.html`.
    - `web_accessible_resources[0].resources[0]`: `dashboard/evals.html` -> `modules/evals/evals.html`.
    Update `vite.config.ts` `additionalInputs`: `['dashboard/evals.html']` -> `['modules/evals/evals.html']` (relative to `root: 'src'`; leave `root`, `base`, `build.outDir` unchanged). Then run `npm run build`. If the build errors on the `additionalInputs` path convention (A1 risk), recheck the vite-plugin-web-extension path resolution against the plugin and adjust the path relative to root accordingly.
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm run build && test -f dist/modules/popup/index.html && test -f dist/modules/dashboard/index.html && test -f dist/modules/evals/evals.html && grep -q "modules/popup/index.html" dist/manifest.json</automated>
  </verify>
  <done>
    Build succeeds; `dist/modules/{popup,dashboard,evals}/` HTML files exist; `dist/manifest.json` has the updated module paths.
  </done>
  <acceptance_criteria>
    - `src/manifest.json` `action.default_popup` = `modules/popup/index.html`, `options_ui.page` = `modules/dashboard/index.html`, `web_accessible_resources[0].resources[0]` = `modules/evals/evals.html`.
    - `vite.config.ts` `additionalInputs` = `['modules/evals/evals.html']`.
    - `npm run build` exits 0.
    - `dist/modules/popup/index.html`, `dist/modules/dashboard/index.html`, `dist/modules/evals/evals.html` all exist; `dist/manifest.json` references the `modules/` paths (no `dist/dashboard/` or `dist/popup/` legacy structure).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the built extension loads all three pages</name>
  <what-built>
    The three UX surfaces are now self-contained modules under `src/modules/` and the build emits them to `dist/modules/{popup,dashboard,evals}/`, with `manifest.json` and `vite.config.ts` repointed. Task 2 confirmed the files exist in `dist/` and the manifest references them. This checkpoint confirms the running extension actually loads all three pages (the D-09 highest-risk integration point that no automated test covers).
  </what-built>
  <how-to-verify>
    1. Open Chrome -> chrome://extensions -> enable Developer mode -> "Load unpacked" -> select the `dist/` folder (reload it if already loaded).
    2. Click the extension action icon: the POPUP opens and renders the flagged-account list (no blank page, no console error).
    3. Right-click the extension icon -> Options (or via chrome://extensions -> Details -> Extension options): the DASHBOARD page opens and renders (stats/SelectorView visible).
    4. Open the EVALS page (the dashboard's link to evals, or load the evals.html via its web-accessible URL): it renders without error.
    5. Open DevTools on each page and confirm there are no new console errors referencing missing modules or 404 HTML/JS paths.
    Expected: all three pages load and render exactly as before this refactor (zero behavior change).
  </how-to-verify>
  <action>
    This is a verification checkpoint, not an implementation step. Build is already done in Task 2. Pause and present the verification steps below to the user; do not auto-approve (workflow.auto_advance is ignored for this blocking checkpoint). Resume only on the user's signal.
  </action>
  <resume-signal>Type "approved" if all three pages load cleanly, or describe which page failed and any console errors.</resume-signal>
  <verify>
    <human-check>Popup, dashboard, and evals pages all load and render in the unpacked dist/ extension with no new console errors.</human-check>
  </verify>
  <done>User confirms all three pages (popup, dashboard, evals) load cleanly from the built extension; zero behavior change observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Pure file relocation plus build-config repointing. No new inputs, no new network calls, no new trust boundaries. The popup/dashboard/evals continue to read `chrome.storage.local` exactly as before; only their file locations and HTML entry-point paths change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Denial of Service | A UX page fails to load because manifest.json or vite additionalInputs is stale (Pitfall 3) | mitigate | Task 2 repoints manifest + vite atomically and asserts `dist/modules/...` files exist; Task 3 human-verify confirms all three pages load in the running extension. |
| T-33-10 | Tampering | Build resolves wrong HTML or leaves legacy `dist/dashboard/` structure | mitigate | Task 2 acceptance criteria assert `dist/modules/...` paths exist and `dist/manifest.json` references them (no legacy paths). |
| T-33-11 | Tampering | Silent detection-behavior change from a module-file import resolving wrong | mitigate | `npm test` (golden-score + exclusion parity) + `npm run type-check` gate Task 1 before build. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. No install tasks exist. |
</threat_model>

<verification>
Per-track gate (33-RESEARCH.md § Per-Track Verification Recipe — Track 5 adds the build + manual page-load step):
```
npm test && npm run type-check
npm run build
# then: load dist/ as unpacked extension, verify popup + dashboard + evals all load (Task 3 checkpoint)
```
All automated steps exit 0; the human checkpoint confirms the three pages load. Land as one atomic commit.
</verification>

<success_criteria>
- `src/modules/{dashboard,evals,popup}/` exist as self-contained peer modules; `src/dashboard/` and `src/popup/` are gone; no `modules/common/` (MOD-05, D-07, D-08).
- `manifest.json` + `vite.config.ts` resolve all three modules; `npm run build` emits `dist/modules/{popup,dashboard,evals}/` (MOD-05, D-09).
- The built extension loads popup, dashboard, and evals pages (human-verified).
- `npm test`, `npm run type-check` green; detection byte-identical.
</success_criteria>

<output>
Create `.planning/phases/33-improve-modularity/33-04-SUMMARY.md` when done.
</output>
