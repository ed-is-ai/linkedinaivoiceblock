---
phase: 33-improve-modularity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/detector/heuristic.ts
  - src/content/detector/llm.ts
  - src/content/detector/heuristic.test.ts
  - src/content/detector/rederiver.ts
  - src/content/index.ts
  - src/content/observer.ts
  - scripts/eval.ts
  - src/dashboard/evals.tsx
  - src/content/selector/heal.ts
  - src/content/selector/heuristic.ts
  - src/content/selector/sanitizer.ts
  - src/content/selector/validator.ts
  - src/content/selector/heal.test.ts
  - src/content/selector/heuristic.test.ts
  - src/content/selector/sanitizer.test.ts
  - src/content/selector/validator.test.ts
  - src/content/selector/__fixtures__
  - src/tools/library/dom-selector-rederive/rederiver.ts
  - src/tools/library/dom-selector-rederive/heal.ts
  - src/tools/library/dom-selector-rederive/heuristic.ts
  - src/tools/library/dom-selector-rederive/sanitizer.ts
  - src/tools/library/dom-selector-rederive/validator.ts
  - src/tools/library/dom-selector-rederive/heal.test.ts
  - src/tools/library/dom-selector-rederive/heuristic.test.ts
  - src/tools/library/dom-selector-rederive/sanitizer.test.ts
  - src/tools/library/dom-selector-rederive/validator.test.ts
  - src/tools/library/dom-selector-rederive/__fixtures__
  - src/tools/library/dom-selector-registry/SKILL.md
  - src/tools/library/dom-selector-registry/TOOL.md
  - src/tools/library/dom-selector-registry/dom-selector-registry.skill.ts
  - src/tools/library/dom-selector-registry/dom-selector-registry.tool.ts
  - src/content/generated-skill-registry.ts
  - src/shared/generated-tool-registry.ts
autonomous: true
requirements: [MOD-01, MOD-02]

must_haves:
  truths:
    - "Skill-owned detector logic (HeuristicDetector, LLMDetector) is imported directly from its skill folder; no re-export barrel remains in src/content/detector/"
    - "The LLMRederiver and the four selector internals (heal, heuristic, sanitizer, validator) live inside the dom-selector-rederive tool folder, co-located with the tool"
    - "dom-selector-registry follows the tool convention: TOOL.md + .tool.ts (no SKILL.md / .skill.ts)"
    - "src/content/selector-registry.ts is unchanged and still the only selector-string single-writer (CLAUDE.md #1)"
    - "Full test suite stays green and detection golden-score snapshot + exclusion parity are byte-identical after this track"
  artifacts:
    - path: "src/tools/library/dom-selector-rederive/rederiver.ts"
      provides: "LLMRederiver message-relay class co-located with the rederive tool"
    - path: "src/tools/library/dom-selector-rederive/heal.ts"
      provides: "Self-healing orchestrator co-located with the rederive tool"
    - path: "src/tools/library/dom-selector-registry/TOOL.md"
      provides: "Tool manifest renamed from SKILL.md (metadata.kind: tool)"
      contains: "kind: tool"
    - path: "src/tools/library/dom-selector-registry/dom-selector-registry.tool.ts"
      provides: ".tool.ts re-export renamed from .skill.ts"
  key_links:
    - from: "src/content/index.ts"
      to: "src/skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill"
      via: "direct import of HeuristicDetector (barrel deleted)"
      pattern: "skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill"
    - from: "src/content/observer.ts"
      to: "src/tools/library/dom-selector-rederive/heal"
      via: "import triggerHeal/isFeedUrl/hasFeedContainer"
      pattern: "tools/library/dom-selector-rederive/heal"
    - from: "src/tools/library/dom-selector-rederive/heal.ts"
      to: "src/content/selector-registry"
      via: "resolve/insertCandidate via repo-rooted relative path"
      pattern: "content/selector-registry"
---

<objective>
Track 1 + Track 2 (coupled): finish the detector migration and the selector-internals / dom-selector-registry tool migration in a single atomic commit. Delete the two thin re-export barrels (`heuristic.ts`, `llm.ts`) and repoint their importers directly to the skill folders; move `LLMRederiver` and the four selector internals (`heal`, `heuristic`, `sanitizer`, `validator`) plus their tests and `__fixtures__/` into the `dom-selector-rederive` tool folder; rename `dom-selector-registry`'s `SKILL.md` → `TOOL.md` and `.skill.ts` → `.tool.ts`.

These two tracks are coupled because `heal.ts` imports `LLMRederiver` from `../detector/rederiver`; moving `heal.ts` (Track 2) and `rederiver.ts` (Track 1) must happen together so the relative import resolves to `./rederiver` inside the tool folder.

Purpose: Implements MOD-01 (skill/tool-owned logic moves out of `src/content/`; only cross-cutting utilities remain) and MOD-02 (`dom-selector-registry` follows the tool convention; selector internals co-located in the tool folder). Honors D-01, D-02, D-03, D-04.
Output: Reorganized detector + selector internals; zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-improve-modularity/33-CONTEXT.md
@.planning/phases/33-improve-modularity/33-RESEARCH.md
@.planning/phases/33-improve-modularity/33-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete heuristic.ts and llm.ts barrels; repoint all importers to the skill folders</name>
  <files>src/content/detector/heuristic.ts, src/content/detector/llm.ts, src/content/index.ts, scripts/eval.ts, src/dashboard/evals.tsx, src/content/detector/heuristic.test.ts</files>
  <read_first>
    - src/content/detector/heuristic.ts (confirm it is still a thin re-export barrel; lines 1-9 per PATTERNS.md)
    - src/content/detector/llm.ts (confirm thin re-export barrel)
    - src/content/index.ts (lines 5-6: the two barrel imports)
    - scripts/eval.ts (line 30: HeuristicDetector import with .js extension)
    - src/dashboard/evals.tsx (line 18: HeuristicDetector import)
    - src/content/detector/heuristic.test.ts (line 6: imports `./heuristic` barrel)
    - 33-PATTERNS.md § Track 1 (exact before/after import strings) and 33-RESEARCH.md § Track 1 broken-importer tables
  </read_first>
  <action>
    Delete `src/content/detector/heuristic.ts` and `src/content/detector/llm.ts` (both confirmed thin re-export barrels). Do NOT leave a replacement barrel (D-02: thin re-exports become direct imports; no barrel left behind).
    Repoint the four `HeuristicDetector` / `LLMDetector` import sites to the skill folder directly, using the EXACT target strings from 33-PATTERNS.md § Track 1:
    - `src/content/index.ts` L5: `./detector/heuristic` -> `../skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill`
    - `src/content/index.ts` L6: `./detector/llm` -> `../skills/library/detect-aiwriting-llm/detect-aiwriting-llm.skill`
    - `scripts/eval.ts` L30: `../src/content/detector/heuristic.js` -> `../src/skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill.js` (keep the `.js` extension — this is a script under NodeNext resolution)
    - `src/dashboard/evals.tsx` L18: `../content/detector/heuristic` -> `../skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill`
    Keep `src/content/detector/heuristic.test.ts` at its current path (per RESEARCH Open Question 1 recommendation) and repoint its import L6: `./heuristic` -> `../../skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill`.
    Leave `signals/profile.ts`, `comment-expand.ts`, `language.ts`, `tombstone.ts` and their tests untouched — they are genuinely shared content-pipeline infra (D-02; see 33-PATTERNS.md "Files Confirmed as NOT Moving").
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm run type-check</automated>
  </verify>
  <done>
    Both barrel files are gone; `npm run type-check` passes (every HeuristicDetector/LLMDetector import resolves to the skill folder); no file imports `./detector/heuristic` or `./detector/llm` anymore.
  </done>
  <acceptance_criteria>
    - `src/content/detector/heuristic.ts` and `src/content/detector/llm.ts` no longer exist.
    - No grep hit for `detector/heuristic'` or `detector/llm'` import specifiers anywhere in `src/` or `scripts/` (the heuristic.test.ts path now points at the skill folder).
    - `src/content/index.ts` imports `HeuristicDetector` from `../skills/library/detect-aiwriting-heuristic/detect-aiwriting-heuristic.skill` and `LLMDetector` from `../skills/library/detect-aiwriting-llm/detect-aiwriting-llm.skill` (exact strings).
    - `scripts/eval.ts` import retains the `.skill.js` extension.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Move rederiver.ts + the four selector internals (+ tests + __fixtures__) into the dom-selector-rederive tool folder; repoint observer.ts and internal imports</name>
  <files>src/content/detector/rederiver.ts, src/content/selector/heal.ts, src/content/selector/heuristic.ts, src/content/selector/sanitizer.ts, src/content/selector/validator.ts, src/content/selector/heal.test.ts, src/content/selector/heuristic.test.ts, src/content/selector/sanitizer.test.ts, src/content/selector/validator.test.ts, src/content/selector/__fixtures__, src/content/observer.ts, src/tools/library/dom-selector-rederive/</files>
  <read_first>
    - src/content/detector/rederiver.ts (lines 16-18: the RederiveCandidate type import from the tool)
    - src/content/selector/heal.ts (lines 25-30: imports of selector-registry, ./heuristic, ./validator, ./sanitizer, ../detector/rederiver, ../../shared/storage)
    - src/content/selector/heuristic.ts (import of resolve from ../selector-registry)
    - src/content/selector/sanitizer.ts and validator.ts (confirm no selector-registry / shared imports before assuming "unchanged")
    - src/content/selector/heal.test.ts (storageGet import + __fixtures__ relative refs)
    - src/content/observer.ts (line 15: import of triggerHeal/isFeedUrl/hasFeedContainer from ./selector/heal)
    - src/tools/library/dom-selector-rederive/dom-selector-rederive.tool.ts and dom-selector-rederive.test.ts (sibling-import convention `./`)
    - 33-RESEARCH.md § Track 2 (full ownership table + internal-import update table) and 33-PATTERNS.md § Track 2 (exact before/after import strings, target folder shape)
    - CLAUDE.md Critical Constraint #1 (selector-registry.ts single-writer)
  </read_first>
  <action>
    Move these files from their current locations into `src/tools/library/dom-selector-rederive/` (preserve filenames): `src/content/detector/rederiver.ts`; `src/content/selector/heal.ts`; `src/content/selector/heuristic.ts`; `src/content/selector/sanitizer.ts`; `src/content/selector/validator.ts`; the four tests `heal.test.ts`, `heuristic.test.ts`, `sanitizer.test.ts`, `validator.test.ts`; and the entire `src/content/selector/__fixtures__/` directory (all 10 `feed-*.html` files) into `src/tools/library/dom-selector-rederive/__fixtures__/`. After moving, `src/content/selector/` should be empty/removed.
    Update imports to the EXACT target strings in 33-PATTERNS.md § Track 2:
    - `rederiver.ts`: `../../tools/library/dom-selector-rederive/dom-selector-rederive.tool` -> `./dom-selector-rederive.tool` (sibling now).
    - `heal.ts`: `../selector-registry` -> `../../../content/selector-registry`; `../detector/rederiver` -> `./rederiver`; `./heuristic`, `./validator`, `./sanitizer` unchanged (siblings); `../../shared/storage` -> `../../../shared/storage` (interim path — Track 4 / plan 33-02 later moves it to `../../../shared/memory/storage`).
    - `heuristic.ts`: `../selector-registry` -> `../../../content/selector-registry`.
    - `sanitizer.ts` / `validator.ts`: leave imports as-is unless your read finds a `../selector-registry` or `../../shared/` import, in which case re-root by one extra `../` segment.
    - `heal.test.ts`: `../../shared/storage` -> `../../../shared/storage`; any `../__fixtures__/...` refs stay `./__fixtures__/...` or `../dom-selector-rederive/__fixtures__/...` per the moved layout — match the sibling `dom-selector-rederive.test.ts` fixture-reference style.
    - `src/content/observer.ts` L15: `./selector/heal` -> `../tools/library/dom-selector-rederive/heal`.
    Do NOT move `src/content/selector-registry.ts` (CLAUDE.md #1 single-writer) and do NOT modify the `dom-selector-registry` tool folder in this task (that is Task 3).
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm run type-check && npm test</automated>
  </verify>
  <done>
    All five impl files + four tests + `__fixtures__/` live under `src/tools/library/dom-selector-rederive/`; `src/content/selector/` is gone; `selector-registry.ts` untouched in `src/content/`; type-check + full test suite pass.
  </done>
  <acceptance_criteria>
    - `src/tools/library/dom-selector-rederive/` contains `rederiver.ts`, `heal.ts`, `heuristic.ts`, `sanitizer.ts`, `validator.ts`, their four `.test.ts` files, and `__fixtures__/` with all 10 `feed-*.html` files (matches 33-PATTERNS.md target folder shape).
    - `src/content/selector/` no longer exists.
    - `src/content/selector-registry.ts` is byte-identical to its pre-task state (unchanged).
    - `src/content/observer.ts` imports heal from `../tools/library/dom-selector-rederive/heal`.
    - `heal.ts` imports `LLMRederiver` from `./rederiver` and selector-registry from `../../../content/selector-registry`.
    - `npm run type-check` exits 0 and `npm test` exits 0 (golden-score snapshot + exclusion parity byte-identical).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rename dom-selector-registry SKILL.md -> TOOL.md and .skill.ts -> .tool.ts; regenerate registries and confirm clean</name>
  <files>src/tools/library/dom-selector-registry/SKILL.md, src/tools/library/dom-selector-registry/TOOL.md, src/tools/library/dom-selector-registry/dom-selector-registry.skill.ts, src/tools/library/dom-selector-registry/dom-selector-registry.tool.ts, src/content/generated-skill-registry.ts, src/shared/generated-tool-registry.ts</files>
  <read_first>
    - src/tools/library/dom-selector-registry/SKILL.md (confirm frontmatter already has metadata.kind: tool; only filename changes)
    - src/tools/library/dom-selector-registry/dom-selector-registry.skill.ts (confirm it is a thin re-export, NOT imported at runtime — per 33-RESEARCH.md § Track 2 D-03)
    - src/tools/library/dom-selector-rederive/TOOL.md (analog format)
    - scripts/skill-order.json (confirm dom-selector-registry is NOT in the tools array, so codegen output is unaffected by the rename)
    - 33-RESEARCH.md § Track 2 D-03 and 33-PATTERNS.md § Track 2 D-03
  </read_first>
  <action>
    Rename `src/tools/library/dom-selector-registry/SKILL.md` -> `TOOL.md` (content unchanged; frontmatter already declares `metadata.kind: tool`). Rename `dom-selector-registry.skill.ts` -> `dom-selector-registry.tool.ts` (file body unchanged; confirmed not imported at runtime, so no importer repoint needed). After the renames AND the moves from Tasks 1-2, run `npm run generate-skill-registry` to regenerate both committed registry files, then run `npm run check-skill-registry && npm run check-tool-registry` to confirm the generated output is clean (no diff). Commit the regenerated files if codegen produced any change.
  </action>
  <verify>
    <automated>cd "c:/Git/linkedin.blocker" && npm run generate-skill-registry && npm run check-skill-registry && npm run check-tool-registry</automated>
  </verify>
  <done>
    `dom-selector-registry` folder has `TOOL.md` + `dom-selector-registry.tool.ts` (no SKILL.md / .skill.ts); both stale-check commands exit 0.
  </done>
  <acceptance_criteria>
    - `src/tools/library/dom-selector-registry/TOOL.md` exists with `kind: tool` in frontmatter; `SKILL.md` is gone.
    - `src/tools/library/dom-selector-registry/dom-selector-registry.tool.ts` exists; `dom-selector-registry.skill.ts` is gone.
    - `npm run check-skill-registry` exits 0 and `npm run check-tool-registry` exits 0 (generated registries regenerate clean after all Track 1+2 moves).
    - `npm run type-check` and `npm test` still pass (re-run if any generated file changed).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a pure file-relocation refactor. No new inputs, no new network calls, no new trust boundaries are introduced. The only data crossing a boundary remains the existing rederive flow (content script -> service worker -> Anthropic API) which is moved verbatim, not modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | Selector single-writer (`src/content/selector-registry.ts`) | mitigate | File MUST NOT move (CLAUDE.md #1). Task 2 acceptance criteria assert it is byte-identical after the track; selector strings stay sourced only via SelectorRegistry. |
| T-33-02 | Tampering | Generated registries (`generated-skill-registry.ts`, `generated-tool-registry.ts`) drift after file moves | mitigate | Task 3 regenerates via codegen and gates on `check-skill-registry` + `check-tool-registry` (git diff --exit-code). Never hand-edit generated files. |
| T-33-03 | Tampering | Silent behavior change from a botched move (wrong import resolves to a stale copy) | mitigate | Per-track gate: `npm test` (golden-score snapshot + exclusion parity, byte-identical) + `npm run type-check` must pass before commit. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH § Package Legitimacy Audit: not applicable). No install tasks exist. |
</threat_model>

<verification>
Run the per-track gate from 33-RESEARCH.md § Per-Track Verification Recipe:
```
npm test && npm run type-check
npm run check-skill-registry && npm run check-tool-registry
```
All must exit 0. The golden-score snapshot (`heuristic.test.ts`) and exclusion parity (`exclusions.test.ts`) inside `npm test` enforce byte-identical detection. Land this plan as one atomic commit (Tracks 1+2 coupled).
</verification>

<success_criteria>
- No re-export barrel for HeuristicDetector/LLMDetector remains in `src/content/detector/` (MOD-01).
- `LLMRederiver` + heal/heuristic/sanitizer/validator (+ tests + fixtures) are co-located in the `dom-selector-rederive` tool folder (MOD-01, MOD-02).
- `dom-selector-registry` uses `TOOL.md` + `.tool.ts` (MOD-02, D-03).
- `src/content/selector-registry.ts` unchanged (CLAUDE.md #1).
- `npm test`, `npm run type-check`, `check-skill-registry`, `check-tool-registry` all green; detection byte-identical.
</success_criteria>

<output>
Create `.planning/phases/33-improve-modularity/33-01-SUMMARY.md` when done.
</output>
