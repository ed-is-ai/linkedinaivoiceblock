---
phase: 25.1-capture-and-export-unflagged-posts-for-eval-negatives
plan: 02
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/shared/postStore.ts
  - src/content/index.ts
autonomous: true
requirements: [CAPTURE-01, CAPTURE-02]
must_haves:
  truths:
    - "Below-FLAG_THRESHOLD posts are persisted to unflaggedPosts ONLY when the captureUnflaggedPosts opt-in is enabled"
    - "Captured posts are deduped by urn, text-truncated at 1000 chars, newest-first, capped at 200 with oldest evicted"
    - "Each captured post records the score and engine that were computed, and is stored UNLABELED (no label field written)"
    - "When the opt-in is OFF (default), no unflagged post is ever written"
  artifacts:
    - path: "src/shared/postStore.ts"
      provides: "persistUnflaggedPost FIFO-capped, dedup-by-urn, truncating helper"
      contains: "export async function persistUnflaggedPost"
    - path: "src/content/index.ts"
      provides: "gated capture call inserted before the L323 below-threshold early return"
      contains: "persistUnflaggedPost"
  key_links:
    - from: "src/content/index.ts detect-then-hide closure"
      to: "src/shared/postStore.ts persistUnflaggedPost"
      via: "fire-and-forget call gated on captureUnflaggedPosts before the mergedScore < FLAG_THRESHOLD return"
      pattern: "captureUnflaggedPosts.*persistUnflaggedPost|persistUnflaggedPost\\("
    - from: "src/content/index.ts settings read"
      to: "Settings.captureUnflaggedPosts"
      via: "settings?.captureUnflaggedPosts ?? false"
      pattern: "captureUnflaggedPosts \\?\\? false"
---

<objective>
Capture the posts the detector scored but did NOT flag — the clear human-looking negatives currently dropped by the early return at `src/content/index.ts:323` (`if (mergedScore < FLAG_THRESHOLD) return;`). Add a `persistUnflaggedPost` helper that mirrors `persistStoredPost`'s FIFO/dedup/truncation discipline against the new `unflaggedPosts` key, and call it from the content script — gated on the new opt-in, BEFORE the early return.

Purpose: Supplies real, UNLABELED human-negatives for the Phase 26 eval, with zero new API cost (the detector already ran on these posts during normal browsing — D-04).

Output: `persistUnflaggedPost` in `src/shared/postStore.ts`; a gated, fire-and-forget capture call in `src/content/index.ts`.

Implements D-01 (capture below FLAG_THRESHOLD), D-02 (cap 200), D-03 (capture all + dedup by urn), D-04 (record engine + score), D-05 (gate on opt-in default false, reuse 1000-char truncation), D-06 (store UNLABELED — never write `label`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-CONTEXT.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistUnflaggedPost FIFO helper to postStore.ts</name>
  <read_first>
    - src/shared/postStore.ts (whole file, 1-64) — `persistStoredPost` (L37-64) is the verbatim FIFO template; `POST_STORE_CAP = 200` (L20) and `POST_TEXT_MAX_CHARS = 1000` (L23) are the reusable constants; module-header rule at L8-9 (NO content/ or DOM imports)
    - src/shared/types.ts `UnflaggedPost` interface (added in Plan 01) — the exact entry shape
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md L25-82 (the copy-this-shape guidance, including `seenAt` not `hiddenAt`, and the "do NOT write label" rule)
  </read_first>
  <behavior>
    - Given an empty `unflaggedPosts` store, calling `persistUnflaggedPost` with a new urn writes a 1-entry array, newest-first.
    - Given a store already containing an entry with the same urn, calling again is a no-op (dedup by urn — no duplicate, no churn of the cap).
    - Given text longer than 1000 chars, the stored `text` is trimmed and sliced to exactly 1000 chars.
    - Given a store at 200 entries, adding a new unique urn keeps the length at 200 (oldest entry evicted via pop).
    - The written entry carries `score`, `engineUsed`, and `seenAt` (a numeric timestamp), and carries NO `label` property.
  </behavior>
  <action>
    Add an exported `async function persistUnflaggedPost` to `src/shared/postStore.ts`, co-located with the existing helper (reuse the existing `POST_TEXT_MAX_CHARS = 1000` constant; add a separate `UNFLAGGED_STORE_CAP = 200` constant or reuse `POST_STORE_CAP` — either is fine, D-02 cap is 200). It accepts `{ urn, authorId, authorName, score, text, engineUsed }` where `engineUsed: 'heuristic' | 'llm'`.

    Copy the `persistStoredPost` discipline exactly: read `unflaggedPosts` (default `[]`) via `storageGet(['unflaggedPosts'])`; dedup with `if (arr.some(p => p.urn === urn)) return;`; build the entry with `text: text.trim().slice(0, POST_TEXT_MAX_CHARS)` and `seenAt: Date.now()`; prepend newest-first (`const updated = [entry, ...arr]`); evict oldest if over cap (`if (updated.length > CAP) updated.pop()`); `await storageSet({ unflaggedPosts: updated })`.

    Per D-06, do NOT set any `label` field on the entry. Import the `UnflaggedPost` type alongside the existing `StoredPost` import; add NO content/ or DOM imports (module-header rule).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- postStore</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/postStore.ts` contains `export async function persistUnflaggedPost`
    - The helper reads and writes the `unflaggedPosts` storage key (not `storedPosts`)
    - The constructed entry sets `seenAt`, `engineUsed`, and applies `.trim().slice(0, POST_TEXT_MAX_CHARS)` to text
    - The helper contains a dedup-by-urn early return and a cap-eviction `pop()` with cap value 200
    - The entry object literal contains NO `label` key (grep the function body: zero matches for `label`)
    - `npx tsc --noEmit` exits 0; `npm test -- postStore` exits 0 (existing postStore tests, if any, still pass; if a postStore test file exists, add cases covering the five behaviors above — if none exists, create `src/shared/postStore.test.ts` with those cases)
  </acceptance_criteria>
  <done>persistUnflaggedPost exists, mirrors the FIFO/dedup/truncation contract against unflaggedPosts, records score+engine+seenAt, never writes a label, and is covered by tests for the five behaviors.</done>
</task>

<task type="auto">
  <name>Task 2: Gate-and-capture below-threshold posts in the content script before the L323 early return</name>
  <read_first>
    - src/content/index.ts L10-14 (import block) and L26 (`const FLAG_THRESHOLD = 35;`)
    - src/content/index.ts L210-217 (the storage read + `settings?.autoHideThreshold ?? 60` pattern — model the new opt-in read on this)
    - src/content/index.ts L312-360 (the `detector.detect(...).then(async (result) => { ... })` closure; `mergedScore`, `result.engineUsed`, `urn`, `authorId`, `authorName`, `postText` are all in scope; L323 is the hook point; L349-355 `persistStoredPost(...).catch(() => {})` is the call analog)
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md L126-186 (exact hook location, settings-read pattern, and call shape)
  </read_first>
  <action>
    In `src/content/index.ts`:

    (1) Add `import { persistUnflaggedPost } from '../shared/postStore';` to the import block (the file already imports `persistStoredPost` from there — extend that line or add a sibling import).

    (2) Where `settings` is read (the destructure that already yields `autoHideThreshold`, ~L210-217), derive `const captureUnflaggedPosts = settings?.captureUnflaggedPosts ?? false;` — OFF by default per D-05. Ensure this value is reachable inside the `detector.detect().then` closure the same way `autoHideThreshold`/`effectiveHideThreshold` already are (closure capture).

    (3) Inside the `.then` closure, IMMEDIATELY BEFORE the line `if (mergedScore < FLAG_THRESHOLD) return;` (L323), insert a gated capture: when `captureUnflaggedPosts && mergedScore < FLAG_THRESHOLD`, call `persistUnflaggedPost({ urn, authorId: authorId || urn, authorName, score: mergedScore, text: postText, engineUsed: result.engineUsed })` as fire-and-forget with a trailing `.catch(() => {})` (matching the L349 non-blocking convention). Do NOT `await` it in a way that delays the existing early return / hide path. The early return at L323 stays exactly as-is and still fires for below-threshold posts after the (conditional) capture.

    Do NOT alter the flagged/hidden branches (L325-356), the DEBUG log, or any threshold logic. This is purely an additive, opt-in-gated hook.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/index.ts` imports `persistUnflaggedPost`
    - `src/content/index.ts` contains `settings?.captureUnflaggedPosts ?? false`
    - The `persistUnflaggedPost(...)` call appears textually BEFORE the `if (mergedScore < FLAG_THRESHOLD) return;` line and is guarded by `captureUnflaggedPosts && mergedScore < FLAG_THRESHOLD`
    - The capture call passes `engineUsed: result.engineUsed` and `score: mergedScore`, and uses a trailing `.catch(() => {})`
    - The existing `if (mergedScore < FLAG_THRESHOLD) return;` line is unchanged and still present
    - No existing test in the suite regresses: `npm test` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Below-threshold posts are captured via persistUnflaggedPost only when the opt-in is enabled, inserted before the unchanged early return, fire-and-forget, with engine + score recorded; the full suite and type-check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn feed DOM → chrome.storage.local | Text of posts the user never flagged is read and persisted locally — a broader privacy surface than flagged-post storage |
| content script → storage write volume | Unbounded scroll could attempt to persist many posts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.1-03 | Information Disclosure | content-script capture hook | mitigate | Capture is gated on `captureUnflaggedPosts ?? false` — OFF by default, so no unflagged text is stored without explicit user consent (D-05) |
| T-25.1-04 | Information Disclosure | `UnflaggedPost.text` | mitigate | Text is truncated to 1000 chars (`POST_TEXT_MAX_CHARS`) before storage; no PII beyond what LinkedIn already renders publicly is captured |
| T-25.1-05 | Denial of Service | unbounded storage growth | mitigate | FIFO cap of 200 enforced at write time (pop oldest); dedup-by-urn prevents scroll re-churn |
| T-25.1-06 | Information Disclosure | data egress | accept | No network egress added — chrome.storage.local only; the detector call that already ran is reused (no new API call) |
| T-25.1-SC | Tampering | npm installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm test` green (full suite, including any new postStore tests).
- Grep confirms the capture call precedes the L323 early return and is gated on the opt-in.
</verification>

<success_criteria>
- With the opt-in OFF (default), zero writes to `unflaggedPosts`.
- With the opt-in ON, below-threshold posts are deduped, truncated, capped at 200, stored UNLABELED with score + engine.
- No regression in the existing detect/hide flow.
</success_criteria>

<output>
Create `.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-02-SUMMARY.md` when done.
</output>
