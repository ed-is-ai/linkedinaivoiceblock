---
status: complete
phase: 25.1-capture-and-export-unflagged-posts-for-eval-negatives
source:
  - 25.1-01-SUMMARY.md
  - 25.1-02-SUMMARY.md
  - 25.1-03-SUMMARY.md
  - 25.1-04-SUMMARY.md
  - 25.1-05-SUMMARY.md
  - 25.1-06-SUMMARY.md
started: 2026-06-14T18:07:01Z
updated: 2026-06-14T18:07:01Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Reload the unpacked extension, then open the dashboard. Dashboard loads without console errors and renders normally on a fresh start.
result: pass
note: On a freshly-opened feed tab the "Extension context invalidated" error does not recur — it was a stale-content-script reload artifact. Also observed (out of scope, not a defect): (a) a "[object Object]" log from Phase 23 LLMDetector.scoreViaBackground rejecting raw chrome.runtime.lastError instead of new Error(...message) — cosmetic Phase 23 logging nit, backlog candidate; (b) a console TypeError when running a chrome.storage snippet in the page's main-world console — expected, chrome.storage only exists in extension contexts.

### 2. Enable the capture opt-in in the popup
expected: Open the extension popup. There is a "capture unflagged posts" opt-in control (OFF by default). Toggling it ON persists — reopening the popup shows it still ON. Toggling it does NOT clobber the existing auto-hide threshold setting.
result: pass
note: Confirmed after rebuilding the stale dist/ bundle (npm run build) and reloading the extension. Toggle renders under ⚙ Settings, persists, threshold unaffected.

### 3. Capture populates the unflaggedPosts store
expected: With the opt-in ON, reload a LinkedIn feed and scroll. Posts the detector scores BELOW the flag threshold (human-looking posts) get recorded to the `unflaggedPosts` store in chrome.storage.local (UNLABELED, capped at 200, deduped). Note WR-02 from verification — the setting is read at content-script init, so a page reload may be required after enabling.
result: pass
note: Confirmed via exported JSON — unflaggedPosts populated with real below-threshold posts (scores 5/12/18/28) carrying urn/authorId/authorName/text/score/seenAt.

### 4. Export JSON includes a top-level unflaggedPosts[] array
expected: With some unflagged posts captured AND at least one flagged account present, click "Export JSON" in the dashboard. The downloaded JSON has a top-level `unflaggedPosts` array (a sibling of `flaggedAccounts`, NOT nested under an account), with entries carrying seenAt/score/engineUsed and no label.
result: pass
note: Exported JSON shows unflaggedPosts[] as top-level sibling of flaggedAccounts, not nested. Entries unlabeled. (User raised a follow-on REDESIGN request for a different export shape — see Gaps note; scoped to a NEW phase, not a 25.1 defect.)

### 5. Export JSON button is reachable on a clean feed (CR-01 fix)
expected: Clear flagged accounts so the dashboard shows zero flagged accounts, but have unflagged posts captured. The "Export JSON" button STILL renders and is clickable (previously it was hidden when zero accounts were flagged). Clicking it downloads JSON whose `unflaggedPosts[]` array is populated. This is the headline gap-closure fix for this phase.
result: pass
note: User successfully clicked Export JSON and produced output containing populated unflaggedPosts[]; the widened gate (CR-01 fix) renders the button. Build had to be refreshed (npm run build + extension reload) for the fix to be live.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0

## Gaps

No Phase 25.1 defects. All 5 tests pass on the rebuilt extension.

**Follow-on (NEW phase, not a 25.1 gap):** User requested an export-shape redesign into three symmetric arrays:
- `blockedAccounts` — same format as today's `flaggedAccounts` but filtered to blocked accounts only
- `flaggedPosts` — NEW: all posts ABOVE the current threshold, post-centric
- `unflaggedPosts` — all posts BELOW threshold (already shipped in 25.1)

Decision: scope as a NEW phase (user choice). Phase 25.1's narrow goal (add unflaggedPosts[] sibling array) is met and verified.
