---
phase: 25.1-capture-and-export-unflagged-posts-for-eval-negatives
plan: 05
type: execute
wave: 2
depends_on: ["25.1-06"]
gap_closure: true
files_modified:
  - src/dashboard/index.tsx
autonomous: true
requirements: [EXPORT-01, EXPORT-04]
must_haves:
  truths:
    - "The dashboard Export JSON button renders when unflagged posts exist even though zero accounts are flagged and zero posts are stored"
    - "The Export JSON button continues to render in the pre-existing cases (flagged accounts present, or stored posts present)"
    - "Clicking Export JSON on an unflagged-only feed downloads a JSON file whose top-level unflaggedPosts[] array is populated"
  artifacts:
    - path: "src/dashboard/index.tsx"
      provides: "Export JSON button gated on accounts.length > 0 || posts.length > 0 || unflagged.length > 0 so unflagged-only feeds can export"
      contains: "accounts.length > 0 || posts.length > 0 || unflagged.length > 0"
  key_links:
    - from: "src/dashboard/index.tsx Export JSON button render gate"
      to: "handleExportJson onClick"
      via: "render condition that includes unflagged.length > 0"
      pattern: "accounts\\.length > 0 \\|\\| posts\\.length > 0 \\|\\| unflagged\\.length > 0"
---

<objective>
Close gap CR-01 (BLOCKER) from `25.1-VERIFICATION.md`: the dashboard "Export JSON" button is gated exclusively on `accounts.length > 0`, so a user who enables capture and browses a clean feed (zero posts ever flagged) accumulates `unflaggedPosts` but has no button to click. This makes the phase's primary use case — supplying eval negatives from clean feeds — unreachable.

Purpose: The `unflagged` state is already correctly loaded (`src/dashboard/index.tsx:95`) and already passed to `handleExportJson` (`src/dashboard/index.tsx:131`); only the render gate blocks reachability. Widening the gate is the minimal fix that makes the data flow (already FLOWING per the verification Level-4 trace) terminate in a usable button.

Output: a one-condition change to the Export JSON button render gate in `src/dashboard/index.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-VERIFICATION.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the Export JSON button render gate to include unflagged posts</name>
  <files>src/dashboard/index.tsx</files>
  <read_first>
    - src/dashboard/index.tsx (read the full file, focus on lines 256-277 — the "Data management" card and the Export JSON / Export Posts CSV button gates. Confirm the live variable names: `accounts` (line 75), `posts` (line 77), `unflagged` (line 78). Confirm `handleExportJson` (line 129) already calls `buildJsonExport(accounts, posts, unflagged)`.)
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-VERIFICATION.md (Gap 1 / CR-01: the exact target condition is `(accounts.length > 0 || posts.length > 0 || unflagged.length > 0)`)
  </read_first>
  <action>
    In `src/dashboard/index.tsx`, locate the Export JSON button render gate at line 264 (`{accounts.length > 0 && (<button style={s.actionBtn} onClick={handleExportJson}>Export JSON</button>)}`). Change ONLY the gate condition for the Export JSON button from `accounts.length > 0` to `accounts.length > 0 || posts.length > 0 || unflagged.length > 0`, using the existing state variables `accounts`, `posts`, and `unflagged` (all already declared and in scope). Do NOT change the Export Posts CSV button gate (line 267) — that button exports `posts` (StoredPost[]) and its `accounts.length > 0` gate is intentional and out of scope. Do NOT change the Export LLM call traces button. Preserve the existing inline-style convention: keep `style={s.actionBtn}` and the `onClick={handleExportJson}` handler unchanged. Do not add any new styles, helpers, or imports. The empty-state hint at line 260-262 ("No flagged accounts yet…") may remain as-is; it is non-blocking and not part of this gap.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/index.tsx` contains the exact substring `accounts.length > 0 || posts.length > 0 || unflagged.length > 0`
    - The Export JSON `<button>` element with `onClick={handleExportJson}` is rendered under that widened condition (not under a bare `accounts.length > 0`)
    - The Export Posts CSV button gate is unchanged (still `accounts.length > 0`)
    - `npm run type-check` exits 0 (tsc passes)
    - `npm test` exits 0 (all vitest suites pass, including dataManagement.test.ts)
    - No new imports, styles, or helper functions were added to the file
  </acceptance_criteria>
  <done>The Export JSON button renders whenever any of flagged accounts, stored posts, or unflagged posts exist; type-check and tests pass; only the JSON button gate was modified.</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes (no type regressions from the gate change).
- `npm test` passes (the existing `dataManagement.test.ts` `buildJsonExport` tests still pass; no test depended on the old gate).
- Static check: the file contains `accounts.length > 0 || posts.length > 0 || unflagged.length > 0` exactly once, attached to the Export JSON button.
</verification>

<success_criteria>
CR-01 closed: on a clean feed where `accounts.length === 0` and `posts.length === 0` but `unflagged.length > 0`, the Export JSON button renders and `handleExportJson` (already wired to `buildJsonExport(accounts, posts, unflagged)`) is reachable, producing a download whose top-level `unflaggedPosts[]` array is populated.
</success_criteria>

<notes>
Out of scope for this gap-closure pass (deferred warnings from the CR-01 review, NOT in the VERIFICATION `gaps:` frontmatter):
- WR-02: capture opt-in is captured as a `const` at `init()` time (no live update / no reload hint).
- WR-03: `persistUnflaggedPost` min-seenAt eviction can evict the just-added entry under clock skew (vs. the safer newest-first `pop()` strategy).
- WR-04: `mergeSettings` read-modify-write race.
These are tracked in the verification report and intentionally left unaddressed here.
</notes>

<output>
Create `.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-05-SUMMARY.md` when done.
</output>
