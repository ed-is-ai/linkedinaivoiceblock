---
phase: 25.1-capture-and-export-unflagged-posts-for-eval-negatives
plan: 03
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - src/dashboard/dataManagement.ts
  - src/dashboard/dataManagement.test.ts
autonomous: true
requirements: [EXPORT-01]
must_haves:
  truths:
    - "buildJsonExport emits a NEW top-level unflaggedPosts[] array alongside the existing flaggedAccounts[]"
    - "Each exported unflagged entry carries urn, authorId, authorName, text, score, an ISO seenAt, and passes through any user-added label"
    - "The existing flaggedAccounts nesting and the CSV builders are unchanged; existing buildJsonExport([], []) callers/tests still compile via a defaulted third arg"
  artifacts:
    - path: "src/dashboard/dataManagement.ts"
      provides: "buildJsonExport with additive unflagged parameter and top-level unflaggedPosts array"
      contains: "unflaggedPosts"
    - path: "src/dashboard/dataManagement.test.ts"
      provides: "test asserting unflaggedPosts appears at top level and existing assertions still pass"
      contains: "unflaggedPosts"
  key_links:
    - from: "buildJsonExport(accounts, posts, unflagged)"
      to: "payload.unflaggedPosts"
      via: "new sibling array on the returned JSON payload"
      pattern: "unflaggedPosts:"
---

<objective>
Make the dashboard "Export JSON" output carry the captured negatives. Add a third, defaulted `unflagged` parameter to `buildJsonExport` and emit a new top-level `unflaggedPosts: [...]` array — a sibling of `flaggedAccounts`, NOT nested under any account.

Purpose: This is the export side of the eval-negatives pipeline. The Phase 26 eval walker will read this top-level array in addition to `flaggedAccounts[].posts[]` (D-08). The change must be purely additive so the JSON/CSV exports and their existing tests are unaffected (D-07).

Output: `buildJsonExport` extended additively in `src/dashboard/dataManagement.ts`; a new test in `src/dashboard/dataManagement.test.ts`.

Implements D-07 (additive top-level `unflaggedPosts[]`, do not fake account nesting, keep existing structure intact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-CONTEXT.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive unflaggedPosts array to buildJsonExport + test</name>
  <read_first>
    - src/dashboard/dataManagement.ts L1 (type-import line) and L14-36 (the current `buildJsonExport` body — the exact self-analog to extend additively)
    - src/dashboard/dataManagement.ts L81-102 (the CSV/traces builders — confirm these must NOT be touched)
    - src/dashboard/dataManagement.test.ts L73-121 (the existing `buildJsonExport` describe block; note the `buildJsonExport([], [])` two-arg calls at L75, L82, L89, L97, L103, L112, L117 that must keep compiling)
    - src/shared/types.ts `UnflaggedPost` interface (from Plan 01) — the input element shape, including `seenAt: number` and optional `label?`
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md L190-220 (exact additive-change guidance and the default-arg rationale)
  </read_first>
  <behavior>
    - `buildJsonExport([], [])` (two args) still returns JSON whose parse has `exportedAt` and `flaggedAccounts` — unchanged existing behavior.
    - `buildJsonExport([], [])` with no third arg yields a top-level `unflaggedPosts` that is `[]` (defaulted).
    - `buildJsonExport([], [], [unflaggedPost])` yields a top-level `unflaggedPosts` array of length 1 whose entry has `urn`, `authorId`, `authorName`, `text`, `score`, an ISO-string `seenAt`, and passes through a `label` when present on the input.
    - The `flaggedAccounts` nesting (accounts with `posts[]`, ISO timestamps) is byte-for-byte unchanged for the same first two args.
  </behavior>
  <action>
    In `src/dashboard/dataManagement.ts`:

    (1) Add `UnflaggedPost` to the line-1 type import from `'../shared/types'`.

    (2) Change the `buildJsonExport` signature to add a third parameter with a default: `unflagged: UnflaggedPost[] = []`. The default `[]` keeps the six existing two-arg test calls and the dashboard caller compiling unchanged.

    (3) On the returned `payload` object, add a new top-level sibling property AFTER `flaggedAccounts` (do NOT nest it inside any account): `unflaggedPosts: unflagged.map(p => ({ urn: p.urn, authorId: p.authorId, authorName: p.authorName, text: p.text, score: p.score, seenAt: new Date(p.seenAt).toISOString(), ...(p.label !== undefined ? { label: p.label } : {}) }))`. Pass through `label` only when present (D-06: the extension never invents one; the export just forwards a user-supplied label if the input has it).

    Do NOT modify the `flaggedAccounts` mapping, `csvEscape`, `buildPostsCsvExport`, `buildTracesExport`, `deriveCleanseCount`, or `filterCleansed`.

    In `src/dashboard/dataManagement.test.ts`: add at least one test in the `buildJsonExport` describe block asserting (a) a two-arg call yields a top-level `unflaggedPosts` equal to `[]`, and (b) a three-arg call with one `UnflaggedPost` yields `parsed.unflaggedPosts` length 1 with the expected `urn`/`score`/ISO `seenAt`/passed-through `label`. Add a small `makeUnflagged()` factory if helpful (mirror the existing `makePost`/`makeAccount` factories in that file).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test -- dataManagement</automated>
  </verify>
  <acceptance_criteria>
    - `src/dashboard/dataManagement.ts` `buildJsonExport` signature has a third param defaulting to `[]`
    - The returned payload contains a top-level `unflaggedPosts` key (sibling of `flaggedAccounts`), NOT nested under an account
    - Each mapped unflagged entry contains `urn`, `authorId`, `authorName`, `text`, `score`, and an ISO-string `seenAt`; `label` is forwarded only when present on the input
    - The existing `flaggedAccounts` mapping and all CSV/traces/cleanse functions are unchanged
    - `src/dashboard/dataManagement.test.ts` contains a new assertion referencing `unflaggedPosts`
    - `npm test -- dataManagement` exits 0 (all existing buildJsonExport tests still pass via the default arg); `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>buildJsonExport emits a top-level unflaggedPosts[] additively, existing structure and CSV builders untouched, existing two-arg tests still green, and a new test asserts the additive array.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored unflagged posts → exported JSON file | Captured post text leaves chrome.storage.local into a user-initiated download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.1-07 | Information Disclosure | exported `unflaggedPosts[]` text | mitigate | Only the already-truncated (1000-char) stored text is serialized; export is user-initiated (clicks Export JSON) and writes to a local file, no network egress |
| T-25.1-08 | Tampering | eval label integrity | mitigate | Export forwards a `label` ONLY if the user already added one; the builder never synthesizes a label, preserving D-06 ground-truth integrity |
| T-25.1-SC | Tampering | npm installs | accept | No package installs in this phase |
</threat_model>

<verification>
- `npm test -- dataManagement` green: existing buildJsonExport tests pass unchanged + new unflaggedPosts assertions pass.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Export JSON has a top-level `unflaggedPosts[]`; `flaggedAccounts` nesting and CSV exports unchanged.
- Existing tests pass via the defaulted third arg; new test covers the additive array.
</success_criteria>

<output>
Create `.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-03-SUMMARY.md` when done.
</output>
