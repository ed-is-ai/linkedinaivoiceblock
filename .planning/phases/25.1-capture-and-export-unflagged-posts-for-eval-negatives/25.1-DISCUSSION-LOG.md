# Phase 25.1: Capture and export unflagged posts for eval negatives - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-14
**Phase:** 25.1-capture-and-export-unflagged-posts-for-eval-negatives
**Areas discussed:** What to capture + volume cap (others taken as Claude's discretion)

---

## What to capture (capture set)

| Option | Description | Selected |
|--------|-------------|----------|
| Below FLAG_THRESHOLD only | Capture posts dropped at index.ts:323; clean complement to existing storage | ✓ |
| All scored, non-hidden posts | Includes flagged-but-not-hidden band; overlaps flaggedAccounts | |
| Every scored post (full mirror) | Duplicates existing stores; bloats storage | |

**User's choice:** Below FLAG_THRESHOLD only.

---

## Volume cap + eviction

| Option | Description | Selected |
|--------|-------------|----------|
| FIFO cap 200, newest-first | Mirrors storedPosts | ✓ |
| FIFO cap 500, newest-first | Larger budget | |
| Configurable cap (default 200) | Settings-exposed | |

**User's choice:** FIFO cap 200, newest-first.

---

## Capture rate (overflow selection)

| Option | Description | Selected |
|--------|-------------|----------|
| Capture all, FIFO evict (dedupe by URN) | Store holds 200 most-recent unique negatives | ✓ |
| Probabilistic sample (1-in-N) | Spread across sessions | |
| Dedup-by-author bias | Cap negatives per author | |

**User's choice:** Capture all, FIFO evict; dedupe by URN.

## Claude's Discretion

- Opt-in/privacy: separate opt-in toggle, OFF by default (distinct from Phase 7 hide-time storage opt-in); reuse 1000-char truncation.
- Labeling integrity: captured posts stored UNLABELED — no auto `label:"human"` (would corrupt the eval); human supplies ground truth by editing the export.
- Export shape: new top-level `unflaggedPosts[]` array in buildJsonExport (additive); entries carry urn/authorId/authorName/text/score/seenAt + room for a user label.
- Cross-phase: Phase 26's eval walker MUST read both flaggedAccounts[].posts[] and unflaggedPosts[] (amends 26-CONTEXT D-07/D-08).
- Capture is engine-agnostic and free (piggybacks on detection that already ran).

## Deferred Ideas

- In-dashboard labeling UI (vs hand-editing exported JSON).
- Per-author diversity caps / probabilistic sampling — declined for now; revisit if the 200-cap is too session-biased.
