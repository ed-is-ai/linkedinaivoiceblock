---
phase: 25.1-capture-and-export-unflagged-posts-for-eval-negatives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
autonomous: true
requirements: [CAPTURE-01]
must_haves:
  truths:
    - "A UnflaggedPost type exists describing an UNLABELED below-threshold post (no label is pre-populated)"
    - "StorageSchema declares an optional unflaggedPosts array key"
    - "Settings declares an optional captureUnflaggedPosts opt-in that is OFF (false) when unset"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "UnflaggedPost interface, unflaggedPosts StorageSchema key, captureUnflaggedPosts Settings field"
      contains: "interface UnflaggedPost"
  key_links:
    - from: "src/shared/storage.ts storageGet/storageSet generics"
      to: "src/shared/types.ts StorageSchema.unflaggedPosts"
      via: "keyof StorageSchema constraint"
      pattern: "unflaggedPosts\\?: UnflaggedPost\\[\\]"
---

<objective>
Add the type foundation for capturing below-FLAG_THRESHOLD posts as eval negatives: a new `UnflaggedPost` interface, a new `unflaggedPosts` key on `StorageSchema`, and a new `captureUnflaggedPosts` opt-in field on `Settings`.

Purpose: The typed storage wrapper (`storageGet`/`storageSet` in `src/shared/storage.ts`) is generic over `keyof StorageSchema`, so NO downstream code (persist helper, content-script capture, export, dashboard read) can compile against `unflaggedPosts` until the schema declares it. This plan is the Wave 1 prerequisite for all others.

Output: `src/shared/types.ts` extended with three purely-additive declarations. No behavior change on its own.

Implements D-02 (new `unflaggedPosts` key), D-04 (record engine + score), D-05 (new opt-in default false), D-06 (stored UNLABELED — `label` optional, never pre-populated), D-07 (export field set).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-CONTEXT.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UnflaggedPost type, unflaggedPosts schema key, and captureUnflaggedPosts Settings field</name>
  <read_first>
    - src/shared/types.ts L137-141 (the `Settings` interface — currently holds only `autoHideThreshold`; confirm there is NO existing capture/post-storage toggle, per the PATTERNS.md correction)
    - src/shared/types.ts L166-179 (the `StoredPost` interface — exact analog for the new `UnflaggedPost` shape, including doc-comment style)
    - src/shared/types.ts L335-364 (the `StorageSchema` interface — `storedPosts?: StoredPost[]` at L353-354 is the sibling-key analog)
    - src/shared/storage.ts L25-39 (storageGet/storageSet are generic over `keyof StorageSchema` — this is WHY the key must be declared here first)
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-PATTERNS.md L86-122 (exact type/field guidance)
  </read_first>
  <action>
    In `src/shared/types.ts`, make three additive edits:

    (1) Add an exported `UnflaggedPost` interface modeled on `StoredPost`, with fields: `urn: string`, `authorId: string`, `authorName: string`, `score: number`, `text: string` (doc: truncated at 1000 chars), `seenAt: number` (Unix ms when the post was seen — NOT `hiddenAt`, since these posts are never hidden), `engineUsed: 'heuristic' | 'llm'` (D-04: which engine computed the score), and an optional `label?: string` (D-06/D-07: room for a HUMAN-added ground-truth label — the extension MUST NEVER pre-populate this; doc-comment it as "user-supplied later by editing the exported JSON; never written by the extension"). Place it directly after the `StoredPost` interface (after L179).

    (2) In `StorageSchema`, add a sibling key beside `storedPosts` (after L354): `unflaggedPosts?: UnflaggedPost[];` with a doc comment: "Newest-first array of below-FLAG_THRESHOLD posts captured as eval negatives. Stored UNLABELED. Capped at 200 with oldest-evicted; content script writes (gated by Settings.captureUnflaggedPosts, off by default); dashboard reads for export."

    (3) In `Settings` (L138-141), add: `captureUnflaggedPosts?: boolean;` with a doc comment: "Opt-in (default OFF) to capture below-threshold posts as eval negatives. Distinct, deliberately-enabled eval-prep feature; stores text the user never flagged — a broader privacy surface than flagged-post storage. When undefined, treat as false." Per D-05 this is a NEW field — there is no prior post-storage toggle to mirror.

    Do NOT change any existing field, the `StoredPost` shape, or any other interface. All three additions are optional (`?`) so existing stored data and existing callers remain valid.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/types.ts` contains `export interface UnflaggedPost` with a `seenAt: number` field, an `engineUsed: 'heuristic' | 'llm'` field, and an optional `label?: string` field
    - `src/shared/types.ts` `StorageSchema` contains `unflaggedPosts?: UnflaggedPost[]`
    - `src/shared/types.ts` `Settings` contains `captureUnflaggedPosts?: boolean`
    - The `UnflaggedPost` interface does NOT contain a `hiddenAt` field (uses `seenAt` instead)
    - No existing field was renamed or removed (StoredPost, autoHideThreshold, storedPosts all unchanged)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The three additive type declarations exist, are all optional, never pre-populate `label`, and the project type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn feed DOM → extension storage | Post text the user never flagged is read from the page and persisted locally (broader privacy surface than flagged-post storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.1-01 | Information Disclosure | `Settings.captureUnflaggedPosts` default | mitigate | Field is optional and treated as `false` when undefined — no capture occurs without the user explicitly enabling it (consumer plans enforce `?? false`) |
| T-25.1-02 | Repudiation | `UnflaggedPost.label` | mitigate | `label?` is optional and the type contract documents it as user-supplied-only; the extension never writes it, preserving eval ground-truth integrity (D-06) |
| T-25.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; no new dependencies added |
</threat_model>

<verification>
- `npx tsc --noEmit` passes (the new optional fields do not break the strict/`noUncheckedIndexedAccess` build).
- Grep confirms the three new identifiers exist in `src/shared/types.ts`.
</verification>

<success_criteria>
- `UnflaggedPost`, `unflaggedPosts` key, and `captureUnflaggedPosts` field all present and additive.
- Project type-checks clean; no existing declaration altered.
</success_criteria>

<output>
Create `.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-01-SUMMARY.md` when done.
</output>
