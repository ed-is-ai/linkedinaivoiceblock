---
phase: 25.1-capture-and-export-unflagged-posts-for-eval-negatives
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CAPTURE-01, CAPTURE-02, EXPORT-04]
must_haves:
  truths:
    - "CAPTURE-01 is formally defined in .planning/REQUIREMENTS.md with a description and a traceability row mapping it to Phase 25.1"
    - "CAPTURE-02 is formally defined in .planning/REQUIREMENTS.md with a description and a traceability row mapping it to Phase 25.1"
    - "A new eval-negatives export requirement (EXPORT-04) is defined in .planning/REQUIREMENTS.md scoped to the top-level unflaggedPosts[] array, without mutating the Phase 9 v1.1 EXPORT-01 definition"
    - "The ROADMAP and the Phase 25.1 plan references reconcile to the authoritative requirement IDs (CAPTURE-01, CAPTURE-02, EXPORT-04)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formal CAPTURE-01, CAPTURE-02, EXPORT-04 definitions plus traceability rows for Phase 25.1"
      contains: "CAPTURE-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 25.1 Requirements line reconciled to CAPTURE-01, CAPTURE-02, EXPORT-04"
      contains: "EXPORT-04"
  key_links:
    - from: ".planning/REQUIREMENTS.md Eval Negatives section"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "each new REQ-ID has a matching traceability row mapped to Phase 25.1"
      pattern: "CAPTURE-01 \\| Phase 25.1|CAPTURE-01.*25.1"
---

<objective>
Close gap 2 (REQUIREMENTS ORPHAN) from `25.1-VERIFICATION.md`: CAPTURE-01 and CAPTURE-02 are referenced by all four existing Phase 25.1 plans and by the ROADMAP but are entirely absent from the active `.planning/REQUIREMENTS.md`. The export capability is referenced as EXPORT-01, but the active REQUIREMENTS.md defines EXPORT-01 only inside the archived v1.1 milestone ("export all flagged accounts and stored posts"), not as the Phase 25.1 eval-negatives `unflaggedPosts[]` capability. Add authoritative definitions and traceability rows so the implemented work is traceable and auditable.

Purpose: Phase 25.1 lives in the v9.0 (Eval Harness) milestone alongside EVAL-01..04. The implementation is already verified-correct; this plan supplies the missing authoritative requirement document so future audits can verify against a definition rather than against orphaned IDs.

Output: new CAPTURE-01, CAPTURE-02, and EXPORT-04 requirement entries + traceability rows in `.planning/REQUIREMENTS.md`, and a reconciled Phase 25.1 Requirements reference in `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-VERIFICATION.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CAPTURE-01, CAPTURE-02, EXPORT-04 in REQUIREMENTS.md with traceability rows</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the full file. Learn the exact conventions: checkbox-prefixed requirement lines `- [x] **ID-NN**: description.`; bolded IDs; one `## Section` heading per requirement group; per-milestone `## Traceability` table with header `| REQ-ID | Phase | Status |`. Note the v9.0 milestone "Eval Harness" section begins at line 128 with EVAL-01..04 at lines 138-141 — this is where Phase 25.1 belongs.)
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-CONTEXT.md (decisions D-01..D-08: what is captured, opt-in OFF by default, UNLABELED storage, top-level unflaggedPosts[] export shape)
    - .planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-VERIFICATION.md (Requirements Coverage table at lines 113-119 — the exact scope wording the new requirements must match: CAPTURE-01 = "Type foundation + below-threshold capture helper"; CAPTURE-02 = "Opt-in gate + popup toggle + settings merge"; EXPORT = "Export JSON includes unflaggedPosts[] top-level array")
  </read_first>
  <action>
    Add a new `## Eval Negatives Capture & Export (CAPTURE / Phase 25.1)` requirement section inside the v9.0 "Eval Harness" milestone, immediately after the EVAL-04 line (line 141) and before that milestone's "## Future Requirements (deferred from v9.0)" heading. Use the existing checkbox+bold-ID convention (`- [x] **ID**: description.`) and mark all three as `[x]` (the implementation is verified complete).

    Define exactly three requirements:
    - **CAPTURE-01**: The detector's below-`FLAG_THRESHOLD` posts (the human-looking negatives dropped at `content/index.ts` early return) are persisted UNLABELED to a new capped `unflaggedPosts` store in `chrome.storage.local` — a newest-first array, FIFO-capped at 200, deduped by `urn`, post text truncated to 1000 chars, recording `seenAt`, `score`, and `engineUsed`; backed by an `UnflaggedPost` type, an `unflaggedPosts` StorageSchema key, and a `persistUnflaggedPost` helper. (Cite the source: types.ts, postStore.ts.)
    - **CAPTURE-02**: Capture is gated behind a distinct opt-in `Settings.captureUnflaggedPosts`, OFF by default, deliberately separate from any other storage opt-in (it stores text the user never flagged — a broader privacy surface); the popup exposes a toggle for it and writes settings via a merge so the opt-in and `autoHideThreshold` do not clobber each other; the content-script capture call fires only when the opt-in is enabled.
    - **EXPORT-04**: The dashboard "Export JSON" includes a top-level `unflaggedPosts[]` array (sibling of `flaggedAccounts[]`) so the Phase 26 eval has real human-negatives; the export builder adds the array additively (existing flagged-account nesting and CSV exports unchanged), and the Export JSON button is reachable whenever unflagged posts exist even with zero flagged accounts.

    In the requirement text, note explicitly that EXPORT-04 is a NEW ID scoped to Phase 25.1 eval negatives and does NOT modify the archived v1.1 EXPORT-01 (flagged accounts + stored posts). Use EXPORT-04 (not EXPORT-03) because EXPORT-03 is already assigned to the v1.2 "Export Posts CSV" requirement (`.planning/milestones/v1.2-REQUIREMENTS.md`) and must not collide.

    Then add three rows to the v9.0 milestone "## Traceability" table — the table whose header is `| REQ-ID | Phase | Status |` containing the EVAL-01..04 rows (lines 88-91 region of the v7.0 table is a DIFFERENT table; the EVAL rows live in the v9.0 milestone, which currently has NO traceability table). If the v9.0 milestone lacks a `## Traceability` table, create one matching the exact format of the v7.0 table (`| REQ-ID | Phase | Status |` with a `|--------|-------|--------|` separator row) and add rows for EVAL-01..04 (Phase 26 / Planned) plus the three new rows. Each new row: `| CAPTURE-01 | Phase 25.1 | Complete |`, `| CAPTURE-02 | Phase 25.1 | Complete |`, `| EXPORT-04 | Phase 25.1 | Complete |`. Match the column alignment and `Status` vocabulary (`Complete` / `Planned`) used by the existing v7.0 table.

    Do NOT mutate, renumber, or remove any existing requirement (EVAL-*, TRACE-*, SELECTOR-*, ADAPT-*) or the archived EXPORT-01 semantics.
  </action>
  <verify>
    <automated>grep -q "CAPTURE-01" .planning/REQUIREMENTS.md && grep -q "CAPTURE-02" .planning/REQUIREMENTS.md && grep -q "EXPORT-04" .planning/REQUIREMENTS.md && grep -q "CAPTURE-01 | Phase 25.1" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a `- [x] **CAPTURE-01**:` line with a description covering the unflaggedPosts store, FIFO cap 200, dedup-by-urn, 1000-char truncation, UNLABELED storage, and seenAt/score/engineUsed fields
    - `.planning/REQUIREMENTS.md` contains a `- [x] **CAPTURE-02**:` line covering the OFF-by-default `captureUnflaggedPosts` opt-in, popup toggle, merge-safe settings write, and gated capture
    - `.planning/REQUIREMENTS.md` contains a `- [x] **EXPORT-04**:` line covering the additive top-level `unflaggedPosts[]` export array and the reachable Export JSON button
    - The EXPORT-04 text explicitly states it does not modify the v1.1 EXPORT-01 definition
    - The v9.0 milestone Traceability table contains rows `CAPTURE-01 | Phase 25.1`, `CAPTURE-02 | Phase 25.1`, `EXPORT-04 | Phase 25.1`
    - No existing requirement line or traceability row was modified or removed (EXPORT-03 still maps to v1.2 Posts CSV; EXPORT-01 archived semantics untouched)
  </acceptance_criteria>
  <done>CAPTURE-01, CAPTURE-02, and EXPORT-04 are authoritatively defined in the v9.0 milestone of REQUIREMENTS.md with matching traceability rows, no existing IDs disturbed, and EXPORT-03 collision avoided.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP Phase 25.1 references to the authoritative IDs</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read lines 277-294 — the Phase 25.1 section. The `**Requirements**:` line at line 280 reads `CAPTURE-01, CAPTURE-02, EXPORT-01`; the per-plan bullets at lines 291 and 294 cite `EXPORT-01`.)
    - .planning/REQUIREMENTS.md (confirm the IDs just defined in Task 1: CAPTURE-01, CAPTURE-02, EXPORT-04)
  </read_first>
  <action>
    In `.planning/ROADMAP.md`, in the Phase 25.1 section only: change the `**Requirements**:` line (line 280) from `CAPTURE-01, CAPTURE-02, EXPORT-01` to `CAPTURE-01, CAPTURE-02, EXPORT-04`. Update the two per-plan bullet citations that reference `EXPORT-01` — line 291 (`25.1-03-PLAN.md — … (EXPORT-01)`) and line 294 (`25.1-04-PLAN.md — … (EXPORT-01, CAPTURE-02)`) — replacing `EXPORT-01` with `EXPORT-04` in both. Do not alter any other phase's requirements, plan checkboxes, goal text, or wave structure. Leave the existing `[x]` completion checkboxes on the Phase 25.1 plan bullets as-is. Do NOT edit the four existing PLAN.md files (25.1-01..04) — their `requirements:` frontmatter is a historical record of execution and is out of scope for this documentation reconciliation; the authoritative mapping now lives in REQUIREMENTS.md and the ROADMAP.
  </action>
  <verify>
    <automated>grep -q "EXPORT-04" .planning/ROADMAP.md && ! grep -n "EXPORT-01" .planning/ROADMAP.md | grep -q "25.1" && grep -q "CAPTURE-01, CAPTURE-02, EXPORT-04" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 25.1 `**Requirements**:` line in `.planning/ROADMAP.md` reads `CAPTURE-01, CAPTURE-02, EXPORT-04`
    - Neither Phase 25.1 per-plan bullet (25.1-03, 25.1-04) cites `EXPORT-01` anymore; both cite `EXPORT-04`
    - No other phase section in ROADMAP.md was modified
    - The Phase 25.1 plan-completion checkboxes remain `[x]`
  </acceptance_criteria>
  <done>The ROADMAP Phase 25.1 requirements line and per-plan citations reference CAPTURE-01, CAPTURE-02, EXPORT-04, matching the authoritative definitions in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
- `grep` confirms CAPTURE-01, CAPTURE-02, EXPORT-04 all present in REQUIREMENTS.md with traceability rows mapped to Phase 25.1.
- `grep` confirms no Phase 25.1 reference in ROADMAP.md still cites EXPORT-01.
- Manual scan: the archived v1.1 EXPORT-01 and v1.2 EXPORT-03 definitions are untouched (no ID collision, no semantic mutation).
</verification>

<success_criteria>
Gap 2 closed: CAPTURE-01, CAPTURE-02, and EXPORT-04 are formally defined in the authoritative `.planning/REQUIREMENTS.md` (v9.0 milestone) with traceability rows mapping them to Phase 25.1, and the ROADMAP references reconcile to those IDs — making the verified-but-orphaned Phase 25.1 implementation fully traceable.
</success_criteria>

<notes>
ID choice rationale: the planning brief suggested EXPORT-03, but EXPORT-03 is already assigned to the v1.2 "Export Posts CSV" requirement (`.planning/milestones/v1.2-REQUIREMENTS.md`). To avoid a cross-milestone ID collision, this plan introduces EXPORT-04 for the Phase 25.1 eval-negatives export. The archived v1.1 EXPORT-01 (flagged accounts + stored posts) is left unchanged, satisfying the brief's "do NOT mutate the Phase 9 semantics" constraint.

This is a documentation-only plan; no source code is touched and no build/test run is required beyond the grep assertions.
</notes>

<output>
Create `.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-06-SUMMARY.md` when done.
</output>
