---
phase: 25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/26-eval-runner/26-CONTEXT.md
autonomous: true
requirements: [EXPORT-05]

must_haves:
  truths:
    - "REQUIREMENTS.md defines EXPORT-05 describing the symmetric export redesign"
    - "REQUIREMENTS.md traceability table lists EXPORT-05 against Phase 25.2"
    - "ROADMAP Phase 25.2 Requirements line reads EXPORT-05 (no longer TBD)"
    - "26-CONTEXT.md eval walker contract reads top-level flaggedPosts[] (positives) + unflaggedPosts[] (negatives), not nested flaggedAccounts[].posts[]"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "EXPORT-05 requirement definition + traceability row"
      contains: "EXPORT-05"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 25.2 Requirements line set to EXPORT-05"
      contains: "EXPORT-05"
    - path: ".planning/phases/26-eval-runner/26-CONTEXT.md"
      provides: "Amended eval-input walker contract referencing flaggedPosts[]"
      contains: "flaggedPosts[]"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "EXPORT-05 requirement id"
      pattern: "EXPORT-05"
---

<objective>
Establish the requirement-tracking and cross-phase contract for the symmetric export redesign before the code lands.

Purpose: EXPORT-05 is the new requirement ID this phase tracks under. The ROADMAP currently shows `TBD`, and the Phase 26 eval consumer must read positives from the new top-level `flaggedPosts[]`. This plan records EXPORT-05 in REQUIREMENTS.md, wires the ROADMAP line, and confirms the 26-CONTEXT.md eval-walker amendment matches the locked contract (D-07).

Output: Updated REQUIREMENTS.md (EXPORT-05 entry + traceability row), updated ROADMAP.md (Phase 25.2 Requirements line), and a verified/corrected 26-CONTEXT.md amendment block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/26-eval-runner/26-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EXPORT-05 requirement and traceability row in REQUIREMENTS.md; set ROADMAP Phase 25.2 Requirements line</name>
  <read_first>
    - .planning/REQUIREMENTS.md (read the existing EXPORT-04 entry at ~L151 and the Traceability table at L157-165 — mirror their wording and table format exactly)
    - .planning/ROADMAP.md (read Phase 25.2 block at L296-304, especially the `**Requirements**: TBD` line at L299)
    - .planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-CONTEXT.md (D-01..D-06 — the authoritative shape this requirement describes)
  </read_first>
  <action>
    In REQUIREMENTS.md, add a new checkbox entry `EXPORT-05` immediately after `EXPORT-04` (under the "Eval Negatives Capture & Export" section). Mirror EXPORT-04's wording style. The EXPORT-05 text must state: the dashboard "Export JSON" adds a NEW top-level post-centric `flaggedPosts[]` array (sibling of `flaggedAccounts[]` and `unflaggedPosts[]`), sourced from the existing `storedPosts` (hidden posts), with entry shape mirroring `unflaggedPosts` but using native `hiddenAt` (not `seenAt`), OMITTING `engineUsed`, and including an optional user-added `label` emitted only when present; each exported `flaggedAccounts[]` entry replaces its `status` enum with a derived `blocked: boolean` (`blocked = status === 'blocked'`) with `status` dropped from output; the nested `flaggedAccounts[].posts[]` array is retained (intentional duplication, D-06); the change is additive/transform-only over already-stored data (no new capture, no storage-schema change, `FlaggedAccount.status` in storage/types unchanged); and the archived EXPORT-01/03 and the Phase 25.1 EXPORT-04 definitions are untouched. State that EXPORT-05 enriches the EVAL-01 downstream dataset.
    Add a traceability table row `| EXPORT-05 | Phase 25.2 | Planned |` to the v9.0 Traceability table.
    In ROADMAP.md, change the Phase 25.2 `**Requirements**: TBD (run /gsd-discuss-phase 25.2)` line to `**Requirements**: EXPORT-05`.
  </action>
  <verify>
    <automated>grep -q "EXPORT-05" .planning/REQUIREMENTS.md && grep -q "EXPORT-05 | Phase 25.2" .planning/REQUIREMENTS.md && grep -q "Requirements\*\*: EXPORT-05" .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains an `EXPORT-05` checkbox entry describing top-level `flaggedPosts[]` + `blocked` boolean transform + retained nested `posts[]`
    - REQUIREMENTS.md Traceability table contains a row `| EXPORT-05 | Phase 25.2 | Planned |`
    - EXPORT-05 text explicitly states it is additive/transform-only and does NOT modify archived EXPORT-01/03 or EXPORT-04
    - ROADMAP.md Phase 25.2 `**Requirements**` line reads `EXPORT-05` and no longer contains `TBD`
    - EXPORT-04 entry and all archived requirement definitions remain byte-unchanged
  </acceptance_criteria>
  <done>EXPORT-05 is defined in REQUIREMENTS.md with a traceability row, and ROADMAP Phase 25.2 Requirements line reads EXPORT-05.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm/correct the 26-CONTEXT.md eval-walker amendment matches the D-07 flaggedPosts[] contract</name>
  <read_first>
    - .planning/phases/26-eval-runner/26-CONTEXT.md (read the "Amendment — Phase 25.2" block at L39-50 — it already exists; verify it is correct and complete)
    - .planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-CONTEXT.md (D-07 — the authoritative eval-walker contract)
  </read_first>
  <action>
    Verify the existing "Amendment — Phase 25.2" block in 26-CONTEXT.md (L39-50) states ALL of: (1) the eval walker reads BOTH top-level `flaggedPosts[]` (positives) and `unflaggedPosts[]` (negatives); (2) it must NOT walk `flaggedAccounts[].posts[]` (that nested array is a harmless human-readable duplicate of `flaggedPosts[]` and reading it would double-count positives); (3) the `flaggedPosts[]` entry shape is `{ urn, authorId, authorName, text, score, hiddenAt }` + optional user-added `label` (note: OMITS `engineUsed`, matching D-03); (4) both arrays are stored UNLABELED by default — the user adds `label: "ai"` to positives / `label: "human"` to negatives before eval; unlabeled entries are skipped; (5) positive class for metrics = `ai`; (6) the stored `score` is ignored (eval re-scores fresh). If any clause is missing or drifts from D-07, correct the amendment in place. If all six clauses are already present and accurate, make no edit and record "amendment already correct, no change" in the SUMMARY. Do NOT delete the "Superseded — Phase 25.1" block beneath it (kept as history).
  </action>
  <verify>
    <automated>grep -q "flaggedPosts\[\]" .planning/phases/26-eval-runner/26-CONTEXT.md && grep -q "double-count" .planning/phases/26-eval-runner/26-CONTEXT.md && grep -qi "positive class for metrics = \`ai\`" .planning/phases/26-eval-runner/26-CONTEXT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 26-CONTEXT.md "Amendment — Phase 25.2" block names `flaggedPosts[]` as the positive source and `unflaggedPosts[]` as the negative source
    - The block explicitly instructs NOT to walk `flaggedAccounts[].posts[]` (double-count avoidance)
    - The `flaggedPosts[]` shape listed in the block OMITS `engineUsed`
    - Positive class for metrics is stated as `ai`; unlabeled entries skipped; stored `score` ignored
    - The "Superseded — Phase 25.1" history block is still present (not deleted)
  </acceptance_criteria>
  <done>The 26-CONTEXT.md Phase 25.2 amendment fully and accurately encodes the D-07 eval-walker contract (corrected if it had drifted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan edits planning markdown only (REQUIREMENTS.md, ROADMAP.md, 26-CONTEXT.md). No code, no runtime, no inputs, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.2-01 | Tampering | planning docs | accept | Docs-only edits under version control; git diff is the audit trail. No runtime surface. |

No package installs in this plan — package legitimacy gate not applicable.
</threat_model>

<verification>
- `grep "EXPORT-05" .planning/REQUIREMENTS.md` returns the requirement entry and the traceability row
- `grep "Requirements\*\*: EXPORT-05" .planning/ROADMAP.md` confirms the ROADMAP line is updated
- 26-CONTEXT.md amendment references `flaggedPosts[]` as the positive source and forbids walking the nested `posts[]`
</verification>

<success_criteria>
- EXPORT-05 exists in REQUIREMENTS.md (definition + traceability row), mirroring EXPORT-04's style, additive-only
- ROADMAP Phase 25.2 Requirements line reads `EXPORT-05`
- 26-CONTEXT.md eval-walker amendment accurately encodes the D-07 contract
</success_criteria>

<output>
Create `.planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-01-SUMMARY.md` when done.
</output>
