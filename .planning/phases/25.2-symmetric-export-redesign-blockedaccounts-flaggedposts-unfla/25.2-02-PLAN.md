---
phase: 25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/dashboard/dataManagement.ts
  - src/dashboard/dataManagement.test.ts
autonomous: true
requirements: [EXPORT-05]

must_haves:
  truths:
    - "buildJsonExport returns an object with a top-level flaggedPosts[] array sibling to flaggedAccounts and unflaggedPosts"
    - "Each flaggedPosts[] entry has urn, authorId, authorName, text, score, hiddenAt (ISO) and OMITS engineUsed"
    - "flaggedPosts[] emits label only when the source post carries a label (mirrors unflaggedPosts conditional spread)"
    - "Each exported flaggedAccounts[] entry has blocked: boolean and does NOT have status"
    - "blocked === true iff the source account status === 'blocked'"
    - "Each exported flaggedAccounts[] entry still has its nested posts[] array"
    - "FlaggedAccount.status in src/shared/types.ts is unchanged (storage shape untouched)"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "FlaggedPost interface mirroring UnflaggedPost (named export type)"
      contains: "interface FlaggedPost"
    - path: "src/dashboard/dataManagement.ts"
      provides: "Reshaped buildJsonExport: flaggedPosts[] + status->blocked transform + retained nested posts[]"
      contains: "flaggedPosts"
    - path: "src/dashboard/dataManagement.test.ts"
      provides: "Assertions for flaggedPosts[], blocked boolean, absent status, conditional label, empty defaults"
      contains: "flaggedPosts"
  key_links:
    - from: "src/dashboard/dataManagement.ts"
      to: "src/shared/types.ts"
      via: "import FlaggedPost type"
      pattern: "FlaggedPost"
    - from: "src/dashboard/dataManagement.ts buildJsonExport"
      to: "posts: StoredPost[] parameter"
      via: "flaggedPosts derives from existing posts param (no new param)"
      pattern: "flaggedPosts"
---

<objective>
Reshape `buildJsonExport` into the symmetric three-array export and update its tests.

Purpose: Give the Phase 26 eval a richer, post-centric positives source. Adds a NEW top-level `flaggedPosts[]` (from the existing `storedPosts`), collapses each exported account's `status` enum into a `blocked: boolean` (export-only transform), and retains the nested `flaggedAccounts[].posts[]` view (D-01..D-06). Storage/types `FlaggedAccount.status` is unchanged — the boolean is derived inside `buildJsonExport`.

Output: A `FlaggedPost` interface in `src/shared/types.ts`, a reshaped `buildJsonExport`, and updated `dataManagement.test.ts` assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-CONTEXT.md
@.planning/phases/25.1-capture-and-export-unflagged-posts-for-eval-negatives/25.1-CONTEXT.md
@src/dashboard/dataManagement.ts
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the FlaggedPost interface in src/shared/types.ts mirroring UnflaggedPost</name>
  <read_first>
    - src/shared/types.ts (read the `UnflaggedPost` interface at L196-216 — the doc-comment + field template to mirror; and `StoredPost` at L173-186 — the source shape the new type maps from)
    - .planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-CONTEXT.md (D-03 — the exact flaggedPosts entry shape)
  </read_first>
  <action>
    Add a new exported `FlaggedPost` interface to src/shared/types.ts, placed adjacent to `UnflaggedPost` for symmetry, with a doc comment in the same style as `UnflaggedPost` explaining it is the export-only post-centric positives shape for the Phase 25.2 symmetric export (sourced from hidden `storedPosts`; never written to storage — it is an export projection). Fields, in order: `urn: string`, `authorId: string`, `authorName: string`, `score: number`, `text: string`, `hiddenAt: number` (native hidden timestamp — note in the doc comment it stays `hiddenAt`, NOT `seenAt`, because these posts were hidden), and `label?: string` (optional user-added ground-truth label, never written by the extension, per 25.1 D-06). Do NOT include `engineUsed` (D-03 omits it). Do NOT modify `FlaggedAccount`, `StoredPost`, or `UnflaggedPost`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/types.ts contains `export interface FlaggedPost` with fields urn, authorId, authorName, score, text, hiddenAt, and optional label
    - FlaggedPost does NOT declare an `engineUsed` field
    - `FlaggedAccount` interface (incl. its `status` field) is byte-unchanged
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>A named `FlaggedPost` interface exists in types.ts mirroring UnflaggedPost, omitting engineUsed, with FlaggedAccount untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reshape buildJsonExport — top-level flaggedPosts[], status->blocked transform, retained nested posts[]</name>
  <files>src/dashboard/dataManagement.ts, src/dashboard/dataManagement.test.ts</files>
  <read_first>
    - src/dashboard/dataManagement.ts (read `buildJsonExport` at L14-49 — the single change site; the `unflaggedPosts` mapping at L37-45 is the exact template incl. the `...(p.label !== undefined ? { label: p.label } : {})` conditional-label idiom; the account map at L26-36 is where `status` must become `blocked`)
    - src/dashboard/dataManagement.test.ts (read the `buildJsonExport` describe block at L84-171, and the `makeAccount`/`makePost`/`makeUnflagged` fixtures at L14-82)
    - .planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <behavior>
    - Test: `buildJsonExport([], [])` returns an object with a top-level `flaggedPosts` key equal to `[]`
    - Test: given one StoredPost, `flaggedPosts[0]` has urn, authorId, authorName, text, score equal to the source, and `hiddenAt` equal to `new Date(source.hiddenAt).toISOString()`
    - Test: `flaggedPosts[0]` does NOT have an `engineUsed` property
    - Test: a StoredPost extended with `label: 'ai'` yields `flaggedPosts[0].label === 'ai'`; a StoredPost without label yields `flaggedPosts[0]` with NO `label` property
    - Test: given an account with `status: 'blocked'`, the exported `flaggedAccounts[0].blocked === true` and `flaggedAccounts[0]` has NO `status` property
    - Test: given an account with `status: 'pending'` (and separately `'dismissed'`), exported `flaggedAccounts[0].blocked === false`
    - Test: the exported `flaggedAccounts[0]` STILL has a nested `posts` array (D-06 retention) with the existing `{ urn, score, text, hiddenAt }` shape
    - Test: `flaggedPosts` is a top-level sibling, NOT nested under any flaggedAccounts entry
  </behavior>
  <action>
    In `buildJsonExport`: (1) Build a new top-level `flaggedPosts` array by mapping the existing `posts: StoredPost[]` parameter (the SAME source already used for nested account posts — confirm no new parameter is needed) to `{ urn, authorId, authorName, text, score, hiddenAt: new Date(p.hiddenAt).toISOString() }` plus the conditional-label spread `...(p.label !== undefined ? { label: p.label } : {})`, mirroring the `unflaggedPosts` mapping exactly (but with `hiddenAt` instead of `seenAt`, and without `engineUsed`). Place `flaggedPosts` as a top-level key in the payload, sibling to `flaggedAccounts` and `unflaggedPosts`. (2) In the `flaggedAccounts` account map, replace the spread-then-passthrough of `status` with a derived `blocked: a.status === 'blocked'` and ensure `status` is NOT emitted: change the `...a` spread so `status` is excluded (destructure `status` out, or build the exported object explicitly) and add `blocked`. Keep the existing `firstSeenAt`/`lastSeenAt` ISO conversions and the nested `posts` mapping unchanged (D-06 retention). Note: `StoredPost` has no `label` field today; reference it via an optional-access on the mapped param so the conditional spread compiles — if needed, type the map callback param to allow an optional `label` without modifying the `StoredPost` interface. Then update `dataManagement.test.ts`: extend `makePost` usage (or add inline overrides) to cover the labeled case, add the `flaggedPosts` assertions and the `blocked`/absent-`status` assertions per the behavior block, and keep all existing unflaggedPosts and nested-posts assertions passing.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/dataManagement.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `buildJsonExport` output JSON has a top-level `flaggedPosts` array sibling to `flaggedAccounts` and `unflaggedPosts`
    - `flaggedPosts[]` entries contain urn, authorId, authorName, text, score, hiddenAt (ISO string) and NOT engineUsed
    - `flaggedPosts[]` includes `label` only when the source post carries one (conditional spread)
    - Exported `flaggedAccounts[]` entries contain `blocked` (boolean) and do NOT contain `status`
    - `blocked === (sourceAccount.status === 'blocked')` verified for blocked / pending / dismissed inputs
    - Exported `flaggedAccounts[]` entries still contain the nested `posts[]` array
    - `npx vitest run src/dashboard/dataManagement.test.ts` exits 0
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>buildJsonExport emits the symmetric `{ exportedAt, flaggedAccounts (with blocked, no status, nested posts), flaggedPosts, unflaggedPosts }` shape and the scoped test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local -> exported JSON file | Already-stored post text and author names are projected into the downloaded JSON. This boundary already existed for `flaggedAccounts`/`unflaggedPosts`; `flaggedPosts[]` is the SAME data re-shaped — no NEW data leaves storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25.2-02 | Information Disclosure | buildJsonExport `flaggedPosts[]` | accept | `flaggedPosts[]` re-projects data already present in storage and already emitted under nested `flaggedAccounts[].posts[]` — no new exposure. Export is user-initiated, local-only (Blob download), no network. |
| T-25.2-03 | Tampering | exported JSON consumed by Phase 26 eval | accept | Eval treats export JSON as untrusted input and re-scores fresh (26 D-08); stored `score`/`label` are user-controlled by design. Out of scope for this transform-only plan. |

Pure in-memory transform over data already in chrome.storage.local at export time: no new network calls, no new inputs, no auth, no injection surface. No high-severity threats. No package installs — legitimacy gate not applicable.
</threat_model>

<verification>
- `npx vitest run src/dashboard/dataManagement.test.ts` passes with new `flaggedPosts` and `blocked` assertions
- `npm run type-check` exits 0 (FlaggedPost type wired, no StoredPost/FlaggedAccount changes)
- Manual shape check: a `buildJsonExport(accounts, posts, unflagged)` call yields `{ exportedAt, flaggedAccounts, flaggedPosts, unflaggedPosts }` with accounts carrying `blocked` (not `status`) and a retained nested `posts[]`
</verification>

<success_criteria>
- Top-level `flaggedPosts[]` present, sourced from `storedPosts`, shape mirrors `unflaggedPosts` minus `engineUsed`, with `hiddenAt` and conditional `label`
- Each exported account carries `blocked: boolean` derived from `status`, with `status` dropped and nested `posts[]` retained
- `FlaggedAccount.status` and `StoredPost` storage shapes are unchanged
- Scoped test suite and type-check pass
</success_criteria>

<output>
Create `.planning/phases/25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla/25.2-02-SUMMARY.md` when done.
</output>
