# Phase 25.2: Symmetric export redesign - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-14
**Phase:** 25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla
**Areas discussed:** Replace vs additive, flaggedPosts shape, blockedAccounts content

---

## Area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Replace vs additive | Replace flaggedAccounts vs keep alongside | ✓ |
| flaggedPosts source + gap | Source of flaggedPosts; 35-60 text gap | (folded into flaggedPosts shape) |
| flaggedPosts shape | Exact entry fields | ✓ |
| blockedAccounts content | Filter + nesting | ✓ |

---

## Replace vs additive

| Option | Description | Selected |
|--------|-------------|----------|
| Full replace | Remove flaggedAccounts; export = { exportedAt, blockedAccounts[], flaggedPosts[], unflaggedPosts[] } | ✓ |
| Additive (keep both) | Keep flaggedAccounts[].posts[] AND add new arrays; duplicates data | |

**User's choice:** Full replace — *later revised* in the blockedAccounts area (see below) to keep `flaggedAccounts` with a `blocked` boolean rather than a filtered `blockedAccounts`. The durable new element is the top-level `flaggedPosts[]`.

### Phase 26 contract follow-on

| Option | Description | Selected |
|--------|-------------|----------|
| Amend 26-CONTEXT now | Update 26-CONTEXT D-07/D-08 amendment as part of this phase | ✓ |
| Note for 26 planner only | Leave 26-CONTEXT untouched; reconcile at plan time | |

**User's choice:** Amend 26-CONTEXT now. (Done — the eval walker now reads `flaggedPosts[]` + `unflaggedPosts[]`.)

---

## flaggedPosts shape

### Source

| Option | Description | Selected |
|--------|-------------|----------|
| Existing storedPosts | flaggedPosts = storedPosts (hidden, score>=60) reshaped flat; export-only; accepts 35-60 text gap | ✓ |
| Expand capture to fill gap | Also capture text for flagged-not-hidden 35-60 band; larger scope | |

**User's choice:** Existing storedPosts — export-only, gap accepted.

### Fields

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror unflaggedPosts | { urn, authorId, authorName, text, score, hiddenAt, label? }; keep native hiddenAt; omit engineUsed | ✓ |
| Exact storedPosts fields | { urn, authorId, authorName, score, text, hiddenAt } verbatim, no label | |

**User's choice:** Mirror unflaggedPosts (keep `hiddenAt`, optional `label`, omit `engineUsed`).

---

## blockedAccounts content

| Option | Description | Selected |
|--------|-------------|----------|
| Blocked only, no nested posts | status==='blocked' only; account metadata only | |
| Blocked only, keep nested posts | status==='blocked' only; nest posts[] | |
| All flagged accounts | Export every flaggedAccount regardless of status | |
| **Other (user free-text)** | Keep flaggedAccounts; add a per-account `blocked` property | ✓ |

**User's choice (free-text):** "actually, keep flaggedAccounts. But add a new property to each account to show if it's blocked" → refined to **replace `status` with `blocked: boolean`**, and **keep nested posts**.

**Notes:**
- User refined #1 in plain text: replace the `status` enum with a `blocked` boolean (flagged tradeoff: loses pending/dismissed distinction — accepted).
- For nesting: chose **Keep nested posts** — accepts the duplication with top-level `flaggedPosts[]` for the convenience of the account→posts view.
- This supersedes the earlier "full replace / rename to blockedAccounts" choice from the first area.

---

## Claude's Discretion

- Whether to add a named `FlaggedPost` interface vs inline mapping in `buildJsonExport`.
- Exact `dataManagement.test.ts` assertions for the new shape.
- `buildJsonExport` signature (likely no new param — `flaggedPosts` derives from the existing `posts` arg).

## Deferred Ideas

- Fill the 35–60 positives gap by capturing flagged-but-not-hidden post text (capture-side; its own phase).
- Post-centric CSV symmetry for flaggedPosts/unflaggedPosts.
- Re-add `status` alongside `blocked` if the pending/dismissed distinction is later needed.
