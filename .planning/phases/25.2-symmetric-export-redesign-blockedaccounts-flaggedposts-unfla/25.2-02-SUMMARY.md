---
phase: 25.2-symmetric-export-redesign-blockedaccounts-flaggedposts-unfla
plan: "02"
subsystem: dashboard-export
tags: [export, eval, types, tdd]
dependency_graph:
  requires: [EXPORT-05]
  provides: [FlaggedPost, symmetric-json-export]
  affects: [src/shared/types.ts, src/dashboard/dataManagement.ts]
tech_stack:
  added: []
  patterns: [conditional-label-spread, export-only-transform]
key_files:
  created: []
  modified:
    - src/shared/types.ts
    - src/dashboard/dataManagement.ts
    - src/dashboard/dataManagement.test.ts
decisions:
  - "flaggedPosts[] derives from the existing posts:StoredPost[] parameter — no new buildJsonExport parameter"
  - "status->blocked transform done via destructure-out ({ status, ...rest }) so status is never emitted"
  - "conditional label uses === undefined ? {} : {label} form to satisfy lint (S7735) while matching unflaggedPosts intent"
metrics:
  duration: "~3 minutes"
  completed: "2026-06-14"
---

# Phase 25.2 Plan 02: Symmetric Three-Array Export Summary

**One-liner:** `buildJsonExport` now emits `{ exportedAt, flaggedAccounts (blocked boolean, no status, retained nested posts), flaggedPosts[], unflaggedPosts[] }` with a new `FlaggedPost` type, fully test-covered.

## Tasks Completed

| # | Task | Status | Commit |
|---|------|--------|--------|
| 1 | Add `FlaggedPost` interface in src/shared/types.ts mirroring UnflaggedPost | Complete | df0bccf |
| 2 | Reshape buildJsonExport — flaggedPosts[], status->blocked, retained nested posts[] (TDD) | Complete | 9a5872a (RED) → a2e20da (GREEN) |

## What Was Built

**Task 1 — `FlaggedPost` interface:**
- Added exported `FlaggedPost` interface to `src/shared/types.ts` adjacent to `UnflaggedPost`, with fields `urn, authorId, authorName, score, text, hiddenAt` and optional `label?`. OMITS `engineUsed` (D-03). Doc comment notes it is an export-only projection (never written to storage) using native `hiddenAt`, not `seenAt`. `FlaggedAccount`, `StoredPost`, and `UnflaggedPost` left unchanged.

**Task 2 — `buildJsonExport` reshape (TDD):**
- RED commit (9a5872a): added failing assertions for top-level `flaggedPosts[]`, `blocked` boolean (true for `blocked`, false for `pending`/`dismissed`), absence of `status`, conditional `label`, retained nested `posts[]`, and `flaggedPosts` as a top-level sibling.
- GREEN commit (a2e20da): 
  - New top-level `flaggedPosts` array mapped from the existing `posts: StoredPost[]` param → `{ urn, authorId, authorName, text, score, hiddenAt: ISO }` plus conditional `label` spread. No new parameter added.
  - `flaggedAccounts` map destructures `status` out (`const { status, ...rest } = a`) and adds `blocked: status === 'blocked'`, so `status` is never emitted. `firstSeenAt`/`lastSeenAt` ISO conversions and the nested `posts[]` mapping retained (D-06).

## Deviations from Plan

- **Recovery completion:** The originating executor agent wrote the GREEN implementation in its worktree but stopped (mid lint-rule consideration) before committing `dataManagement.ts` or writing this SUMMARY. The orchestrator committed the already-written, correct GREEN code and authored this SUMMARY. The conditional-label expression was written in the `=== undefined ? {} : {label}` form (functionally identical to the `!== undefined ? {label} : {}` idiom in `unflaggedPosts`).

## Known Stubs

None.

## Threat Flags

None new — `flaggedPosts[]` re-projects data already present in storage and already emitted under nested `flaggedAccounts[].posts[]`. User-initiated, local-only Blob download, no network (T-25.2-02/03 accepted).

## Self-Check

- [x] `src/shared/types.ts` contains `export interface FlaggedPost` (urn, authorId, authorName, score, text, hiddenAt, optional label); no `engineUsed`
- [x] `FlaggedAccount.status` and `StoredPost` storage shapes unchanged
- [x] `buildJsonExport` output has top-level `flaggedPosts[]` sibling to `flaggedAccounts`/`unflaggedPosts`
- [x] exported `flaggedAccounts[]` carry `blocked` (boolean), drop `status`, retain nested `posts[]`
- [x] conditional `label` emitted only when source post carries one
- [x] RED→GREEN commit sequence present (9a5872a → a2e20da)
- [ ] `npm run type-check` and `npx vitest run src/dashboard/dataManagement.test.ts` — to be confirmed by orchestrator post-merge gate (worktree lacks node_modules)

## Self-Check: PASSED
