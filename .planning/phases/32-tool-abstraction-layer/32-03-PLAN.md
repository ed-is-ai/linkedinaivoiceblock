---
phase: 32-tool-abstraction-layer
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - src/background/index.ts
  - src/content/detector/rederiver.ts
  - src/skills/library/dom-selector-registry/SKILL.md
  - src/skills/library/AUTHORING.md
autonomous: true
requirements: [TOOL-02]

must_haves:
  truths:
    - "background/index.ts REDERIVE_SELECTOR handler resolves the tool via ToolRegistry and calls toolRegistry.get('dom-selector-rederive').execute({ target, domSkeleton }) instead of calling rederiveSelector directly (D-04)"
    - "The migrated functions (rederiveSelector, REDERIVE_SYSTEM_PROMPT, RederiveCandidate, RederiveModelOutput, isRederiveModelOutput) are REMOVED from background/index.ts; REDERIVE_SYSTEM_PROMPT + RederiveCandidate are re-imported from the tool (single source of truth, D-08)"
    - "The rate-limit machinery (checkRateLimit/acquireRateLimitLatch/releaseRateLimitLatch), the pre-latch API-key check, and the handler itself STAY in background/index.ts; the success trace is recorded at the background call site from the returned usage (D-06/D-07)"
    - "rederiver.ts imports RederiveCandidate from the tool (no local duplicate definition); LLMRederiver class stays in place (D-08)"
    - "dom-selector-registry/SKILL.md metadata.kind is corrected exclusion → tool (CR-01); the folder is NOT added to skill-order.json tools array"
    - "AUTHORING.md documents the skill-vs-tool decision rule (D-01/D-02), the tool prefix/authoring workflow, and cites fetchComments as the composite-detector seam example; composite detectors are DOCUMENTED as follow-ups, NOT refactored (D-03)"
    - "Zero behavior change: full test suite + check-skill-registry + check-tool-registry pass; golden-score snapshot and exclusion parity byte-identical; ratelimit.test.ts green unmodified (SC#6)"
  artifacts:
    - path: "src/background/index.ts"
      provides: "Rewired REDERIVE_SELECTOR handler using ToolRegistry"
      contains: "dom-selector-rederive"
    - path: "src/skills/library/dom-selector-registry/SKILL.md"
      provides: "Corrected kind: tool manifest"
      contains: "kind: tool"
    - path: "src/skills/library/AUTHORING.md"
      provides: "Skill-vs-tool decision rule + tool authoring workflow"
      contains: "tool"
  key_links:
    - from: "src/background/index.ts"
      to: "src/shared/tool-registry.ts"
      via: "import { get } and toolRegistry.get('dom-selector-rederive').execute"
      pattern: "dom-selector-rederive'\\)\\.execute"
    - from: "src/background/index.ts"
      to: "src/skills/library/dom-selector-rederive/dom-selector-rederive.tool"
      via: "import REDERIVE_SYSTEM_PROMPT + RederiveCandidate"
      pattern: "dom-selector-rederive.tool"
    - from: "src/content/detector/rederiver.ts"
      to: "src/skills/library/dom-selector-rederive/dom-selector-rederive.tool"
      via: "import type RederiveCandidate"
      pattern: "dom-selector-rederive.tool"
---

<objective>
Complete the migration end-to-end and close the zero-behavior-change loop: rewire `background/index.ts` to invoke the tool through `ToolRegistry`, delete the now-relocated functions from background, hoist the success trace to the call site, dedup `RederiveCandidate` in `rederiver.ts`, fix the `dom-selector-registry` kind mislabel (CR-01), and document the skill-vs-tool rule in `AUTHORING.md`. The plan closes with the full byte-identical guard suite.

Depends on Plans 01 (tool file + Tool type) and 02 (ToolRegistry + generated module). This is the only plan that touches `background/index.ts`.

Purpose: TOOL-02 (migrate rederiveSelector into the library with byte-identical behavior; CR-01 kind fix; documented skill-vs-tool audit).
Output: rewired background, deduped rederiver import, corrected dom-selector-registry manifest, extended AUTHORING.md, green guard suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-tool-abstraction-layer/32-CONTEXT.md
@.planning/phases/32-tool-abstraction-layer/32-RESEARCH.md
@.planning/phases/32-tool-abstraction-layer/32-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire background handler to ToolRegistry, remove relocated code, dedup rederiver import</name>
  <files>src/background/index.ts, src/content/detector/rederiver.ts</files>
  <read_first>
    - src/background/index.ts (the file being modified — read the full file: imports L1-30, recordTrace L31-71, REDERIVE_SYSTEM_PROMPT L114-141, REDERIVE_COOLOFF_MS L144 / REDERIVE_DAILY_CAP L146, RederiveCandidate L148-151, RederiveModelOutput L152-154, isRederiveModelOutput L161-172, checkRateLimit L180-222, acquireRateLimitLatch L234-243, releaseRateLimitLatch L247-249, rederiveSelector L258-335, REDERIVE_SELECTOR handler L362-412 including the error-trace recordTrace calls at L374-383 and L398-406)
    - src/content/detector/rederiver.ts (the file being modified — local RederiveCandidate L17-20 to remove; LLMRederiver class L22-52 stays untouched)
    - src/skills/library/dom-selector-rederive/dom-selector-rederive.tool.ts (from Plan 01 — confirm the exports: domSelectorRederiveTool, RederiveCandidate, REDERIVE_SYSTEM_PROMPT, and the execute() return shape `{ candidates, usage }`)
    - src/shared/tool-registry.ts (from Plan 02 — confirm the `get` export name/signature for the import)
    - src/background/ratelimit.test.ts (the guard — read to confirm what the handler's externally observable contract is: rate-limit checks, sendResponse payloads, chrome.storage mutations must stay identical; this test must remain green UNMODIFIED)
    - .planning/phases/32-tool-abstraction-layer/32-PATTERNS.md (sections "src/background/index.ts (MODIFY)", "src/content/detector/rederiver.ts (MODIFY)", "Fire-and-Forget Trace Recording" — exact rewired try-block, import additions, removal list, dedup export-type line)
    - .planning/phases/32-tool-abstraction-layer/32-RESEARCH.md (sections "REDERIVE_SELECTOR handler rewire — exact diff", "The trace-recording seam (D-07)", Pitfall 3 + Pitfall 4 + Open Question 3 on the success-trace userPrompt string)
  </read_first>
  <action>
    In `src/background/index.ts`:
    - Add imports: `import { get as getTool } from '../shared/tool-registry'`; `import type { RederiveCandidate } from '../skills/library/dom-selector-rederive/dom-selector-rederive.tool'`; `import { REDERIVE_SYSTEM_PROMPT } from '../skills/library/dom-selector-rederive/dom-selector-rederive.tool'` (depth from src/background/ is `../skills/library/...`).
    - REMOVE the now-relocated declarations: the local `REDERIVE_SYSTEM_PROMPT` const (L114-141), `interface RederiveCandidate` (L148-151), `interface RederiveModelOutput` (L152-154), `function isRederiveModelOutput` (L161-172), and `async function rederiveSelector` (L258-335). Their identifiers now resolve to the imports.
    - KEEP unchanged (D-06): `REDERIVE_COOLOFF_MS`, `REDERIVE_DAILY_CAP`, `checkRateLimit`, `acquireRateLimitLatch`, `releaseRateLimitLatch`, the pre-latch API-key check, the full `REDERIVE_SELECTOR` handler structure, and the two error-trace `recordTrace` calls (which still reference the now-imported `REDERIVE_SYSTEM_PROMPT`).
    - Rewire the handler try block (per PATTERNS.md "Rewired handler try block"): resolve `getTool<{ target: string; domSkeleton: string }, { candidates: RederiveCandidate[]; usage: AnthropicUsage | undefined }>('dom-selector-rederive')`, `await tool.execute({ target, domSkeleton })`, destructure `{ candidates, usage }`, then call `recordTrace` (success) with `source: 'rederiver'`, the same `model`, `systemPrompt: REDERIVE_SYSTEM_PROMPT`, `userPrompt: \`Target: ${...}\n\nDOM skeleton:\n${...}\``, and the returned `usage`; then `sendResponse({ result: candidates })`. The success trace is the one hoisted out of execute() (D-07). Note (Open Question 3): the hoisted userPrompt uses the original target/domSkeleton without the retry hint — this is observability-only and acceptable.

    In `src/content/detector/rederiver.ts`:
    - REMOVE the local `export interface RederiveCandidate { ... }` (L17-20) and replace with `export type { RederiveCandidate } from '../../skills/library/dom-selector-rederive/dom-selector-rederive.tool'` (depth from src/content/detector/ is `../../skills/library/...`). Leave the `LLMRederiver` class untouched.
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npx vitest run src/background/ratelimit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/background/index.ts` contains `toolRegistry`/`getTool` resolution and the literal call shape `'dom-selector-rederive').execute(` (grep)
    - `src/background/index.ts` no longer contains `async function rederiveSelector` and no longer contains `function isRederiveModelOutput`
    - `src/background/index.ts` imports `REDERIVE_SYSTEM_PROMPT` and `RederiveCandidate` from `.../dom-selector-rederive/dom-selector-rederive.tool`
    - `src/background/index.ts` STILL contains `checkRateLimit`, `acquireRateLimitLatch`, `releaseRateLimitLatch` (rate-limit stays — D-06)
    - `src/background/index.ts` records a success trace at the handler call site (a `recordTrace(` call follows `tool.execute(`)
    - `src/content/detector/rederiver.ts` no longer defines a local `interface RederiveCandidate` and instead imports/re-exports it from the tool
    - `npx vitest run src/background/ratelimit.test.ts` exits 0 with NO modification to that test file (`git diff --exit-code src/background/ratelimit.test.ts`)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>background/index.ts invokes the tool through ToolRegistry with the success trace hoisted to the call site, the relocated code is gone, rate-limit/latch/key-check stay, and RederiveCandidate is deduped — with ratelimit.test.ts green and unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: CR-01 kind fix + AUTHORING.md skill-vs-tool rule + zero-behavior-change guard sweep</name>
  <files>src/skills/library/dom-selector-registry/SKILL.md, src/skills/library/AUTHORING.md</files>
  <read_first>
    - src/skills/library/dom-selector-registry/SKILL.md (the file being modified — line 5 `kind: exclusion`; the body text "NOT wired into any skill array" must stay accurate)
    - src/skills/library/dom-selector-registry/dom-selector-registry.skill.ts (confirm it is a thin re-export from selector-registry — must NOT gain a storageSet call; CLAUDE.md constraint #1 single-writer)
    - src/skills/library/AUTHORING.md (the file being modified — current prefix table L23-28, the `metadata.kind` allowed-values line ~L63, and the skill authoring workflow section to mirror for tools)
    - .planning/phases/32-tool-abstraction-layer/32-PATTERNS.md (section "src/skills/library/AUTHORING.md (MODIFY)" — exact prefix-table row, kind-line edit, the two new sections; and "dom-selector-registry/SKILL.md (MODIFY)" one-line change)
    - .planning/phases/32-tool-abstraction-layer/32-RESEARCH.md (sections "AUTHORING.md Extension", "dom-selector-registry Kind Fix (CR-01)", "Skill-vs-Tool Audit Results" — the audit table + composite-seam documentation, RESEARCH Pitfall 5)
  </read_first>
  <action>
    In `src/skills/library/dom-selector-registry/SKILL.md`: change `kind: exclusion` to `kind: tool` (CR-01). This is the ONLY change to that file. Do NOT add `dom-selector-registry` to `skill-order.json` tools array (RESEARCH Pitfall 5 — it has no execute(); the fix is metadata-only). Do NOT touch `dom-selector-registry.skill.ts`.

    In `src/skills/library/AUTHORING.md` (additive only):
    - Update the `metadata.kind` allowed-values line to list `signal`, `exclusion`, `detector`, `tool`.
    - Add the tool prefix-table row per PATTERNS.md (document that `dom-selector-rederive` is a `tool` and that `dom-selector-registry` was reclassified to `tool` via CR-01).
    - Add a "Skill-vs-Tool Decision Rule (D-01/D-02)" section: the I/O boundary is the discriminator — a skill is host-agnostic/deterministic/pure data→result (no network, no chrome.*, no runtime DOM query); a tool performs host I/O. State that composite detectors (`detect-generic-comments`, `detect-llm`) are composites whose I/O portion IS a tool and scoring portion IS a skill; cite the injected `fetchComments` in `detect-generic-comments` as the canonical "composite already decomposed" example and the `SCORE_POST` fetch in `detect-llm`. Record that actual decomposition of these composites is a DOCUMENTED FOLLOW-UP, not done this phase (D-03).
    - Add a "Tool authoring workflow" section mirroring the skill workflow but with: `SKILL.md` `metadata.kind: tool`, a `<name>.tool.ts` impl file (not `.skill.ts`), add to `skill-order.json` `tools` array, `execute(input: I): Promise<O>` instead of `run(ctx)`, and `Tool<I, O>` imported from `src/shared/skills/types`. End with the regenerate + `npm test && npm run check-tool-registry` step.

    Then run the full zero-behavior-change guard sweep (verify block).
  </action>
  <verify>
    <automated>npm run check-skill-registry &amp;&amp; npm run check-tool-registry &amp;&amp; npm run type-check &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/library/dom-selector-registry/SKILL.md` frontmatter contains `kind: tool` and no longer contains `kind: exclusion`
    - `scripts/skill-order.json` does NOT contain `dom-selector-registry` (CR-01 is metadata-only)
    - `src/skills/library/dom-selector-registry/dom-selector-registry.skill.ts` is unchanged (`git diff --exit-code` on it) — single-writer invariant preserved
    - `src/skills/library/AUTHORING.md` contains a "Skill-vs-Tool" decision-rule section referencing the I/O boundary and citing `fetchComments`, plus a tool authoring workflow mentioning `.tool.ts` and `metadata.kind: tool`
    - AUTHORING.md documents composite-detector decomposition as a follow-up (does NOT instruct refactoring detect-llm/detect-generic-comments)
    - `npm run check-skill-registry` exits 0 and `npm run check-tool-registry` exits 0
    - `npm test` exits 0 (full suite — golden-score snapshot + exclusion parity + ratelimit + generated-registry tests all green)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>The CR-01 kind mislabel is corrected (metadata-only), AUTHORING.md documents the skill-vs-tool rule and tool workflow with composite seams as documented follow-ups, and the full byte-identical guard suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content script → service worker | REDERIVE_SELECTOR message; handler stays in background, unchanged contract |
| service worker → Anthropic API | The fetch now lives in the tool's execute() but is still invoked only from the background handler behind the rate-limit + latch + key-check |
| chrome.storage.local → background | Rate-limit state + API key reads stay in background (stateless SW, CLAUDE.md #4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Information Disclosure | API key handling across the new tool boundary | mitigate | Key read stays inside execute() exactly as before; no new logging; success trace records only `usage` + prompts (no key). ratelimit.test.ts green-unmodified proves the observable contract is unchanged |
| T-32-08 | Denial of Service / cost | Rate-limit cool-off + daily-cap + single-flight latch | mitigate | checkRateLimit/acquireRateLimitLatch/releaseRateLimitLatch + pre-latch key check STAY in background (D-06); acceptance asserts their continued presence; the latch ordering around tool.execute() is preserved |
| T-32-09 | Tampering | LLM response → selector strings (isRederiveModelOutput) | mitigate | Validation moved with the tool in Plan 01 and still runs before candidates return; this plan does not weaken it; full suite green confirms parse behavior unchanged |
| T-32-10 | Elevation of Privilege | dom-selector-registry mis-registered as runnable tool | mitigate | CR-01 fix is SKILL.md-only; folder explicitly NOT added to skill-order.json tools (RESEARCH Pitfall 5); skill.ts unchanged, no execute() introduced |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH §Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npm test` full suite green: golden-score snapshot (heuristic.test.ts) and exclusion parity (exclusions.test.ts) byte-identical; ratelimit.test.ts green and UNMODIFIED (`git diff --exit-code src/background/ratelimit.test.ts`).
- `npm run check-skill-registry` and `npm run check-tool-registry` both exit 0.
- `npm run type-check` exits 0 (REDERIVE_SYSTEM_PROMPT + RederiveCandidate resolve from the tool at every call site; no dangling references — RESEARCH Pitfall 3).
- `dom-selector-registry.skill.ts` and `scripts/skill-order.json` (tools array) unchanged for the CR-01 fix (metadata-only).
</verification>

<success_criteria>
- TOOL-02 SC#3: rederiveSelector + helpers fully migrated; background imports from the new location; self-healing selector flow behaves byte-identically.
- TOOL-02 SC#4: dom-selector-registry kind mislabel corrected to `tool` (CR-01).
- TOOL-02 SC#5: skills audited against the documented skill-vs-tool rule; AUTHORING.md records the rule + composite seams as follow-ups.
- SC#6: zero behavior change — full suite + both stale-checks pass; golden-score + exclusion parity byte-identical.
</success_criteria>

<output>
Create `.planning/phases/32-tool-abstraction-layer/32-03-SUMMARY.md` when done.
</output>
