---
status: secured
phase: 32-tool-abstraction-layer
asvs_level: 1
block_on: high
threats_total: 11
threats_closed: 11
threats_open: 0
audited: 2026-06-16
---

# SECURITY.md — Phase 32: Tool Abstraction Layer

**Audit date:** 2026-06-16
**ASVS Level:** 1
**block_on:** high
**Auditor:** gsd-security-auditor (Claude Sonnet 4.6)

---

## Threat Verification

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| T-32-01 | Tampering | mitigate | CLOSED | `isRederiveModelOutput` present at `dom-selector-rederive.tool.ts:86–97`; validates schema before `parsed.candidates` is returned at line 181; both attempts guard-checked in the retry loop |
| T-32-02 | Information Disclosure | mitigate | CLOSED | `apiKey` used only at `dom-selector-rederive.tool.ts:139` (`'x-api-key': apiKey`); never passed to caller, never logged — `console.warn` at line 184 logs only `lastErr.message`, not the key; return value is `{ candidates, usage }` only |
| T-32-03 | Information Disclosure | accept | CLOSED | Both imports in tool file are `import type` (`dom-selector-rederive.tool.ts:20–21`); no runtime value crosses the shared-module boundary; zero behavior change |
| T-32-04 | Tampering | mitigate | CLOSED | `generated-tool-registry.ts:3` — `DO NOT EDIT` header present; `package.json:12` — `check-tool-registry` script runs `git diff --exit-code src/shared/generated-tool-registry.ts`; `.github/workflows/ci.yml:30` — `npm run check-tool-registry` step present |
| T-32-05 | Elevation of Privilege | mitigate | CLOSED | `tool-registry.ts:39–41` — `_registry` Map built synchronously from `GENERATED_TOOLS` with no `chrome.*` call at init; `tool-registry.ts:57–59` — `get()` throws `[ToolRegistry] Unknown tool: '<name>'` on miss; `generated-tool-registry.test.ts` asserts `GENERATED_TOOLS.map(t => t.name)` toStrictEqual `['dom-selector-rederive']` (order-pinning) |
| T-32-06 | Tampering | mitigate | CLOSED | `generated-tool-registry.ts` — grep for `import(` and `import.meta.glob` returns no matches; static import only at line 10 |
| T-32-07 | Information Disclosure | mitigate | CLOSED | API key read stays inside `execute()` at `dom-selector-rederive.tool.ts:124–125`; success trace at `background/index.ts:262–268` records only `usage` + prompts (no key); WR-01 (retry userPrompt not forwarded) is a documented observability-only deviation — no key/secret in the trace (accepted per plan Open Question 3) |
| T-32-08 | Denial of Service / cost | mitigate | CLOSED | `checkRateLimit` at `background/index.ts:128`, `acquireRateLimitLatch` at line 182, `releaseRateLimitLatch` at line 195 all present; handler calls them in order: checkRateLimit (line 227) → pre-latch key check (line 234) → acquireRateLimitLatch (line 248) → tool.execute (line 258) → releaseRateLimitLatch in finally (line 281) |
| T-32-09 | Tampering | mitigate | CLOSED | `isRederiveModelOutput` at `dom-selector-rederive.tool.ts:86–97` runs at line 176 before `parsed.candidates` is trusted; validation not weakened; relocated verbatim from background |
| T-32-10 | Elevation of Privilege | mitigate | CLOSED | `dom-selector-registry/SKILL.md` has `kind: tool` (CR-01 corrected); `skill-order.json` — `dom-selector-registry` absent from `tools` array (grep: no matches); no `execute()` in the dom-selector-registry folder |
| T-32-SC | Tampering | mitigate | CLOSED | `package.json` — no new entries in `dependencies` or `devDependencies` introduced by Phase 32; zero new packages installed |

---

## Unregistered Flags

None. SUMMARY.md `## Threat Flags` sections for all three plans (32-01, 32-02, 32-03) explicitly state "None" — no new attack surface appeared during implementation without a threat mapping.

---

## Accepted Risks Log

| ID | Threat | Accepted Deviation | Reference |
|----|--------|--------------------|-----------|
| T-32-03 | New shared-module import surface | Types-only import (`import type`) — zero runtime secret exposure | 32-01 PLAN threat_model |
| WR-01 | Success-trace userPrompt on retry | userPrompt in the success trace always reflects the first-attempt bare prompt, not the retry-hint-prefixed prompt actually sent on attempt 2. Observability-only regression; no security impact (no key/secret in the trace). | 32-REVIEW.md WR-01; Plan Open Question 3 |

---

## Informational (Non-Security)

| ID | Finding | Source |
|----|---------|--------|
| WR-02 | HTTP-error branch (`execute()` lines 160–163) lacks a unit test | 32-REVIEW.md WR-02 — test coverage gap, not a security gap |
| IN-01 | `isRederiveModelOutput` accepts empty `candidates[]` (vacuous-truth `every`) | 32-REVIEW.md IN-01 — pre-existing, not introduced by Phase 32 |
| IN-02 | Codegen does not assert that folders in `tools[]` have `kind: tool` | 32-REVIEW.md IN-02 — authoring ergonomics; compile-time catch is adequate |

---

_Generated by gsd-security-auditor. Implementation files are read-only. Gaps must be addressed before shipping._
