---
phase: 32-tool-abstraction-layer
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - scripts/generate-skill-registry.ts
  - scripts/skill-order.json
  - src/shared/generated-tool-registry.ts
  - src/shared/tool-registry.ts
  - src/shared/generated-tool-registry.test.ts
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [TOOL-01]

must_haves:
  truths:
    - "scripts/generate-skill-registry.ts emits a SECOND committed module src/shared/generated-tool-registry.ts (static imports only, MV3-CSP-safe) without changing the existing src/content/generated-skill-registry.ts byte-for-byte (D-05, RESEARCH Pitfall 1)"
    - "skill-order.json has a tools array containing exactly ['dom-selector-rederive']; dom-selector-registry is NOT in it (RESEARCH Pitfall 5)"
    - "ToolRegistry.get('dom-selector-rederive') returns the tool synchronously from a code-seeded Map with no chrome.* call at module init (RESEARCH Pitfall 2)"
    - "check-tool-registry npm script + CI step exist and pass; check-skill-registry remains green"
  artifacts:
    - path: "src/shared/tool-registry.ts"
      provides: "Synchronous code-seeded ToolRegistry.get(name)"
      contains: "export function get<I, O>"
    - path: "src/shared/generated-tool-registry.ts"
      provides: "GENERATED_TOOLS array (codegen output)"
      contains: "export const GENERATED_TOOLS"
    - path: "src/shared/generated-tool-registry.test.ts"
      provides: "Order-pinning + execute drift-guard tests for GENERATED_TOOLS"
    - path: "scripts/skill-order.json"
      provides: "tools bucket"
      contains: "dom-selector-rederive"
  key_links:
    - from: "src/shared/tool-registry.ts"
      to: "src/shared/generated-tool-registry.ts"
      via: "import { GENERATED_TOOLS }"
      pattern: "import \\{ GENERATED_TOOLS"
    - from: "src/shared/generated-tool-registry.ts"
      to: "src/skills/library/dom-selector-rederive/dom-selector-rederive.tool"
      via: "static import domSelectorRederiveTool"
      pattern: "domSelectorRederiveTool"
---

<objective>
Stand up the runtime Tool infrastructure that mirrors the proven SkillRegistry codegen stack: extend `generate-skill-registry.ts` with a `tools` bucket that emits a committed `src/shared/generated-tool-registry.ts`, create the synchronous `ToolRegistry` (`get(name)`) in `src/shared/`, add order-pinning/drift tests, and wire a `check-tool-registry` stale-check into npm + CI.

Depends on Plan 01: the codegen statically imports `domSelectorRederiveTool` from the tool file, and the registry is typed against `Tool<I, O>` — both must already exist.

Purpose: TOOL-01 (first-class ToolRegistry + tools folder convention with codegen + stale-check).
Output: extended codegen, `generated-tool-registry.ts`, `tool-registry.ts`, its test, `check-tool-registry` script + CI step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-tool-abstraction-layer/32-CONTEXT.md
@.planning/phases/32-tool-abstraction-layer/32-RESEARCH.md
@.planning/phases/32-tool-abstraction-layer/32-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend codegen to emit generated-tool-registry.ts + add tools bucket</name>
  <files>scripts/generate-skill-registry.ts, scripts/skill-order.json</files>
  <read_first>
    - scripts/generate-skill-registry.ts (the file being modified — read the full file: `SkillOrder` interface L24-28, `SkillFrontmatter`/`SkillEntry` kind unions L30-41, `validateFrontmatter` allowed-kinds at L63, `importVarName` L115-131, `importPath` L133-135, `main()` L141-248 and the existing skill writeFileSync at L234)
    - scripts/skill-order.json (the config being extended — current shape has signals/exclusions/detectors)
    - src/content/generated-skill-registry.ts (the EXISTING output that MUST stay byte-identical — read its header + structure so the tool output mirrors it without disturbing the skill output path)
    - src/skills/library/dom-selector-rederive/dom-selector-rederive.tool.ts (from Plan 01 — confirm the export const name `domSelectorRederiveTool` the codegen must import)
    - .planning/phases/32-tool-abstraction-layer/32-PATTERNS.md (sections "scripts/generate-skill-registry.ts (MODIFY)", "scripts/skill-order.json (MODIFY)", "src/shared/generated-tool-registry.ts (NEW)" — exact tool branch code for importVarName/importPath, the toolLines/second-writeFileSync structure, and the generated module shape)
    - .planning/phases/32-tool-abstraction-layer/32-RESEARCH.md (section "How ToolRegistry codegen will mirror this" — Option A, generated tool registry location + import path)
  </read_first>
  <action>
    Add `"tools": ["dom-selector-rederive"]` to `scripts/skill-order.json` as a fourth top-level key (do NOT add `dom-selector-registry` — that is metadata-only, handled in Plan 03). Keep signals/exclusions/detectors arrays byte-identical.

    Extend `scripts/generate-skill-registry.ts` in place (Option A — single multi-output script, per RESEARCH recommendation):
    - Add `tools?: string[]` to the `SkillOrder` interface.
    - Extend the `kind` unions in `SkillFrontmatter` and `SkillEntry` and the `validateFrontmatter` allowed-kinds array (L63) to include `'tool'`: `['signal', 'exclusion', 'detector', 'tool']`.
    - Extend `importVarName` to accept the `'tool'` kind and add a `tool` branch BEFORE the prefix-strip logic: for tools, camelCase the FULL folder name (no prefix strip) and append `Tool` — `dom-selector-rederive` → `domSelectorRederiveTool` (RESEARCH §ToolRegistry Design; do NOT strip the `dom-selector-` prefix for tools).
    - Parameterize `importPath` on kind: `kind === 'tool'` → `'../skills/library/${folder}/${folder}.tool'` (relative from src/shared/); all skill kinds keep `'../skills/library/${folder}/${folder}.skill'` (relative from src/content/). Update existing call sites accordingly.
    - In `main()`, after the detector parse, parse the tool entries: `(order.tools ?? []).map(parseSkillMd).filter(...)`.
    - Build the tool output into a SEPARATE `toolLines: string[]` array (DO-NOT-EDIT header citing `check-tool-registry`, `import type { Tool } from './skills/types'`, static imports for each tool, `export const GENERATED_TOOLS: readonly Tool<unknown, unknown>[] = [...]`, and a `GENERATED_TOOL_METADATA` object) and write it with a SECOND `fs.writeFileSync` to `path.join(repoRoot, 'src', 'shared', 'generated-tool-registry.ts')`.
    - CRITICAL (RESEARCH Pitfall 1): do NOT alter the existing `lines.push(...)` calls or the existing skill `writeFileSync` to `src/content/generated-skill-registry.ts`. The skill output must regenerate byte-identical.

    Run `npm run generate-skill-registry` to emit `src/shared/generated-tool-registry.ts`, then `git diff --exit-code src/content/generated-skill-registry.ts` to prove the existing skill output is unchanged.
  </action>
  <verify>
    <automated>npm run generate-skill-registry &amp;&amp; git diff --exit-code src/content/generated-skill-registry.ts &amp;&amp; npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/skill-order.json` contains `dom-selector-rederive` inside a `tools` array and does NOT contain `dom-selector-registry`
    - `scripts/generate-skill-registry.ts` allowed-kinds includes `'tool'` (grep: `'detector', 'tool'`)
    - Running `npm run generate-skill-registry` creates/updates `src/shared/generated-tool-registry.ts` containing `export const GENERATED_TOOLS` and a static `import { domSelectorRederiveTool } from '../skills/library/dom-selector-rederive/dom-selector-rederive.tool'`
    - `src/shared/generated-tool-registry.ts` contains NO `import(` dynamic-import call and NO `import.meta.glob` (MV3-CSP-safe)
    - `git diff --exit-code src/content/generated-skill-registry.ts` exits 0 (existing skill output byte-identical)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Codegen emits a committed, MV3-CSP-safe `generated-tool-registry.ts` with `GENERATED_TOOLS = [domSelectorRederiveTool]` while leaving the existing skill registry output byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ToolRegistry + order/drift tests + check-tool-registry script + CI step</name>
  <files>src/shared/tool-registry.ts, src/shared/generated-tool-registry.test.ts, package.json, .github/workflows/ci.yml</files>
  <read_first>
    - src/content/skill-registry.ts (analog — the code-seed / sync-getter portion ~L46-76; ToolRegistry is the simpler subset: no chrome.storage hydration, no migrate(), no onChanged)
    - src/content/generated-skill-registry.test.ts (analog — order-pinning + kind-drift describe blocks, toStrictEqual on .map(...))
    - src/shared/generated-tool-registry.ts (from Task 1 — confirm GENERATED_TOOLS export name and the tool `name` field)
    - package.json (the file being modified — the `check-skill-registry` script line is the pattern to mirror)
    - .github/workflows/ci.yml (the file being modified — the existing "Stale-check generated skill registry" step is the pattern to mirror)
    - .planning/phases/32-tool-abstraction-layer/32-PATTERNS.md (sections "src/shared/tool-registry.ts (NEW)", "src/shared/generated-tool-registry.test.ts (NEW)", "npm Script Pattern (stale-check)")
    - .planning/phases/32-tool-abstraction-layer/32-RESEARCH.md (sections "ToolRegistry Design" + Pitfall 2 — synchronous singleton, no chrome.* at module init)
  </read_first>
  <behavior>
    - `get('dom-selector-rederive')` returns an object whose `execute` is a function (typeof === 'function').
    - `get('does-not-exist')` throws an Error whose message contains `Unknown tool`.
    - Importing `src/shared/tool-registry.ts` performs NO chrome.* access (the Map is built synchronously from GENERATED_TOOLS) — module import succeeds with chrome globals absent.
    - `GENERATED_TOOLS.map(t => t.name)` toStrictEqual `['dom-selector-rederive']` (order-pinned to skill-order.json `tools`).
    - Every entry in `GENERATED_TOOLS` satisfies `typeof t.execute === 'function'` (drift-guard).
  </behavior>
  <action>
    Create `src/shared/tool-registry.ts` mirroring the skill-registry code-seed pattern, minimal surface: `import { GENERATED_TOOLS } from './generated-tool-registry'` and `import type { Tool } from './skills/types'`; build a module-level `const _registry: Map<string, Tool<any, any>> = new Map(GENERATED_TOOLS.map(t => [t.name, t]))` (synchronous, no chrome.* — RESEARCH Pitfall 2); export `function get<I, O>(name: string): Tool<I, O>` that throws `Error("[ToolRegistry] Unknown tool: '<name>'")` on miss and returns the cast tool otherwise. No `list()`/`has()`/`register()` this phase. No `chrome.storage` hydration, no `migrate()`, no `onChanged` listener (D-05, code-seeded only).

    Create `src/shared/generated-tool-registry.test.ts` mirroring the skill registry test: a describe block for order-pinning (`GENERATED_TOOLS.map(t => t.name)` toStrictEqual `['dom-selector-rederive']`) and a describe block for the execute drift-guard (`typeof t.execute === 'function'` for every tool). Note: `Tool` objects have no `kind` field — assert on `execute`, not `kind`.

    Add to `package.json` scripts: `"check-tool-registry": "npm run generate-skill-registry && git diff --exit-code src/shared/generated-tool-registry.ts"`. Do NOT modify the existing `check-skill-registry` or `prebuild` scripts.

    Add a CI step to `.github/workflows/ci.yml` immediately AFTER the existing "Stale-check generated skill registry" step, named e.g. "Stale-check generated tool registry (TOOL-01)", running `npm run check-tool-registry`. Keep the existing steps unchanged.
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npx vitest run src/shared/generated-tool-registry.test.ts &amp;&amp; npm run check-tool-registry &amp;&amp; npm run check-skill-registry</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/tool-registry.ts` contains `export function get<I, O>` and `import { GENERATED_TOOLS }`, and contains NO `chrome.` reference
    - `src/shared/generated-tool-registry.test.ts` asserts `GENERATED_TOOLS.map(t => t.name)` toStrictEqual `['dom-selector-rederive']` and `typeof t.execute === 'function'`
    - `package.json` contains `"check-tool-registry"` whose command diffs `src/shared/generated-tool-registry.ts`
    - `.github/workflows/ci.yml` contains `npm run check-tool-registry`
    - `npx vitest run src/shared/generated-tool-registry.test.ts` exits 0
    - `npm run check-tool-registry` exits 0 and `npm run check-skill-registry` exits 0
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>A synchronous code-seeded `ToolRegistry.get(name)` exists in `src/shared/`, backed by `GENERATED_TOOLS`, with order/drift tests and a green `check-tool-registry` stale-check wired into npm and CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time codegen → committed source | Codegen writes a committed module that ships into the bundle; tampering here changes runtime imports |
| generated-tool-registry → tool impl | Static import graph; only code-seeded entries are reachable at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | generated-tool-registry.ts drift / hand-edit | mitigate | `check-tool-registry` stale-check in npm + CI (git diff --exit-code) blocks any committed module that diverges from codegen output; DO-NOT-EDIT header present |
| T-32-05 | Elevation of Privilege | Tool name collision / registry poisoning | mitigate | Registry is code-seeded only (no chrome.storage hydration, D-05); `get()` throws on unknown name; order-pinning test asserts exact GENERATED_TOOLS contents |
| T-32-06 | Tampering | Dynamic import / import.meta.glob in generated module | mitigate | Static-imports-only enforced; acceptance criterion greps that the generated module contains no `import(` or `import.meta.glob` (MV3-CSP-safe, Phase 30 D-07) |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH §Package Legitimacy Audit: zero new deps) |
</threat_model>

<verification>
- `npm run generate-skill-registry` leaves `src/content/generated-skill-registry.ts` byte-identical (RESEARCH Pitfall 1 guard).
- `npm run check-tool-registry` and `npm run check-skill-registry` both exit 0.
- `npx vitest run src/shared/generated-tool-registry.test.ts` exits 0.
- `src/shared/tool-registry.ts` and `src/shared/generated-tool-registry.ts` contain no dynamic import / import.meta.glob / chrome.* at init.
</verification>

<success_criteria>
- TOOL-01: first-class ToolRegistry stood up via the SkillRegistry codegen mirror — codegen emits committed `generated-tool-registry.ts`, `ToolRegistry.get()` resolves it synchronously, stale-checked in CI; existing skill registry output unchanged.
</success_criteria>

<output>
Create `.planning/phases/32-tool-abstraction-layer/32-02-SUMMARY.md` when done.
</output>
