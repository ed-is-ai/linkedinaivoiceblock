---
phase: 24-trace-capture-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/shared/pricing.ts
  - src/shared/pricing.test.ts
  - src/shared/traceStore.ts
  - src/shared/traceStore.test.ts
autonomous: true
requirements: [TRACE-01, TRACE-02, TRACE-03]

must_haves:
  truths:
    - "A TraceEntry type exists capturing model, full systemPrompt, 500-char userPrompt, the 4-bucket token split, costUsd, ISO timestamp, source, and optional error/unpriced"
    - "computeCostUsd applies the cache-aware formula (input + cacheCreation×1.25 + cacheRead×0.10 + output) using MODEL_PRICING rates"
    - "An unknown model returns costUsd 0 and unpriced true (no wrong number emitted)"
    - "appendTrace prepends newest-first and evicts the oldest once the array exceeds 500, leaving exactly 500"
    - "MODEL_PRICING is a code constant (Sonnet 4.6 $3/$15, Haiku 4.5 $1/$5) usable as a refresh-on-load seed"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "TraceEntry interface + llbTraces/llbModelPricing keys on StorageSchema + ModelPricing type"
      contains: "interface TraceEntry"
    - path: "src/shared/pricing.ts"
      provides: "MODEL_PRICING constant + computeCostUsd cache-aware cost function"
      exports: ["MODEL_PRICING", "computeCostUsd"]
    - path: "src/shared/traceStore.ts"
      provides: "appendTrace FIFO-capped writer (cap 500) over llbTraces"
      exports: ["appendTrace", "TRACE_STORE_CAP"]
    - path: "src/shared/pricing.test.ts"
      provides: "Cost-formula + unknown-model unit tests"
    - path: "src/shared/traceStore.test.ts"
      provides: "FIFO cap + prepend-order unit tests with in-memory chrome.storage mock"
  key_links:
    - from: "src/shared/traceStore.ts"
      to: "src/shared/storage.ts"
      via: "storageGet/storageSet on llbTraces"
      pattern: "storage(Get|Set)"
    - from: "src/shared/pricing.ts"
      to: "src/shared/types.ts"
      via: "ModelPricing type import"
      pattern: "ModelPricing"
---

<objective>
Build the pure, unit-testable foundation of the trace system: the `TraceEntry` schema (D-04),
the cache-aware cost model (`pricing.ts`, D-05/D-06/D-08), and the FIFO-capped trace writer
(`traceStore.ts`, D-09). No service-worker wiring here — that is Plan 02. This plan defines the
contracts Plan 02 consumes.

Purpose: Isolate the deterministic logic (schema, arithmetic, cap) so it is fully tested
without mocking the Anthropic fetch. Plan 02 then only has to call `appendTrace` with values
read off the live `usage` object.
Output: `TraceEntry`/`ModelPricing` types + `llbTraces`/`llbModelPricing` storage keys;
`MODEL_PRICING` constant + `computeCostUsd`; `appendTrace` + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-trace-capture-storage/24-CONTEXT.md
@.planning/phases/24-trace-capture-storage/24-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TraceEntry + ModelPricing types and storage keys to types.ts</name>
  <files>src/shared/types.ts</files>
  <read_first>
    - src/shared/types.ts — `StoredPost` interface (~L166-179) is the flat-field structural analog; `StorageSchema` (~L275-307) is where new optional keys go, following the `storedPosts` JSDoc + `llbRederive*` naming style.
    - .planning/phases/24-trace-capture-storage/24-CONTEXT.md — D-04 (full field list), D-08 (unpriced flag).
    - .planning/phases/24-trace-capture-storage/24-PATTERNS.md — types.ts section for the exact field shapes.
  </read_first>
  <action>
    Add an exported `TraceEntry` interface with these fields, in this order: `model: string`;
    `systemPrompt: string` (FULL text, NOT truncated — TRACE-01 lists "system prompt" with no
    truncation, per D-04); `userPrompt: string` (truncated to 500 chars by the caller in Plan 02 —
    document the 500-char bound in the JSDoc but do NOT truncate here); `inputTokens: number`;
    `outputTokens: number`; `cacheCreationTokens: number`; `cacheReadTokens: number`;
    `costUsd: number`; `timestamp: string` (ISO 8601); `source: 'detector' | 'rederiver'`;
    `error?: string` (present only on failed attempts, D-03); `unpriced?: boolean` (true when the
    model is unknown, D-08). Keep it flat — no nested objects (StoredPost analog).
    Add an exported `ModelPricing` type: an index signature `{ [model: string]: { inputPerMTok: number; outputPerMTok: number } | undefined }`.
    Add two optional keys to `StorageSchema` following the `storedPosts` JSDoc style and the
    `llb` prefix convention used by the `llbRederive*` keys: `llbTraces?: TraceEntry[]`
    (JSDoc: newest-first, capped at 500 per TRACE-03, SW writes / Phase 25 dashboard reads) and
    `llbModelPricing?: ModelPricing` (JSDoc: cache-aware pricing table, SW OVERWRITES from the
    MODEL_PRICING constant on every load per D-06 — explicitly the opposite of SelectorRegistry's
    preserve-stored semantics).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/types.ts` contains `export interface TraceEntry`.
    - TraceEntry contains all of: `model`, `systemPrompt`, `userPrompt`, `inputTokens`, `outputTokens`, `cacheCreationTokens`, `cacheReadTokens`, `costUsd`, `timestamp`, `source`, `error?`, `unpriced?`.
    - `source` field type is exactly `'detector' | 'rederiver'`.
    - StorageSchema contains `llbTraces?: TraceEntry[]` and `llbModelPricing?: ModelPricing`.
    - `export type ModelPricing` (or `export interface ModelPricing`) is present.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>TraceEntry, ModelPricing, and the two storage keys compile cleanly under strict mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pricing.ts — MODEL_PRICING constant + cache-aware computeCostUsd</name>
  <files>src/shared/pricing.ts, src/shared/pricing.test.ts</files>
  <read_first>
    - src/shared/postStore.ts (L19-23) — the named-constant + JSDoc documentation style to mirror for `MODEL_PRICING`.
    - .planning/phases/24-trace-capture-storage/24-CONTEXT.md — D-05 (formula + multipliers), D-06 (pricing values + "priced 2026-06"), D-08 (unknown model), and the Canonical References pricing table.
    - .planning/phases/24-trace-capture-storage/24-PATTERNS.md — pricing.ts section for the constant shape and the computeCostUsd signature.
    - src/dashboard/dataManagement.test.ts (L1-45) — the vitest import + fixture-factory style used in this codebase.
  </read_first>
  <behavior>
    - Sonnet detector call: usage {input_tokens 1000, output_tokens 500, cache 0/0} on `claude-sonnet-4-6` → costUsd = (1000×3 + 500×15)/1e6 = 0.0105, unpriced false.
    - Cache-aware call: {input 100, cache_creation 1000, cache_read 2000, output 50} on `claude-sonnet-4-6` → costUsd = (100×3 + 1000×3×1.25 + 2000×3×0.10 + 50×15)/1e6 = (300 + 3750 + 600 + 750)/1e6 = 0.00540, unpriced false.
    - Haiku rederiver: {input 200, output 100, cache 0/0} on `claude-haiku-4-5-20251001` → costUsd = (200×1 + 100×5)/1e6 = 0.0007, unpriced false.
    - Unknown model `claude-foo`: any usage → costUsd 0, unpriced true (D-08).
    - Missing cache fields (undefined) are treated as 0.
  </behavior>
  <action>
    Create `src/shared/pricing.ts`. Export `MODEL_PRICING: ModelPricing` (import the type from
    `./types`) with exactly two entries, with a JSDoc noting prices are as published 2026-06 and
    that this constant is the source of truth re-seeded into storage on every load (D-06):
    `'claude-sonnet-4-6'` → `{ inputPerMTok: 3.00, outputPerMTok: 15.00 }`,
    `'claude-haiku-4-5-20251001'` → `{ inputPerMTok: 1.00, outputPerMTok: 5.00 }`.
    Export `computeCostUsd(model: string, usage: { input_tokens: number; output_tokens: number; cache_creation_input_tokens?: number; cache_read_input_tokens?: number }): { costUsd: number; unpriced: boolean }`.
    Look up `MODEL_PRICING[model]`; if absent return `{ costUsd: 0, unpriced: true }` (D-08).
    Otherwise apply the cache-aware formula from D-05: `costUsd = (input_tokens × inputPerMTok + (cache_creation_input_tokens ?? 0) × inputPerMTok × 1.25 + (cache_read_input_tokens ?? 0) × inputPerMTok × 0.10 + output_tokens × outputPerMTok) / 1_000_000`, returning `{ costUsd, unpriced: false }`.
    Use the named multiplier constants 1.25 (5-min cache write premium) and 0.10 (cache read) with explanatory comments. Do NOT round costUsd.
    Write `src/shared/pricing.test.ts` covering every case in <behavior>; assert costUsd with `toBeCloseTo` and unpriced with `toBe`.
  </action>
  <verify>
    <automated>npx vitest run src/shared/pricing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/pricing.ts` exports `MODEL_PRICING` and `computeCostUsd`.
    - `MODEL_PRICING['claude-sonnet-4-6']` equals `{ inputPerMTok: 3, outputPerMTok: 15 }` and `MODEL_PRICING['claude-haiku-4-5-20251001']` equals `{ inputPerMTok: 1, outputPerMTok: 5 }`.
    - `computeCostUsd('claude-foo', anyUsage)` returns `{ costUsd: 0, unpriced: true }`.
    - The cache-aware behavior case computes 0.00540 (within toBeCloseTo tolerance).
    - `npx vitest run src/shared/pricing.test.ts` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>computeCostUsd reproduces the D-05 formula for both models, treats missing cache fields as 0, and flags unknown models; all pricing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: traceStore.ts — FIFO-capped appendTrace (cap 500)</name>
  <files>src/shared/traceStore.ts, src/shared/traceStore.test.ts</files>
  <read_first>
    - src/shared/postStore.ts (full file) — the exact read → mutate → cap → write idiom to copy verbatim, substituting llbTraces/TraceEntry/cap 500 and DROPPING the dedup + text-truncation lines.
    - src/shared/storage.ts (L25-39) — `storageGet`/`storageSet` typed wrappers; never call chrome.storage.local directly.
    - .planning/phases/24-trace-capture-storage/24-CONTEXT.md — D-09 (FIFO cap 500, newest-first, no dedup).
    - .planning/phases/24-trace-capture-storage/24-PATTERNS.md — traceStore.ts section ("copies postStore structure verbatim") and the "FIFO cap — prepend + pop" shared pattern (do NOT use slice).
    - src/background/ratelimit.test.ts (L13-69) — the in-memory `store` + `makeChrome()` mock pattern for chrome.storage.local in vitest; reuse this mock shape.
  </read_first>
  <action>
    Create `src/shared/traceStore.ts`. Import `storageGet`/`storageSet` from `./storage` and
    `TraceEntry` from `./types`. Export `const TRACE_STORE_CAP = 500;` with a JSDoc citing TRACE-03.
    Export `async function appendTrace(entry: TraceEntry): Promise<void>`: read the current
    `llbTraces` (default `[]`) via `storageGet(['llbTraces'])`; prepend the new entry
    (`[entry, ...existing]`); if `updated.length > TRACE_STORE_CAP` call `updated.pop()` to evict
    the oldest (the prepend + pop idiom from postStore — do NOT use slice); write back via
    `storageSet({ llbTraces: updated })`. Unlike postStore, do NOT dedup and do NOT truncate text
    (every call gets an entry per D-03; userPrompt is already truncated by the Plan 02 caller).
    Write `src/shared/traceStore.test.ts` using an in-memory chrome.storage mock (copy the
    `store` + `makeChrome()` pattern from ratelimit.test.ts; assign to `globalThis.chrome` in
    beforeEach and reset `store`). Cases: (a) appending to an empty store yields a 1-element
    `llbTraces`; (b) the newest entry is at index 0 (prepend order); (c) after 501 appends the
    array length is exactly 500 and the very first-appended entry has been evicted (assert by a
    unique marker field such as a distinct `timestamp` or `model` on entry #1).
  </action>
  <verify>
    <automated>npx vitest run src/shared/traceStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/traceStore.ts` exports `appendTrace` and `TRACE_STORE_CAP` (=== 500).
    - `traceStore.ts` imports `storageGet`/`storageSet` from `./storage` and never references `chrome.storage.local` directly.
    - `traceStore.ts` contains no dedup check (no `.some(` URN guard) and no `.slice(` on the entry array (uses `.pop()`).
    - The 501-append test asserts final `llbTraces.length === 500` and that the first-appended entry is gone.
    - `npx vitest run src/shared/traceStore.test.ts` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>appendTrace prepends newest-first and evicts oldest at >500 via pop(); the FIFO test proves exactly 500 remain after 501 appends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic `usage` object → traceStore | Numeric token counts only; no secret material crosses here in this plan |
| TraceEntry → chrome.storage.local | Local-only persistence; nothing transmitted in Phase 24 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | TraceEntry schema (types.ts) | mitigate | Schema has NO `apiKey`/`x-api-key`/auth field — the API key is structurally un-storable in a trace. Acceptance criteria enumerate the exact allowed fields; no key field exists. |
| T-24-02 | Information Disclosure | `userPrompt` field | mitigate | Detector userPrompt is post text (PII-ish), bounded to 500 chars by the Plan 02 caller; documented bound in TraceEntry JSDoc. Consistent with the existing opt-in storedPosts persistence. Local-only, never transmitted this phase. |
| T-24-03 | Tampering | npm/pip/cargo installs | accept | No new dependencies added in this plan (uses existing vitest + storage wrappers). No install task → no slopcheck surface. |

(No package installs in this plan; package legitimacy gate not triggered.)
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (whole project compiles with the new types).
- `npx vitest run src/shared/pricing.test.ts src/shared/traceStore.test.ts` exits 0.
- `grep` of TraceEntry field list shows no `apiKey`/`anthropicApiKey`/`x-api-key` field (T-24-01).
- `MODEL_PRICING` contains exactly the two in-use models with the D-06 values.
</verification>

<success_criteria>
- TraceEntry captures every D-04 field with `source: 'detector' | 'rederiver'`; the API key is structurally absent.
- computeCostUsd reproduces the D-05 cache-aware formula and flags unknown models (D-08).
- appendTrace caps at exactly 500 via prepend + pop (D-09, TRACE-03).
- All three artifacts compile and their tests pass; the project tsc is clean.
</success_criteria>

<output>
Create `.planning/phases/24-trace-capture-storage/24-01-SUMMARY.md` when done.
</output>
