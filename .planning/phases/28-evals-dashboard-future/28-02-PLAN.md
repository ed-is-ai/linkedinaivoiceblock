---
phase: 28-evals-dashboard-future
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/dashboard/evals.html
  - src/dashboard/evals.tsx
  - src/manifest.json
  - vite.config.ts
  - src/popup/index.tsx
  - src/dashboard/index.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "evals.html is a standalone Vite/Preact page, not a tab on dashboard.html (D-01)"
    - "The Evals page is reachable via entry links from BOTH the popup and the dashboard (D-02)"
    - "`npm run build` emits dashboard/evals.html into dist and it is web-accessible (D-01, D-02)"
    - "The page reads storedPosts/unflaggedPosts/evalRuns from chrome.storage.local on mount"
    - "Clicking AI/Human on a post row writes the label through to storage via setPostLabel (D-08)"
    - "The bulk-seed button calls bulkSeedLabels and never overwrites a manual label (D-09)"
  artifacts:
    - path: "src/dashboard/evals.html"
      provides: "HTML entry for the Evals page"
      contains: "evals.tsx"
    - path: "src/dashboard/evals.tsx"
      provides: "Preact Evals console page shell + labeling UI"
      min_lines: 80
    - path: "src/manifest.json"
      provides: "web_accessible_resources entry for dashboard/evals.html"
      contains: "evals.html"
  key_links:
    - from: "src/popup/index.tsx"
      to: "dashboard/evals.html"
      via: "chrome.runtime.getURL + window.open"
      pattern: "getURL\\('dashboard/evals.html'\\)"
    - from: "src/dashboard/index.tsx"
      to: "dashboard/evals.html"
      via: "chrome.runtime.getURL + window.open"
      pattern: "getURL\\('dashboard/evals.html'\\)"
    - from: "src/dashboard/evals.tsx"
      to: "setPostLabel / bulkSeedLabels"
      via: "import from ../shared/postStore"
      pattern: "setPostLabel|bulkSeedLabels"
---

<objective>
Ship the standalone Evals page shell, its build wiring, the navigation links from popup + dashboard,
and the click-to-label + bulk-seed UI. This plan delivers a reachable, buildable, labeling-capable
page; the scoring/metrics engine lands in Plan 03.

Purpose: Stand up the page surface and labeling UX (D-01, D-02, D-08, D-09) so Plan 03 can focus purely
on the run loop and results rendering inside an already-mounted, already-wired page.
Output: New evals.html + evals.tsx; manifest/vite entry registration; popup + dashboard "Open Evals"
links; labeling rows + bulk button wired to the Plan 01 storage functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-evals-dashboard-future/28-CONTEXT.md
@.planning/phases/28-evals-dashboard-future/28-PATTERNS.md
@.planning/phases/28-evals-dashboard-future/28-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build entry — evals.html + manifest + vite registration</name>
  <files>src/dashboard/evals.html, src/dashboard/evals.tsx, src/manifest.json, vite.config.ts</files>
  <read_first>
    - src/dashboard/index.html (12-line shape to copy verbatim — change title + script src)
    - src/popup/index.html (HTML entry precedent)
    - src/manifest.json (options_ui §34-37; permissions; host_permissions — note there is currently NO web_accessible_resources)
    - vite.config.ts (whole file — webExtension plugin config)
    - .planning/phases/28-evals-dashboard-future/28-PATTERNS.md (evals.html + manifest/vite section, lines 176-220)
  </read_first>
  <action>
    Create src/dashboard/evals.html by copying src/dashboard/index.html verbatim, changing the title to
    "LinkedIn Blocker — Evals" and the module script src to "./evals.tsx". Create a minimal placeholder
    src/dashboard/evals.tsx for now that mounts an empty App into #root (the full page lands in Task 2)
    so the build has a real entry — `render(<App/>, document.getElementById('root')!)`. Register the new
    page so vite-plugin-web-extension builds and ships it AND it is loadable via chrome.runtime.getURL
    (D-02 — it is NOT a second options_ui slot). Add a `web_accessible_resources` array to
    src/manifest.json (additive — none exists today): `[{ "resources": ["dashboard/evals.html"],
    "matches": ["https://www.linkedin.com/*"] }]`. Then run `npm run build` and inspect dist/ — if
    dashboard/evals.html is NOT emitted, the installed vite-plugin-web-extension does not auto-discover
    web_accessible_resources HTML; in that case declare the page via the plugin's `additionalInputs`
    option in vite.config.ts (this is the one build-wiring choice left to Claude's Discretion per D-01).
    Verify which mechanism the installed plugin version requires by checking the build output, not by
    guessing. Do NOT add the page to options_ui.
  </action>
  <verify>
    <automated>npm run build && test -f dist/dashboard/evals.html && echo EVALS_HTML_EMITTED</automated>
  </verify>
  <acceptance_criteria>
    - src/dashboard/evals.html exists, titled "LinkedIn Blocker — Evals", script src "./evals.tsx".
    - src/manifest.json contains a web_accessible_resources entry listing "dashboard/evals.html".
    - `npm run build` exits 0 and dist/dashboard/evals.html exists (prints EVALS_HTML_EMITTED).
    - evals.html is NOT registered under options_ui.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The Evals page is a standalone build entry, shipped and web-accessible, ready to be opened by getURL links.</done>
</task>

<task type="auto">
  <name>Task 2: Page shell + storage read + nav links</name>
  <files>src/dashboard/evals.tsx, src/popup/index.tsx, src/dashboard/index.tsx</files>
  <read_first>
    - src/dashboard/index.tsx (page skeleton: imports/mount §1-7,359; storage-read useEffect §87-103; styles object `const s` §307-357; load-error pattern)
    - src/popup/index.tsx (openDashboard §114-116; the dashboard link button §157; dashboardLink style §386)
    - .planning/phases/27-eval-improvements/mockups/evals-option-a-console.html (the LOCKED visual contract — layout/sections/palette to render)
    - src/shared/eval/index.ts (barrel — types to import: EvalRun, PostDetail, ThresholdRow)
    - .planning/phases/28-evals-dashboard-future/28-PATTERNS.md (evals.tsx section §91-133; nav-link section §224-237)
    - CLAUDE.md (no CSS class names — inline style objects only)
  </read_first>
  <action>
    Flesh out src/dashboard/evals.tsx into the page shell. Copy the storage-read `useEffect` idiom from
    dashboard/index.tsx:87-103: single `chrome.storage.local.get(['storedPosts','unflaggedPosts',
    'evalRuns'])` → setState (posts, unflagged, runs), `.catch` → friendly loadError. Use ONE typed
    `const s: Record<string, JSX.CSSProperties>` inline-style object (no CSS files / class names —
    CLAUDE.md). Render the static layout sections from the locked mockup evals-option-a-console.html:
    page header, run-controls card (dataset summary + engine toggle heuristic/LLM — wire the run button
    in Plan 03), an empty 4-metric grid placeholder, a results placeholder, and the labeling section.
    Reuse the established palette (primary #0a66c2, destructive #dc2626, muted #6b7280/#9ca3af, borders
    #e5e7eb/#d1d5db). Add nav links: in src/popup/index.tsx add an `openEvals()` that calls
    `window.open(chrome.runtime.getURL('dashboard/evals.html'), '_blank', 'noreferrer')` plus a sibling
    button (style styles.dashboardLink) next to the existing "View Dashboard" button (D-02); in
    src/dashboard/index.tsx add the same getURL('dashboard/evals.html') window.open button using the
    existing s.actionBtn style inside the Data-management card or a header link (D-02). Handle empty /
    first-run state (no posts → friendly "no labeled posts yet" copy — Claude's Discretion).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/dashboard/evals.tsx reads storedPosts, unflaggedPosts, evalRuns on mount and stores them in state with a load-error fallback.
    - All styling is inline style objects (source assertion: no `class=`/`className=` in evals.tsx; no new .css import).
    - src/popup/index.tsx contains `getURL('dashboard/evals.html')` reachable from a visible button.
    - src/dashboard/index.tsx contains `getURL('dashboard/evals.html')` reachable from a visible button.
    - Page renders the mockup's section structure (run controls, metric grid, results, labeling).
    - `npx tsc --noEmit` and `npm run build` both exit 0.
  </acceptance_criteria>
  <done>The Evals page mounts, loads storage data, matches the locked mockup's layout shell, and is reachable from both popup and dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Click-to-label rows + bulk-seed button wired to storage</name>
  <files>src/dashboard/evals.tsx, src/dashboard/evals.test.ts</files>
  <read_first>
    - src/dashboard/evals.tsx (the page shell from Task 2 — the labeling section to fill)
    - src/shared/postStore.ts (setPostLabel + bulkSeedLabels from Plan 01 — the write functions to call)
    - src/shared/types.ts (StoredPost.label?, UnflaggedPost.label?)
    - .planning/phases/27-eval-improvements/mockups/evals-option-a-console.html (labeling rows + bulk button visual contract)
    - .planning/phases/28-evals-dashboard-future/28-CONTEXT.md (D-08 single click-to-label; D-09 idempotent bulk)
  </read_first>
  <behavior>
    - Clicking "AI" on a post row calls setPostLabel(urn, 'ai') and the row reflects the new label after the write resolves.
    - Clicking "Human" on a post row calls setPostLabel(urn, 'human') and the row reflects it.
    - The bulk-seed button calls bulkSeedLabels() and after it resolves, previously-unlabeled posts show seeded labels while previously-manually-labeled posts are unchanged.
    - The labeled-count / dataset summary updates to reflect labels currently in state.
  </behavior>
  <action>
    Fill the labeling section of src/dashboard/evals.tsx. Render a row per post (merge storedPosts +
    unflaggedPosts) showing a text preview, current label (if any), and two buttons AI / Human. AI
    button → `await setPostLabel(urn, 'ai')` then update local state for that urn so the row reflects the
    label without a full reload; Human button → `await setPostLabel(urn, 'human')` similarly (D-08 —
    this page is the sanctioned label writer). Add a bulk-seed button labeled per the mockup that calls
    `await bulkSeedLabels()` then re-reads storedPosts/unflaggedPosts into state (D-09 — server-side
    idempotency already guarantees manual labels survive; the UI just reflects the result). Show a
    dataset summary line (e.g. "N labeled of M posts") derived from current state. Use the inline-style
    palette from Task 2. Extract any pure helper used for the summary count (e.g. countLabeled(posts)) so
    it is unit-testable, and add a colocated test src/dashboard/evals.test.ts covering: AI/Human click
    invokes setPostLabel with the right args (mock the module), and the labeled-count helper. Component
    interaction can be tested via @testing-library/preact if already a dependency; otherwise unit-test the
    handlers/helpers directly.
  </action>
  <verify>
    <automated>npx vitest run src/dashboard/evals.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - evals.tsx renders one row per post with AI and Human buttons and a current-label indicator.
    - AI click calls setPostLabel(urn,'ai'); Human click calls setPostLabel(urn,'human') (asserted with a mocked postStore).
    - Bulk-seed button calls bulkSeedLabels and re-reads storage into state.
    - A pure labeled-count helper exists and is unit-tested.
    - `npx vitest run src/dashboard/evals.test.ts` passes; `npx tsc --noEmit` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>Posts can be labeled by click and bulk-seeded from the page, writing through to storage via the Plan 01 functions, with the UI reflecting the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page/UI → chrome.storage.local | Labeling clicks mutate persisted post data |
| extension page surface | New web_accessible_resources entry exposes evals.html |

No new network surface in this plan (scoring lands in Plan 03).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Elevation of Privilege | web_accessible_resources exposing evals.html to linkedin.com | mitigate | `matches` restricted to https://www.linkedin.com/*; page only reads/writes local storage, no privileged API exposed |
| T-28-06 | Tampering | bulk-seed overwriting manual labels | mitigate | Idempotency enforced in Plan 01 bulkSeedLabels (`label === undefined` guard); UI only reflects result |
| T-28-07 | Spoofing | XSS via rendered post text in labeling rows | mitigate | Preact escapes text by default; render post text as children, never dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npm run build` exits 0 and dist/dashboard/evals.html is emitted.
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/dashboard/evals.test.ts` passes.
- evals.tsx uses inline-style objects only (no class/className, no .css import).
</verification>

<success_criteria>
- Standalone evals.html page exists, builds, and is web-accessible (D-01).
- Reachable via getURL links from popup AND dashboard (D-02).
- Click-to-label and bulk-seed write through to storage via Plan 01 functions (D-08, D-09).
- Layout matches the locked Option A mockup shell.
</success_criteria>

<output>
Create `.planning/phases/28-evals-dashboard-future/28-02-SUMMARY.md` when done.
</output>
