---
phase: 35-dashboard-polish-feed-health
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/dashboard/index.tsx
  - src/modules/dashboard/SelectorView.tsx
  - src/modules/dashboard/index.html
autonomous: false
requirements: [SHA-02, EXPORT-01, EXPORT-02, BRAND-01]
must_haves:
  truths:
    - "The Selector Health table rows are visually aligned across all columns, including the long COMMENT_EXPAND_BUTTON target name, with the name fully readable (no truncation)"
    - "The data-management 'Export JSON' button reads 'Export matching behaviour'; export contents and handleExportJson behavior are unchanged"
    - "The 'Export Posts CSV' button reads 'Export Posts seen (N)' where N = posts.length, is gated on posts.length > 0, and still downloads the same stored-posts CSV"
    - "The dashboard header shows title 'LinkedIn AIVoice blocker - Feed Health' and subtitle 'because your brain deserves better'; the browser-tab <title> matches the header"
  artifacts:
    - path: "src/modules/dashboard/index.tsx"
      provides: "Rebranded <h1> + subtitle (BRAND-01), Export matching behaviour label (EXPORT-01), Export Posts seen (N) label + posts.length gate (EXPORT-02)"
      contains: "LinkedIn AIVoice blocker - Feed Health"
    - path: "src/modules/dashboard/SelectorView.tsx"
      provides: "Aligned target column — fixed/min-width, whiteSpace:nowrap, names fully readable (SHA-02)"
      contains: "whiteSpace"
    - path: "src/modules/dashboard/index.html"
      provides: "Browser-tab <title> matching header (BRAND-01 / D-10)"
      contains: "LinkedIn AIVoice blocker - Feed Health"
  key_links:
    - from: "src/modules/dashboard/index.tsx"
      to: "posts state (StoredPost[])"
      via: "posts.length gate + (N) count"
      pattern: "posts\\.length > 0"
    - from: "src/modules/dashboard/index.tsx"
      to: "handleExportPostsCsv / buildPostsCsvExport"
      via: "onClick (unchanged behavior)"
      pattern: "handleExportPostsCsv"
---

<objective>
Apply the dashboard-module polish for Phase 35: align the Selector Health table rows (SHA-02),
relabel the two data-management export buttons (EXPORT-01, EXPORT-02), fix the Posts-CSV
visibility gate (EXPORT-02 / D-08), and rebrand the header + browser-tab title (BRAND-01).
All edits are confined to `src/modules/dashboard/` — pure render-layer string/CSS changes with
no new storage writes and no change to export contents or handler behavior.

Purpose: Make the dashboard accurate and on-brand — rows aligned, buttons clearly labeled with
the live posts count, and the new branding in both the header and the tab bar.

Output: 3 edited dashboard files (index.tsx, SelectorView.tsx, index.html). Build/type-check/tests
stay green; a human-verify checkpoint confirms the visual result.

Locked strings (exact, verbatim — do not paraphrase):
- BRAND-01 title (D-09): `LinkedIn AIVoice blocker - Feed Health` (note "AIVoice" one word, spaced hyphen).
- BRAND-01 subtitle (D-09): `because your brain deserves better`.
- EXPORT-01 label (D-06): `Export matching behaviour` (British spelling).
- EXPORT-02 label (D-07): `Export Posts seen (N)` where N = `posts.length`.
- BRAND-01 tab title (D-10 / RESEARCH A1): set `<title>` to the header text `LinkedIn AIVoice blocker - Feed Health`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-dashboard-polish-feed-health/35-CONTEXT.md
@.planning/phases/35-dashboard-polish-feed-health/35-RESEARCH.md
@.planning/phases/35-dashboard-polish-feed-health/35-PATTERNS.md

@src/modules/dashboard/index.tsx
@src/modules/dashboard/SelectorView.tsx
@src/modules/dashboard/index.html
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebrand header + tab title, relabel both export buttons, flip Posts-CSV gate</name>
  <files>src/modules/dashboard/index.tsx, src/modules/dashboard/index.html</files>
  <read_first>
    - src/modules/dashboard/index.tsx:253 — current `<h1 style={s.heading}>LinkedIn Blocker — Feed Health</h1>`; the `s.heading` token (~line 384) is `{ fontSize: 22, fontWeight: 700, marginBottom: 24 }`.
    - src/modules/dashboard/index.tsx:333-345 — Data Management block: line 334 `Export JSON` button (gate `accounts.length > 0 || posts.length > 0 || unflagged.length > 0` on line 333, unchanged); lines 336-338 `Export Posts CSV` button gated `accounts.length > 0`; lines 339-345 the existing "Export LLM call traces (N)" button — the count-in-label precedent to mirror.
    - src/modules/dashboard/index.tsx:79 — `posts` state `useState<StoredPost[]>([])` (the StoredPost[] array buildPostsCsvExport already exports; posts.length is the N).
    - src/modules/dashboard/index.tsx — handleExportJson (unchanged), handleExportPostsCsv (~line 193, unchanged), and existing muted-style tokens (s.statSub, s.categoryNote) to model the new s.subtitle on.
    - src/modules/dashboard/index.html:6 — current `<title>LinkedIn Blocker — Dashboard</title>`.
    - .planning/phases/35-dashboard-polish-feed-health/35-PATTERNS.md — "index.tsx — BRAND-01, EXPORT-01, EXPORT-02" and "index.html" sections give the exact resulting JSX/HTML.
  </read_first>
  <action>
    BRAND-01 (D-09): change the `<h1>` text from `LinkedIn Blocker — Feed Health` to the exact
    string `LinkedIn AIVoice blocker - Feed Health`. Immediately below the `<h1>`, add a subtitle
    element `<div style={s.subtitle}>because your brain deserves better</div>` (exact string). Add a
    new `s.subtitle` style token in the `s` style-token map (NOT a raw inline literal at the JSX site —
    follow the file's `s.*` convention). Model the token on the existing muted tokens (e.g. a muted
    color like `#6b7280` and a smaller fontSize than the 22px heading), matching s.statSub/s.categoryNote
    — exact font size/color is Claude's discretion (D-09). If `s.heading`'s `marginBottom: 24` causes
    the subtitle to detach from the title, move that margin to `s.subtitle` so the title+subtitle stay
    grouped (executor's call). DO NOT add raw style literals at the JSX site.

    EXPORT-01 (D-06): change the JSON button label from `Export JSON` to the exact string
    `Export matching behaviour`. Label-only — do NOT change `handleExportJson` or the line-333 gate.

    EXPORT-02 (D-07 + D-08): change the Posts-CSV button's gate from `accounts.length > 0` to
    `posts.length > 0`, and change its label from `Export Posts CSV` to `Export Posts seen ({posts.length})`.
    Because the new gate guarantees `posts.length >= 1`, render `({posts.length})` UNCONDITIONALLY inside
    the button (no `posts.length > 0 ? ... : ''` guard — unlike the traces button, which renders at 0).
    Do NOT change `handleExportPostsCsv` or `buildPostsCsvExport` — clicking still downloads the same
    stored-posts CSV (D-08).

    BRAND-01 / D-10 (index.html): change the browser-tab `<title>` on line 6 from
    `LinkedIn Blocker — Dashboard` to the exact header string `LinkedIn AIVoice blocker - Feed Health`
    (RESEARCH A1 — default to header text for tab-bar consistency).
  </action>
  <verify>
    <automated>cd c:/Git/linkedin.blocker && npm run type-check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - index.tsx `<h1>` text is exactly `LinkedIn AIVoice blocker - Feed Health` (one-word "AIVoice", spaced hyphen).
    - index.tsx has a subtitle element rendering exactly `because your brain deserves better`, styled via a new `s.subtitle` token in the `s` map (no raw inline style literal at the JSX site).
    - The JSON button label is exactly `Export matching behaviour`; `handleExportJson` and the line-333 gate are unchanged.
    - The Posts button is gated on `posts.length > 0` (not `accounts.length > 0`) and its label is exactly `Export Posts seen (${posts.length})` rendered unconditionally (no `> 0 ?` guard inside the button); `handleExportPostsCsv` is unchanged.
    - index.html `<title>` is exactly `LinkedIn AIVoice blocker - Feed Health`.
    - `npm run type-check` passes; `npm test` passes.
  </acceptance_criteria>
  <done>Header title+subtitle rebranded, tab title matches, both export buttons relabeled with the live posts count, Posts gate flipped to posts.length, handlers/export contents unchanged, type-check/tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align Selector Health table rows (fixed/min-width target column, names fully readable)</name>
  <files>src/modules/dashboard/SelectorView.tsx</files>
  <read_first>
    - src/modules/dashboard/SelectorView.tsx:84-86 — `columnHeaderTarget: { flex: '0 0 27%' }` (header) and `columnHeaderSelector: { flex: '0 0 32%' }`.
    - src/modules/dashboard/SelectorView.tsx:116-129 — `s.target` cell `{ flex: '0 0 27%', fontSize: 13, fontWeight: 400 }` (NO whiteSpace — root cause of the wrap) and the sibling `s.selector` cell which already sets `overflow:hidden; textOverflow:ellipsis; whiteSpace:'nowrap'`.
    - src/modules/dashboard/index.tsx:378 — page `maxWidth: 640` (the width at which `COMMENT_EXPAND_BUTTON`, 21 chars, wraps to two lines at 27%).
    - .planning/phases/35-dashboard-polish-feed-health/35-PATTERNS.md — "SelectorView.tsx — SHA-02 row alignment" section (Pattern 2): recommended flex tweak, and the grid alternative.
  </read_first>
  <action>
    SHA-02 (D-05): make the long `COMMENT_EXPAND_BUTTON` target name render on a single line so its
    row aligns with the others, WITHOUT truncating the name (names must stay fully readable — D-05).
    Recommended (smaller diff): add `whiteSpace: 'nowrap' as const` to the `s.target` cell, and widen
    the target column basis from `'0 0 27%'` to a value that fits `COMMENT_EXPAND_BUTTON` on one line at
    maxWidth:640 (e.g. `'0 0 30%'`), reclaiming the width from the selector column (e.g. drop
    `columnHeaderSelector`/`selector` from `'0 0 32%'` to `'0 0 29%'` — the selector cell already
    ellipsis-truncates so it absorbs the loss cleanly). CRITICAL: change `columnHeaderTarget` and
    `s.target` TOGETHER to the SAME flex-basis so the header stays aligned with the rows (RESEARCH
    Pitfall 4). DO NOT add `overflow:hidden`/`textOverflow:ellipsis` to `s.target` — that truncates the
    name and violates D-05.

    Alternative (discretion D-05, larger diff but cleaner long-term): convert the header+row from
    flexbox to CSS grid with `gridTemplateColumns: minmax(220px, 30%) 1fr 13% 13% 13%`. Either approach
    is acceptable as long as columns align and every target name (longest: COMMENT_EXPAND_BUTTON) stays
    fully visible on one line. Exact CSS mechanism is Claude's discretion (D-05). All styling goes
    through the `s.*` token map per the file's convention.
  </action>
  <verify>
    <automated>cd c:/Git/linkedin.blocker && npm run type-check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `s.target` includes `whiteSpace: 'nowrap'` (or the equivalent grid mechanism) so the target name renders on one line.
    - `s.target` does NOT include `overflow: 'hidden'` or `textOverflow: 'ellipsis'` (names stay fully readable — D-05).
    - `columnHeaderTarget` and `s.target` share the same flex-basis (header/cell aligned — Pitfall 4); if the grid alternative is used, header and rows use the same `gridTemplateColumns`.
    - All styling changes go through `s.*` tokens (no raw inline literals at the JSX site).
    - `npm run type-check` passes; `npm test` passes.
  </acceptance_criteria>
  <done>The Selector Health target column is fixed/min-width with `whiteSpace:nowrap` (header + cell in sync), COMMENT_EXPAND_BUTTON renders on one line fully readable, rows align, and type-check/tests are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the dashboard polish renders correctly</name>
  <action>Checkpoint task — no code is written here. Pause for the user to visually confirm the Task 1 + Task 2 output against the locked strings and alignment criteria below. The executor presents the verification steps and waits for the resume signal before marking the plan complete.</action>
  <what-built>
    Rebranded header (title "LinkedIn AIVoice blocker - Feed Health" + subtitle "because your brain
    deserves better"), matching browser-tab title, relabeled export buttons ("Export matching behaviour"
    and "Export Posts seen (N)" gated on stored posts), and an aligned Selector Health table where the
    long COMMENT_EXPAND_BUTTON row no longer nudges out of alignment.
  </what-built>
  <how-to-verify>
    1. Run `npm run build` then load the unpacked extension (or run the dev server) and open the
       dashboard page (`src/modules/dashboard/index.html` / dashboard route).
    2. Confirm the browser TAB title reads exactly: `LinkedIn AIVoice blocker - Feed Health`.
    3. Confirm the header shows the title `LinkedIn AIVoice blocker - Feed Health` with the subtitle
       `because your brain deserves better` directly beneath it, styled consistently with the dashboard.
    4. In Data Management, confirm the JSON button reads `Export matching behaviour`. If stored posts
       exist, confirm the second button reads `Export Posts seen (N)` with N = the live posts count;
       confirm it is HIDDEN when there are zero stored posts (and shown when posts exist even if no
       flagged accounts exist). Clicking it still downloads the same stored-posts CSV.
    5. In Selector Health, confirm every row's columns line up — especially the `COMMENT_EXPAND_BUTTON`
       row: the full name is visible on one line (not truncated, not wrapped) and its row aligns with
       the others.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any misalignment / wrong label / wrong string.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → dashboard (Preact render) | Dashboard is a stateless READER; this plan adds no storage writes. Rendered values (candidate.value, posts count) are escaped text, not HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Tampering (XSS) | DOM-text injection via dashboard render (SelectorView candidate.value, header copy) | mitigate | Preact escapes text children by default; candidate.value is rendered as a text child + `title` attribute (existing behavior, unchanged). The SHA-02 CSS change introduces no new sink; no `dangerouslySetInnerHTML` is added. All new strings (header/subtitle/labels) are static literals. |
| T-35-04 | Information disclosure | Export buttons download stored data | accept | EXPORT-01/02 are label/gate-only — export contents and handlers (handleExportJson, handleExportPostsCsv, buildPostsCsvExport) are unchanged; no new data is exposed. The gate flip (accounts.length → posts.length) only changes WHEN the existing button is visible, not WHAT it exports. |
| T-35-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — RESEARCH Package Legitimacy Audit: "Not applicable, installs no external packages." |
</threat_model>

<verification>
- `npm run type-check` — tsc clean.
- `npm test` — full Vitest suite green.
- `npm run build` — dashboard builds (vite build resolves the dashboard module).
- Human-verify checkpoint (Task 3) — visual confirmation of all 5 strings + alignment.
</verification>

<success_criteria>
- Header title + subtitle render the exact locked strings; browser-tab title matches the header.
- JSON button reads "Export matching behaviour"; Posts button reads "Export Posts seen (N)" with the live posts.length count, gated on posts.length > 0; both export the same data as before.
- Selector Health rows align across all columns; COMMENT_EXPAND_BUTTON is fully readable on one line (no truncation, no wrap).
- type-check + tests + build green; human-verify approved.
</success_criteria>

<output>
Create `.planning/phases/35-dashboard-polish-feed-health/35-02-SUMMARY.md` when done.
</output>
