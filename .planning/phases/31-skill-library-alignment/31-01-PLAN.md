---
phase: 31-skill-library-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/library/sponsored/SKILL.md
  - src/skills/library/sponsored/sponsored.skill.ts
  - src/content/exclusions/sponsored.skill.ts
  - scripts/skill-order.json
  - scripts/generate-skill-registry.ts
  - src/content/generated-skill-registry.ts
  - src/content/skill-registry.ts
  - package.json
autonomous: true
requirements: [SKILL-05]
user_setup: []

must_haves:
  truths:
    - "The sponsored exclusion skill lives at src/skills/library/sponsored/ with a SKILL.md manifest and its bundled TS impl"
    - "A build-time codegen script parses + validates SKILL.md frontmatter and emits a committed generated module — no runtime FS, no dynamic import, no eval"
    - "SkillRegistry resolves the sponsored exclusion skill from the generated module"
    - "The content-script exclusion run is byte-identical on the fixture set (exclusions.test.ts passes; golden-score snapshot unchanged)"
  artifacts:
    - path: "src/skills/library/sponsored/SKILL.md"
      provides: "Anthropic Agent Skills manifest for the sponsored exclusion (name/description/metadata.kind)"
      contains: "kind: exclusion"
    - path: "src/skills/library/sponsored/sponsored.skill.ts"
      provides: "Moved sponsoredExclusionSkill impl (byte-identical body, fixed import paths)"
      exports: ["sponsoredExclusionSkill"]
    - path: "scripts/generate-skill-registry.ts"
      provides: "Codegen: reads skill-order.json + SKILL.md, validates frontmatter, emits generated module"
      min_lines: 60
    - path: "scripts/skill-order.json"
      provides: "Explicit ordered skill-folder list driving codegen (D-06)"
      contains: "exclusions"
    - path: "src/content/generated-skill-registry.ts"
      provides: "Committed generated full-wiring module (static imports + ordered arrays + metadata)"
      contains: "GENERATED_EXCLUSION_SKILLS"
    - path: "src/content/skill-registry.ts"
      provides: "CODE_EXCLUSION_SKILLS sourced from generated module instead of direct imports"
      contains: "from './generated-skill-registry'"
  key_links:
    - from: "scripts/generate-skill-registry.ts"
      to: "src/skills/library/sponsored/SKILL.md"
      via: "fs.readFileSync at build time"
      pattern: "readFileSync"
    - from: "src/content/generated-skill-registry.ts"
      to: "src/skills/library/sponsored/sponsored.skill"
      via: "static import"
      pattern: "import.*sponsored.skill"
    - from: "src/content/skill-registry.ts"
      to: "src/content/generated-skill-registry.ts"
      via: "import GENERATED_EXCLUSION_SKILLS"
      pattern: "GENERATED_EXCLUSION_SKILLS"
---

<objective>
Spike the entire skill-library path end-to-end through ONE exclusion skill (`sponsored`, the simplest). This is the tracer bullet: it proves folder + SKILL.md + moved impl → codegen script + skill-order.json → committed generated module → SkillRegistry resolves it → content-script exclusion run byte-identical — before any other skill is touched.

Purpose: Validate the full mechanism (D-02 folder shape, D-03 build-time codegen, D-04 full-wiring generated module, D-05 committed, D-06 ordered config, D-07/D-08 frontmatter schema + validation, D-09 zero behavior change) on the cheapest possible slice so subsequent waves are pure repetition.

Output: `src/skills/library/sponsored/` (SKILL.md + impl), `scripts/generate-skill-registry.ts`, `scripts/skill-order.json`, committed `src/content/generated-skill-registry.ts`, rewired `src/content/skill-registry.ts` (exclusion array only), and the `generate-skill-registry` / `prebuild` / `check-skill-registry` npm scripts. SKILL-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-skill-library-alignment/31-CONTEXT.md
@.planning/phases/31-skill-library-alignment/31-RESEARCH.md
@.planning/phases/31-skill-library-alignment/31-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move sponsored exclusion into the library folder with a SKILL.md manifest</name>
  <files>src/skills/library/sponsored/SKILL.md, src/skills/library/sponsored/sponsored.skill.ts, src/content/exclusions/sponsored.skill.ts</files>
  <read_first>
    - src/content/exclusions/sponsored.skill.ts (the impl being moved — byte-identical body)
    - src/shared/skills/types.ts (ExclusionSkill contract + allowed `kind` discriminants)
    - ~/.claude/skills/graphify/SKILL.md (Anthropic Agent Skills frontmatter shape — lines 1-5)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (Exclusion skill impls section — exact path-fixup rule)
    - CLAUDE.md (constraint #1 single-writer; constraint #5 hard exclusions before detection)
  </read_first>
  <action>
    Create `src/skills/library/sponsored/SKILL.md` with frontmatter `name: sponsored-exclusion`, a non-empty `description` describing that it excludes sponsored/promoted posts before any detection runs via the SPONSORED_MARKER selector, and a `metadata:` block with `kind: exclusion` (D-07). Frontmatter MUST contain ONLY name + description + metadata.kind — NO runtime fields (`flavor`, `inputs`, `sync`, `weightKey`, `id`) per D-01.
    Move the `sponsoredExclusionSkill` impl to `src/skills/library/sponsored/sponsored.skill.ts` with the object body byte-identical to the original. Apply the ONLY required import-path fixup: change `'../selector-registry'` to `'../../content/selector-registry'`. The `'../../shared/skills/types'` and `'../../shared/types'` imports stay unchanged (same depth from src/).
    Delete the original `src/content/exclusions/sponsored.skill.ts` (no skill definition remains outside skills/library/ per D-02). Do NOT add a re-export barrel for exclusion skills — the only consumer is skill-registry.ts, which Task 3 rewires.
    Do NOT introduce any selector string literal — `resolve('SPONSORED_MARKER')` stays exactly as-is (constraint #1).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "sponsored" ; test -f src/skills/library/sponsored/SKILL.md && test -f src/skills/library/sponsored/sponsored.skill.ts && test ! -f src/content/exclusions/sponsored.skill.ts && echo MOVED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/library/sponsored/SKILL.md` exists and its frontmatter contains `name:`, `description:`, and `metadata.kind: exclusion`; it contains NONE of `flavor`/`inputs`/`sync`/`weightKey`/`id`.
    - `src/skills/library/sponsored/sponsored.skill.ts` exports `sponsoredExclusionSkill` and imports `resolve` from `'../../content/selector-registry'`.
    - `src/content/exclusions/sponsored.skill.ts` no longer exists.
    - No selector string literal was added (grep for `SPONSORED_MARKER` shows only the `resolve('SPONSORED_MARKER')` call).
  </acceptance_criteria>
  <done>Sponsored skill is a self-contained library folder (manifest + impl) with corrected imports; original removed.</done>
</task>

<task type="auto">
  <name>Task 2: Build the codegen script + skill-order.json + emit the committed generated module</name>
  <files>scripts/generate-skill-registry.ts, scripts/skill-order.json, src/content/generated-skill-registry.ts, package.json</files>
  <read_first>
    - scripts/trace-summary.ts (tsx script conventions: shebang, ESM imports, __dirname via fileURLToPath, process.stderr.write + process.exit(1) on error, writeFileSync output)
    - .planning/phases/31-skill-library-alignment/31-RESEARCH.md (Q2 codegen mechanism, Q3 js-yaml fence-strip parse, frontmatter validation snippet, prebuild/check-skill-registry npm scripts)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (codegen script section + generated module DO-NOT-EDIT header + skill-order.json shape)
    - src/content/skill-registry.ts (lines 44-95 — the exact CODE_EXCLUSION_SKILLS order being reproduced)
    - package.json (existing scripts field — mirror the `trace-summary`/`eval` tsx entries)
  </read_first>
  <action>
    Add `@types/js-yaml` as a devDependency (js-yaml itself is already a transitive dep — do NOT add it as a direct runtime dep). Run the install so node_modules has the types.
    Create `scripts/skill-order.json` with three ordered string arrays: `signals` (listicle-cta, buzzword, em-dash, ai-vocab, hook-story, motivational, impersonal, generic-comments — the exact CODE_SIGNAL_SKILLS order), `exclusions` (sponsored, company-page, non-english, open-to-work — exact CODE_EXCLUSION_SKILLS order), `detectors` (heuristic, llm). NOTE: this wave only has `sponsored` actually present as a library folder; the other folder names are declared in the config now but their folders/SKILL.md arrive in waves 2-3. The codegen MUST be resilient: for this wave, generate ONLY entries whose `src/skills/library/<name>/SKILL.md` actually exists on disk; skip-and-continue for not-yet-migrated names (do not fail the build for a declared-but-absent folder during the tracer). Document this skip behavior in a code comment as tracer-phase-only.
    Create `scripts/generate-skill-registry.ts` (tsx-runnable, following trace-summary.ts conventions): read `scripts/skill-order.json`; for each name whose SKILL.md exists, read it via `fs.readFileSync`, strip the `---` fences with a regex, parse with `yaml.load` from js-yaml, and validate the frontmatter schema (D-08) — `name` non-empty string, `description` non-empty string, `metadata.kind` in `['signal','exclusion','detector']` — calling `process.exit(1)` with a clear stderr message on any violation. Emit `src/content/generated-skill-registry.ts` with: a DO-NOT-EDIT header (regenerate + stale-check commands), a `import type { SignalSkill, ExclusionSkill }` line, explicit static `import` statements for each present impl (path `'../skills/library/<name>/<name>.skill'`), ordered `GENERATED_SIGNAL_SKILLS` and `GENERATED_EXCLUSION_SKILLS` arrays typed `readonly SignalSkill[]` / `readonly ExclusionSkill[]`, and a `GENERATED_SKILL_METADATA` object literal of the parsed descriptive fields. The emitter MUST use ONLY top-level static `import` statements — never `import()`, `import.meta.glob`, `require`, or `eval` (D-03 / MV3-CSP).
    Add three npm scripts to package.json: `"generate-skill-registry": "tsx scripts/generate-skill-registry.ts"`, `"prebuild": "npm run generate-skill-registry"`, `"check-skill-registry": "npm run generate-skill-registry && git diff --exit-code src/content/generated-skill-registry.ts"`.
    Run `npm run generate-skill-registry` to produce and COMMIT `src/content/generated-skill-registry.ts` (D-05). For this wave the generated `GENERATED_EXCLUSION_SKILLS` array contains exactly `[sponsoredExclusionSkill]` (only sponsored has a library folder); `GENERATED_SIGNAL_SKILLS` is empty for now.
  </action>
  <verify>
    <automated>npm run generate-skill-registry && npx tsc --noEmit src/content/generated-skill-registry.ts 2>&1 | head -5 ; grep -q "GENERATED_EXCLUSION_SKILLS" src/content/generated-skill-registry.ts && grep -q "sponsored.skill" src/content/generated-skill-registry.ts && ! grep -E "import\(|import\.meta\.glob|eval\(" src/content/generated-skill-registry.ts && echo GENERATED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run generate-skill-registry` exits 0 and writes `src/content/generated-skill-registry.ts`.
    - The generated module has a DO-NOT-EDIT header, exports `GENERATED_SIGNAL_SKILLS` and `GENERATED_EXCLUSION_SKILLS`, and statically imports `sponsoredExclusionSkill` from `'../skills/library/sponsored/sponsored.skill'`.
    - The generated module contains NO `import(`, no `import.meta.glob`, no `eval(`, no `require(` (grep returns nothing).
    - `scripts/skill-order.json` lists `exclusions` with `sponsored` first.
    - package.json has `generate-skill-registry`, `prebuild`, and `check-skill-registry` scripts; `@types/js-yaml` is in devDependencies.
    - Feeding a SKILL.md with an invalid `metadata.kind` causes the codegen to exit non-zero (verify by temporarily corrupting a copy, or by a guarded manual check — do not leave the corruption committed).
  </acceptance_criteria>
  <done>Codegen script + ordered config exist; the committed generated module wires sponsored via static import only and passes type-check.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire SkillRegistry exclusion array to the generated module and prove parity</name>
  <files>src/content/skill-registry.ts</files>
  <read_first>
    - src/content/skill-registry.ts (lines 39-95 import block + CODE_SIGNAL_SKILLS/CODE_EXCLUSION_SKILLS; lines 97-293 must be preserved verbatim — getters, migrate, seedIfNeeded, load, addDeclarativeSkill, onChanged listener)
    - src/content/generated-skill-registry.ts (the arrays now exported by Task 2)
    - src/content/exclusions/exclusions.test.ts (the exclusion parity test — the byte-identical proof)
    - src/content/detector/heuristic.test.ts (golden-score snapshot — must stay byte-identical)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (skill-registry.ts modified section — before/after of the replaced block; Pitfall 6 tracer double-import warning)
  </read_first>
  <action>
    In `src/content/skill-registry.ts`, replace the direct exclusion-skill static imports (the `from './exclusions/sponsored.skill'` line, which is now a dangling import after Task 1's move) by sourcing the exclusion array from the generated module. Add `import { GENERATED_EXCLUSION_SKILLS } from './generated-skill-registry'` and set `const CODE_EXCLUSION_SKILLS: ExclusionSkill[] = [...GENERATED_EXCLUSION_SKILLS]`. Because this wave migrated ONLY sponsored, keep the OTHER three exclusion skills (`company-page`, `non-english`, `open-to-work`) imported from their CURRENT `./exclusions/*.skill` paths and append them after the generated array so the full priority order is preserved: `[...GENERATED_EXCLUSION_SKILLS, companyPageExclusionSkill, nonEnglishExclusionSkill, openToWorkExclusionSkill]`. This avoids the Pitfall 6 double-import (sponsored must appear EXACTLY once — only via the generated array, never also via a direct import). Leave the signal-skill imports and `CODE_SIGNAL_SKILLS` exactly as-is for this wave (signals migrate in Wave 2). Preserve lines 97-293 (getters, lifecycle, single-writer, onChanged) byte-for-byte.
  </action>
  <verify>
    <automated>npm test -- src/content/exclusions/exclusions.test.ts src/content/detector/heuristic.test.ts && npx tsc --noEmit 2>&1 | head -5 ; grep -c "sponsoredExclusionSkill" src/content/skill-registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/skill-registry.ts` imports `GENERATED_EXCLUSION_SKILLS` from `'./generated-skill-registry'`.
    - `sponsoredExclusionSkill` appears EXACTLY once in the resolved exclusion order (sourced only via the generated array — `grep -c "sponsoredExclusionSkill" src/content/skill-registry.ts` returns 0, since it is no longer directly imported there).
    - `npm test -- src/content/exclusions/exclusions.test.ts` exits 0 (exclusion parity holds — same posts excluded).
    - `npm test -- src/content/detector/heuristic.test.ts` exits 0 and the golden-score `signalBreakdown` snapshot is byte-identical (git diff empty for that test).
    - `npx tsc --noEmit` reports no new errors.
    - Lines 97-293 of skill-registry.ts (getters/lifecycle/single-writer/onChanged) are unchanged (git diff shows changes only in the import + array section).
  </acceptance_criteria>
  <done>The tracer is complete: sponsored resolves through the generated module, exclusion parity + golden snapshot are byte-identical, full path proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → build-time codegen | SKILL.md files are developer-authored inputs read by the Node codegen script |
| committed generated module → browser bundle | Generated TS is bundled into content.js via Vite static import |
| content script → chrome.storage.local | Unchanged from Phase 30 (single-writer skillRegistry key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | generated-skill-registry.ts (committed artifact drifts from source) | mitigate | `check-skill-registry` regenerate-and-diff guard (added Task 2); stale committed file fails the check |
| T-31-02 | Elevation | codegen emitting dynamic import / eval / new Function | mitigate | Codegen emits ONLY top-level static `import` statements; Task 2 verify greps for `import(`/`import.meta.glob`/`eval(` and fails on any (D-03, MV3-CSP) |
| T-31-03 | Tampering | js-yaml parsing arbitrary YAML tags / code-exec | mitigate | Use default `yaml.load` on a fence-stripped descriptive block (no custom tag schema); frontmatter carries only string scalars (name/description/kind), validated by D-08 schema check |
| T-31-04 | Information disclosure | SKILL.md shipping in the production bundle | mitigate | SKILL.md read via Node `fs` only — never `import`ed; no `.md` enters the Vite module graph; generated module carries parsed literals, not raw markdown |
| T-31-SC | Tampering | npm install (@types/js-yaml devDep) | accept | `@types/js-yaml` is a types-only package, no runtime code, no postinstall scripts (RESEARCH Package Legitimacy Audit, slopcheck [OK]); js-yaml already present transitively |
</threat_model>

<verification>
- `npm test -- src/content/exclusions/exclusions.test.ts` passes (exclusion parity).
- `npm test -- src/content/detector/heuristic.test.ts` passes with byte-identical golden snapshot.
- `npm run generate-skill-registry` is idempotent (second run produces no git diff).
- `npx tsc --noEmit` clean.
- No `import(`, `import.meta.glob`, `eval(`, or `.md` import anywhere in src/.
</verification>

<success_criteria>
The sponsored exclusion skill is a self-contained `src/skills/library/sponsored/` folder (SKILL.md + impl); a committed, codegen-emitted generated module statically wires it; SkillRegistry resolves it via the generated array; and the content-script exclusion run plus the golden-score snapshot are byte-identical. The tracer proves the entire path before any other skill moves.
</success_criteria>

<output>
Create `.planning/phases/31-skill-library-alignment/31-01-SUMMARY.md` when done.
</output>
