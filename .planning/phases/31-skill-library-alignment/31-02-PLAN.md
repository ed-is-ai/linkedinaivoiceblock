---
phase: 31-skill-library-alignment
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/skills/library/company-page/SKILL.md
  - src/skills/library/company-page/company-page.skill.ts
  - src/skills/library/non-english/SKILL.md
  - src/skills/library/non-english/non-english.skill.ts
  - src/skills/library/open-to-work/SKILL.md
  - src/skills/library/open-to-work/open-to-work.skill.ts
  - src/skills/library/listicle-cta/SKILL.md
  - src/skills/library/listicle-cta/listicle-cta.skill.ts
  - src/skills/library/buzzword/SKILL.md
  - src/skills/library/buzzword/buzzword.skill.ts
  - src/skills/library/em-dash/SKILL.md
  - src/skills/library/em-dash/em-dash.skill.ts
  - src/skills/library/ai-vocab/SKILL.md
  - src/skills/library/ai-vocab/ai-vocab.skill.ts
  - src/skills/library/hook-story/SKILL.md
  - src/skills/library/hook-story/hook-story.skill.ts
  - src/skills/library/motivational/SKILL.md
  - src/skills/library/motivational/motivational.skill.ts
  - src/skills/library/impersonal/SKILL.md
  - src/skills/library/impersonal/impersonal.skill.ts
  - src/skills/library/generic-comments/SKILL.md
  - src/skills/library/generic-comments/generic-comments.skill.ts
  - src/content/exclusions/company-page.skill.ts
  - src/content/exclusions/non-english.skill.ts
  - src/content/exclusions/open-to-work.skill.ts
  - src/content/detector/signals/listicle-cta.skill.ts
  - src/content/detector/signals/buzzword.skill.ts
  - src/content/detector/signals/em-dash.skill.ts
  - src/content/detector/signals/ai-vocab.skill.ts
  - src/content/detector/signals/hook-story.skill.ts
  - src/content/detector/signals/motivational.skill.ts
  - src/content/detector/signals/impersonal.skill.ts
  - src/content/detector/signals/generic-comments.skill.ts
  - src/content/generated-skill-registry.ts
  - src/content/skill-registry.ts
autonomous: true
requirements: [SKILL-05]
user_setup: []

must_haves:
  truths:
    - "All 3 remaining exclusion skills and all 8 signal skills live as self-contained src/skills/library/<name>/ folders (SKILL.md + impl)"
    - "No signal or exclusion skill definition remains under src/content/detector/signals/ or src/content/exclusions/"
    - "The regenerated module wires all 8 signals and all 4 exclusions via static imports in the exact CODE_SIGNAL_SKILLS / CODE_EXCLUSION_SKILLS order"
    - "SkillRegistry sources both arrays entirely from the generated module"
    - "Golden-score snapshot is byte-identical and exclusion parity holds"
  artifacts:
    - path: "src/content/generated-skill-registry.ts"
      provides: "Full generated arrays: 8 signals (ordered) + 4 exclusions (ordered) + metadata"
      contains: "GENERATED_SIGNAL_SKILLS"
    - path: "src/content/skill-registry.ts"
      provides: "Both CODE_SIGNAL_SKILLS and CODE_EXCLUSION_SKILLS sourced only from the generated module"
      contains: "GENERATED_SIGNAL_SKILLS"
    - path: "src/skills/library/listicle-cta/listicle-cta.skill.ts"
      provides: "Moved composite signal skill with fixed import paths"
      exports: ["listicleCtaSkill"]
  key_links:
    - from: "src/content/generated-skill-registry.ts"
      to: "src/skills/library/*/*.skill"
      via: "static imports for all 12 skills"
      pattern: "import.*skills/library"
    - from: "src/content/skill-registry.ts"
      to: "src/content/generated-skill-registry.ts"
      via: "import GENERATED_SIGNAL_SKILLS + GENERATED_EXCLUSION_SKILLS"
      pattern: "GENERATED_SIGNAL_SKILLS"
---

<objective>
With the tracer path proven in Wave 1, repeat it for everything else: move the remaining 3 exclusion skills (company-page, non-english, open-to-work) and all 8 signal skills (listicle-cta, buzzword, em-dash, ai-vocab, hook-story, motivational, impersonal, generic-comments) into `src/skills/library/<name>/` folders with SKILL.md manifests, regenerate the committed module so it carries the FULL ordered arrays, and switch SkillRegistry to source both arrays entirely from the generated module.

Purpose: Complete the signal + exclusion migration with zero behavior change (D-09). The signal ORDER is the sharpest landmine — `signalBreakdown` key insertion order and the golden-score snapshot depend on the exact CODE_SIGNAL_SKILLS order (D-06).

Output: 11 new library folders (8 signals + 3 exclusions; sponsored already done), the regenerated full `generated-skill-registry.ts`, and a `skill-registry.ts` whose two arrays come solely from the generated module. SKILL-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-skill-library-alignment/31-RESEARCH.md
@.planning/phases/31-skill-library-alignment/31-PATTERNS.md
@CLAUDE.md
@.planning/phases/31-skill-library-alignment/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the 3 remaining exclusion skills into library folders</name>
  <files>src/skills/library/company-page/SKILL.md, src/skills/library/company-page/company-page.skill.ts, src/skills/library/non-english/SKILL.md, src/skills/library/non-english/non-english.skill.ts, src/skills/library/open-to-work/SKILL.md, src/skills/library/open-to-work/open-to-work.skill.ts, src/content/exclusions/company-page.skill.ts, src/content/exclusions/non-english.skill.ts, src/content/exclusions/open-to-work.skill.ts</files>
  <read_first>
    - src/content/exclusions/company-page.skill.ts, src/content/exclusions/non-english.skill.ts, src/content/exclusions/open-to-work.skill.ts (the impls being moved — byte-identical bodies)
    - src/skills/library/sponsored/SKILL.md and src/skills/library/sponsored/sponsored.skill.ts (the Wave 1 template to mirror)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (Exclusion skill impls section — the path-fixup rule, including non-english's `'../detector/language'` import)
    - CLAUDE.md (constraint #1 single-writer; constraint #5)
  </read_first>
  <action>
    For each of company-page, non-english, open-to-work: create `src/skills/library/<name>/SKILL.md` with frontmatter `name` (`<name>-exclusion` style), a non-empty `description`, and `metadata.kind: exclusion` (no runtime fields, D-01). Move each impl to `src/skills/library/<name>/<name>.skill.ts` with the object body byte-identical.
    Apply path fixups: for company-page and open-to-work change `'../selector-registry'` to `'../../content/selector-registry'`. For non-english change `'../detector/language'` to `'../../content/detector/language'` (its selector-free underlying language module stays in place). The `'../../shared/...'` imports stay unchanged for all three.
    Delete the three original `src/content/exclusions/*.skill.ts` files (D-02). Leave `src/content/exclusions/exclusions.test.ts` in place (it imports from the registry, not from the moved files — confirm and adjust only its import path if it directly references a moved skill file).
    Add no selector string literals (constraint #1).
  </action>
  <verify>
    <automated>test -f src/skills/library/company-page/company-page.skill.ts && test -f src/skills/library/non-english/non-english.skill.ts && test -f src/skills/library/open-to-work/open-to-work.skill.ts && test ! -f src/content/exclusions/company-page.skill.ts && test ! -f src/content/exclusions/non-english.skill.ts && test ! -f src/content/exclusions/open-to-work.skill.ts && echo MOVED_OK</automated>
  </verify>
  <acceptance_criteria>
    - Three new `src/skills/library/<name>/SKILL.md` files exist, each with `metadata.kind: exclusion` and no runtime fields.
    - Three new `<name>.skill.ts` impls export their respective skill consts with corrected import paths (selector-registry → `'../../content/selector-registry'`; non-english language → `'../../content/detector/language'`).
    - The three original `src/content/exclusions/*.skill.ts` files no longer exist.
  </acceptance_criteria>
  <done>All four exclusion skills now live in the library; the old exclusions skill files are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Move all 8 signal skills into library folders (order-critical)</name>
  <files>src/skills/library/listicle-cta/SKILL.md, src/skills/library/listicle-cta/listicle-cta.skill.ts, src/skills/library/buzzword/SKILL.md, src/skills/library/buzzword/buzzword.skill.ts, src/skills/library/em-dash/SKILL.md, src/skills/library/em-dash/em-dash.skill.ts, src/skills/library/ai-vocab/SKILL.md, src/skills/library/ai-vocab/ai-vocab.skill.ts, src/skills/library/hook-story/SKILL.md, src/skills/library/hook-story/hook-story.skill.ts, src/skills/library/motivational/SKILL.md, src/skills/library/motivational/motivational.skill.ts, src/skills/library/impersonal/SKILL.md, src/skills/library/impersonal/impersonal.skill.ts, src/skills/library/generic-comments/SKILL.md, src/skills/library/generic-comments/generic-comments.skill.ts, src/content/detector/signals/listicle-cta.skill.ts, src/content/detector/signals/buzzword.skill.ts, src/content/detector/signals/em-dash.skill.ts, src/content/detector/signals/ai-vocab.skill.ts, src/content/detector/signals/hook-story.skill.ts, src/content/detector/signals/motivational.skill.ts, src/content/detector/signals/impersonal.skill.ts, src/content/detector/signals/generic-comments.skill.ts</files>
  <read_first>
    - All 8 `src/content/detector/signals/*.skill.ts` files (the impls being moved — byte-identical bodies; note listicle-cta uses detectionConfig, generic-comments is the only sync:false)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (Signal skill impls section — the exact path-fixup rule for `'./<fn>'` → `'../../content/detector/signals/<fn>'` and `'../../../shared/...'` → `'../../shared/...'`)
    - src/shared/skills/types.ts (CodeSkill contract)
    - src/content/detector/heuristic.test.ts (golden-score snapshot — pins per-signal scores and signalBreakdown key order; DO NOT let it change)
  </read_first>
  <action>
    For each of the 8 signals (listicle-cta, buzzword, em-dash, ai-vocab, hook-story, motivational, impersonal, generic-comments): create `src/skills/library/<name>/SKILL.md` with `name`, non-empty `description`, and `metadata.kind: signal` (no runtime fields — `flavor`/`inputs`/`sync`/`weightKey`/`id` stay in TS per D-01). Move each `.skill.ts` impl to `src/skills/library/<name>/<name>.skill.ts` with the object body byte-identical (preserve `kind`/`flavor`/`id`/`inputs`/`sync`/`run` exactly; do NOT redeclare any weight — weights stay in the underlying functions / detectionConfig).
    Apply path fixups per skill: every underlying-function import like `'./buzzwords'`, `'./listicle'`, `'./cta'`, `'./em-dash'`, `'./ai-vocab'`, `'./hook-story'`, `'./motivational'`, `'./impersonal'`, `'./comments'` becomes `'../../content/detector/signals/<fn>'` (the underlying function files do NOT move). The type import `'../../../shared/skills/types'` becomes `'../../shared/skills/types'`. The `'../../../shared/detectionConfig'` import (listicle-cta only) becomes `'../../shared/detectionConfig'`.
    Delete all 8 original `src/content/detector/signals/*.skill.ts` files (D-02). The underlying function files (`buzzwords.ts`, `listicle.ts`, `cta.ts`, etc.) and `__tests__/` stay in place untouched.
  </action>
  <verify>
    <automated>for s in listicle-cta buzzword em-dash ai-vocab hook-story motivational impersonal generic-comments; do test -f src/skills/library/$s/$s.skill.ts || { echo "MISSING $s"; exit 1; }; test ! -f src/content/detector/signals/$s.skill.ts || { echo "STALE $s"; exit 1; }; done && echo SIGNALS_MOVED_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 8 `src/skills/library/<name>/<name>.skill.ts` files exist and export their original skill consts with corrected import paths.
    - All 8 SKILL.md files have `metadata.kind: signal` and contain none of `flavor`/`inputs`/`sync`/`weightKey`/`id`.
    - All 8 original `src/content/detector/signals/*.skill.ts` files no longer exist; the underlying function files remain.
    - No weight literal was added to any moved skill (grep the moved files for numeric weight literals shows none beyond what existed in the original bodies).
  </acceptance_criteria>
  <done>All 8 signal skills are self-contained library folders with corrected imports; underlying functions untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the full module and switch SkillRegistry to source both arrays from it</name>
  <files>src/content/generated-skill-registry.ts, src/content/skill-registry.ts</files>
  <read_first>
    - scripts/skill-order.json (confirm the signals + exclusions order is exactly CODE_SIGNAL_SKILLS / CODE_EXCLUSION_SKILLS)
    - scripts/generate-skill-registry.ts (the codegen — now all declared folders exist, so it emits the full arrays)
    - src/content/skill-registry.ts (the Wave-1 partial state: exclusions split between generated + direct imports; signals still direct)
    - src/content/detector/heuristic.test.ts and src/content/exclusions/exclusions.test.ts (the byte-identical proofs)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (skill-registry.ts after-block; ordering landmine)
  </read_first>
  <action>
    Run `npm run generate-skill-registry`. Now that all 12 signal+exclusion folders exist, the codegen emits the FULL `GENERATED_SIGNAL_SKILLS` (8 signals in CODE_SIGNAL_SKILLS order) and `GENERATED_EXCLUSION_SKILLS` (4 exclusions in CODE_EXCLUSION_SKILLS order) plus `GENERATED_SKILL_METADATA`. Commit the regenerated file (D-05).
    In `src/content/skill-registry.ts`: remove ALL remaining direct skill imports (the 8 `./detector/signals/*.skill` imports and the 3 leftover `./exclusions/*.skill` imports kept in Wave 1). Source both arrays solely from the generated module: `import { GENERATED_SIGNAL_SKILLS, GENERATED_EXCLUSION_SKILLS } from './generated-skill-registry'`, then `const CODE_SIGNAL_SKILLS: SignalSkill[] = [...GENERATED_SIGNAL_SKILLS]` and `const CODE_EXCLUSION_SKILLS: ExclusionSkill[] = [...GENERATED_EXCLUSION_SKILLS]`. After this, skill-registry.ts contains ZERO direct `./detector/signals/` or `./exclusions/` imports. Preserve lines 97-293 (getters/lifecycle/single-writer/onChanged) byte-for-byte.
  </action>
  <verify>
    <automated>npm run generate-skill-registry && npm test -- src/content/detector/heuristic.test.ts src/content/exclusions/exclusions.test.ts && npx tsc --noEmit 2>&1 | head -5 ; ! grep -E "detector/signals/.*\.skill|exclusions/.*\.skill" src/content/skill-registry.ts && echo REWIRE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/generated-skill-registry.ts` `GENERATED_SIGNAL_SKILLS` has exactly 8 entries in CODE_SIGNAL_SKILLS order; `GENERATED_EXCLUSION_SKILLS` has exactly 4 in CODE_EXCLUSION_SKILLS order.
    - `src/content/skill-registry.ts` contains NO direct `./detector/signals/*.skill` or `./exclusions/*.skill` import (grep returns nothing).
    - `npm test -- src/content/detector/heuristic.test.ts` exits 0 with byte-identical golden snapshot (git diff empty for that test block).
    - `npm test -- src/content/exclusions/exclusions.test.ts` exits 0 (exclusion parity holds).
    - `npm run generate-skill-registry` is idempotent (no git diff on second run).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>All signals and exclusions resolve through the committed generated module; golden snapshot + exclusion parity byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → build-time codegen | 11 new SKILL.md files read by the Node codegen script |
| committed generated module → browser bundle | Full generated arrays bundled into content.js via static imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02-01 | Tampering | signal-array reorder (golden snapshot drift) | mitigate | skill-order.json is the single order source; Task 3 verify runs heuristic.test.ts which fails on any signalBreakdown key-order change (D-06) |
| T-31-02-02 | Elevation | regenerated module introducing dynamic import / eval | mitigate | Codegen emits only static imports (proven Wave 1); no codegen logic change this wave |
| T-31-02-03 | Information disclosure | moved SKILL.md files entering the bundle | mitigate | SKILL.md still read via fs only, never imported; no `.md` import added |
| T-31-02-04 | Tampering | weight literal reintroduced into a moved signal skill | mitigate | Weights stay in underlying functions / detectionConfig (CFG-01); Task 2 acceptance greps moved files for added weight literals |
</threat_model>

<verification>
- `npm test` full suite green; heuristic.test.ts golden snapshot byte-identical.
- `npm run generate-skill-registry` idempotent.
- `npx tsc --noEmit` clean.
- No skill definition remains under src/content/detector/signals/ or src/content/exclusions/ (only underlying functions + tests).
- skill-registry.ts has zero direct skill imports.
</verification>

<success_criteria>
Every signal and exclusion skill is a self-contained `src/skills/library/<name>/` folder; the committed generated module wires all 12 via static imports in the exact pipeline order; SkillRegistry sources both arrays solely from it; and the golden-score snapshot plus exclusion parity are byte-identical.
</success_criteria>

<output>
Create `.planning/phases/31-skill-library-alignment/31-02-SUMMARY.md` when done.
</output>
