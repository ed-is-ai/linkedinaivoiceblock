---
phase: 31-skill-library-alignment
plan: 04
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - src/content/generated-skill-registry.test.ts
  - src/skills/library/AUTHORING.md
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [SKILL-05]
user_setup: []

must_haves:
  truths:
    - "A test asserts GENERATED_SIGNAL_SKILLS order equals Phase 30 CODE_SIGNAL_SKILLS order and GENERATED_EXCLUSION_SKILLS order equals CODE_EXCLUSION_SKILLS order (D-06 order pinning)"
    - "A test asserts each generated skill's TS kind matches its frontmatter metadata.kind (D-07 drift guard)"
    - "The committed generated module is protected by a stale-check that fails if it is out of date (D-05 regenerate-and-diff)"
    - "A short skill-authoring note documents 'drop a skills/library/<name>/ folder + one registry entry' (SKILL-05 success criterion 5)"
  artifacts:
    - path: "src/content/generated-skill-registry.test.ts"
      provides: "Order-pinning + kind-drift-guard tests"
      contains: "toStrictEqual"
    - path: "src/skills/library/AUTHORING.md"
      provides: "Skill-authoring note: how to add a new library skill"
      min_lines: 20
    - path: "package.json"
      provides: "check-skill-registry stale-check script wired into CI"
      contains: "check-skill-registry"
  key_links:
    - from: "src/content/generated-skill-registry.test.ts"
      to: "src/content/generated-skill-registry.ts"
      via: "import GENERATED_SIGNAL_SKILLS + GENERATED_EXCLUSION_SKILLS"
      pattern: "GENERATED_SIGNAL_SKILLS"
    - from: ".github/workflows/ci.yml"
      to: "package.json check-skill-registry"
      via: "CI step runs npm run check-skill-registry"
      pattern: "check-skill-registry"
---

<objective>
Lock the migration's invariants with tests and CI, then document the authoring path. Add (1) the order-pinning test asserting the generated arrays match the Phase 30 order exactly (D-06), (2) the frontmatter-kind drift-guard test asserting each skill's TS `kind` matches its manifest `metadata.kind` (D-07), (3) wire the `check-skill-registry` stale-check into CI so a stale committed generated module fails the build (D-05), and (4) write the short skill-authoring note (SKILL-05 success criterion 5).

Purpose: Make the ordering landmine and manifest/TS drift impossible to regress silently, and capture the "drop a folder + one entry" authoring workflow.

Output: `generated-skill-registry.test.ts`, CI stale-check wiring, and `AUTHORING.md`. SKILL-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-skill-library-alignment/31-RESEARCH.md
@.planning/phases/31-skill-library-alignment/31-PATTERNS.md
@CLAUDE.md
@.planning/phases/31-skill-library-alignment/31-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Order-pinning + kind-drift-guard tests</name>
  <files>src/content/generated-skill-registry.test.ts</files>
  <behavior>
    - Test 1: GENERATED_SIGNAL_SKILLS.map(s => s.id) === ['listicle-cta','buzzword','em-dash','ai-vocab','hook-story','motivational','impersonal','generic-comments'] (D-06)
    - Test 2: GENERATED_EXCLUSION_SKILLS.map(s => s.id) === ['sponsored','company-page','non-english','open-to-work'] (D-06)
    - Test 3: every skill in GENERATED_SIGNAL_SKILLS has kind === 'signal' (D-07)
    - Test 4: every skill in GENERATED_EXCLUSION_SKILLS has kind === 'exclusion' (D-07)
  </behavior>
  <read_first>
    - src/content/generated-skill-registry.ts (the arrays under test)
    - src/content/detector/heuristic.test.ts (vitest it/expect/toStrictEqual structure to mirror)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (order drift-guard + kind drift-guard test patterns — copy verbatim)
    - .planning/phases/31-skill-library-alignment/31-RESEARCH.md (Q4 required order; Q7 four test layers)
  </read_first>
  <action>
    Create `src/content/generated-skill-registry.test.ts` importing `GENERATED_SIGNAL_SKILLS` and `GENERATED_EXCLUSION_SKILLS` from `'./generated-skill-registry'`. Add two `toStrictEqual` assertions on `.map(s => s.id)` against the exact expected order arrays (the Phase 30 CODE_SIGNAL_SKILLS / CODE_EXCLUSION_SKILLS order). Add two loops asserting `skill.kind === 'signal'` for every signal and `skill.kind === 'exclusion'` for every exclusion (this is the runtime second layer of the D-07 drift guard; the codegen already validates frontmatter kind at build time per D-08). Use vitest `describe`/`it`/`expect` mirroring heuristic.test.ts conventions.
  </action>
  <verify>
    <automated>npm test -- src/content/generated-skill-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- src/content/generated-skill-registry.test.ts` exits 0 with 4 passing tests.
    - The order assertions use `toStrictEqual` on `.map(s => s.id)`.
    - Reordering `scripts/skill-order.json` signals + regenerating causes this test to fail (verify by a temporary local reorder, then revert — do not commit the reorder).
  </acceptance_criteria>
  <done>Order and kind invariants are pinned by passing tests that fail on drift.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the stale-check into CI</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <read_first>
    - package.json (the `check-skill-registry` script added in Wave 1; existing test/type-check/lint scripts)
    - .github/workflows/ (read any existing workflow file; if none exists, this creates the first one)
    - .planning/phases/31-skill-library-alignment/31-RESEARCH.md (Pattern 5 + Q2 stale-check CI wiring)
  </read_first>
  <action>
    Confirm `package.json` has `"check-skill-registry": "npm run generate-skill-registry && git diff --exit-code src/content/generated-skill-registry.ts"` (added in Wave 1; verify it is present and correct). If `.github/workflows/ci.yml` does not exist, create a minimal GitHub Actions workflow that runs on push/PR: checkout, setup-node, `npm ci`, then steps for `npm run check-skill-registry`, `npm run type-check`, and `npm test`. If a CI workflow already exists, ADD a `npm run check-skill-registry` step before the test step rather than creating a new file. The stale-check step must fail the job if the committed generated module is out of date (D-05).
  </action>
  <verify>
    <automated>npm run check-skill-registry && grep -q "check-skill-registry" .github/workflows/ci.yml && echo CI_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check-skill-registry` exits 0 against the current committed generated module (it is up to date).
    - A CI workflow file contains a `npm run check-skill-registry` step that runs before tests.
    - The workflow also runs `npm run type-check` and `npm test`.
  </acceptance_criteria>
  <done>CI fails if the committed generated module drifts from the SKILL.md sources.</done>
</task>

<task type="auto">
  <name>Task 3: Write the skill-authoring note</name>
  <files>src/skills/library/AUTHORING.md</files>
  <read_first>
    - scripts/skill-order.json (the ordered config a new skill must be added to)
    - scripts/generate-skill-registry.ts (the regenerate step)
    - src/skills/library/sponsored/ (a complete reference example: SKILL.md + impl)
    - .planning/phases/31-skill-library-alignment/31-RESEARCH.md (State of the Art table — "drop a folder + rerun codegen")
    - CLAUDE.md (constraint #1, #5 — note the selector-registry single-writer caveat)
  </read_first>
  <action>
    Write `src/skills/library/AUTHORING.md` documenting the authoring workflow: (1) create `src/skills/library/<name>/` with a SKILL.md (frontmatter = name + description + metadata.kind in {signal,exclusion,detector}; NO runtime fields) and a `<name>.skill.ts` impl carrying the runtime contract (kind/flavor/id/inputs/sync/run or check); (2) add `<name>` to the correct array in `scripts/skill-order.json` — APPENDING to the END of `signals` only (never inserting between existing signals, because signalBreakdown key order + the golden snapshot depend on order — D-06); (3) run `npm run generate-skill-registry` to regenerate + commit the module; (4) run `npm test` (the order/kind drift-guard + golden snapshot will catch mistakes). Note the codegen fails the build on invalid frontmatter (D-08), weights live only in detectionConfig / underlying functions (never in SKILL.md or the skill object), the generated module is committed and stale-checked in CI (D-05), and SKILL.md files are build-time inputs only (never shipped in the bundle). Note the selector-registry folder is a thin re-export representation (not wired into any array) preserving CLAUDE.md constraint #1.
  </action>
  <verify>
    <automated>test -f src/skills/library/AUTHORING.md && grep -q "skill-order.json" src/skills/library/AUTHORING.md && grep -qi "append" src/skills/library/AUTHORING.md && echo AUTHORING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/library/AUTHORING.md` exists and documents the four-step "drop a folder + add to skill-order.json + regenerate + test" workflow.
    - It explicitly states signals must be APPENDED to the end of the order list (D-06 landmine).
    - It notes frontmatter-only metadata vs TS runtime contract (D-01), build-time validation (D-08), committed+stale-checked module (D-05), and the selector-registry single-writer caveat.
  </acceptance_criteria>
  <done>The authoring path is documented; SKILL-05 success criterion 5 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed generated module → CI stale-check | CI regenerates and diffs the committed artifact to detect drift/tampering |
| skill-order.json → generated arrays | The ordered config is the single source of order truth, pinned by a test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04-01 | Tampering | committed generated module edited by hand or left stale | mitigate | `check-skill-registry` (regenerate-and-diff) wired into CI (Task 2); job fails on drift (D-05) |
| T-31-04-02 | Repudiation | silent signal reorder bypassing the golden snapshot | mitigate | Order-pinning test (Task 1) fails the build independently of the golden snapshot (D-06) |
| T-31-04-03 | Tampering | frontmatter kind diverging from TS kind | mitigate | Kind-drift-guard test (Task 1) + build-time codegen validation (D-08, D-07) |
| T-31-04-04 | Information disclosure | AUTHORING.md / SKILL.md shipping in bundle | accept | Markdown docs are build-time/dev artifacts; not imported; AUTHORING.md explicitly states SKILL.md is never bundled |
</threat_model>

<verification>
- `npm test` full suite green including the new generated-skill-registry.test.ts.
- `npm run check-skill-registry` exits 0 (committed module up to date).
- CI workflow runs check-skill-registry before tests.
- AUTHORING.md documents the append-only ordering rule and the full authoring workflow.
</verification>

<success_criteria>
The generated arrays' order and each skill's kind are pinned by passing tests; CI fails if the committed generated module is stale; and a skill-authoring note captures the "drop a folder + one registry entry + regenerate" workflow — satisfying all five SKILL-05 success criteria across the phase.
</success_criteria>

<output>
Create `.planning/phases/31-skill-library-alignment/31-04-SUMMARY.md` when done.
</output>
