---
phase: 31-skill-library-alignment
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - src/skills/library/heuristic/SKILL.md
  - src/skills/library/heuristic/heuristic.skill.ts
  - src/skills/library/llm/SKILL.md
  - src/skills/library/llm/llm.skill.ts
  - src/skills/library/selector-registry/SKILL.md
  - src/skills/library/selector-registry/selector-registry.skill.ts
  - src/content/detector/heuristic.ts
  - src/content/detector/llm.ts
  - scripts/generate-skill-registry.ts
  - src/content/generated-skill-registry.ts
autonomous: true
requirements: [SKILL-05]
user_setup: []

must_haves:
  truths:
    - "Both DetectorSkills (heuristic, llm) live as self-contained src/skills/library/<name>/ folders with SKILL.md + impl"
    - "The selector registry has a skills/library/selector-registry/ folder (SKILL.md + thin re-export) preserving CLAUDE.md constraint #1"
    - "No skill definition remains outside src/skills/library/ (the old detector files are thin re-export barrels only, carrying no skill definition)"
    - "The generated module emits GENERATED_DETECTOR_SKILLS metadata; runtime detector instantiation in index.ts is unchanged"
    - "Golden snapshot + exclusion parity remain byte-identical; type-check clean"
  artifacts:
    - path: "src/skills/library/heuristic/heuristic.skill.ts"
      provides: "Moved HeuristicDetector class with fixed skill-registry import"
      exports: ["HeuristicDetector"]
    - path: "src/skills/library/llm/llm.skill.ts"
      provides: "Moved LLMDetector class"
      exports: ["LLMDetector"]
    - path: "src/skills/library/selector-registry/selector-registry.skill.ts"
      provides: "Thin re-export of SelectorRegistry public API (single-writer preserved)"
      contains: "from '../../content/selector-registry'"
    - path: "src/content/generated-skill-registry.ts"
      provides: "Adds GENERATED_DETECTOR_SKILLS descriptive metadata"
      contains: "detector"
  key_links:
    - from: "src/skills/library/heuristic/heuristic.skill.ts"
      to: "src/content/skill-registry.ts"
      via: "import getSignalSkills"
      pattern: "getSignalSkills"
    - from: "src/skills/library/selector-registry/selector-registry.skill.ts"
      to: "src/content/selector-registry.ts"
      via: "re-export (no storage write)"
      pattern: "export.*selector-registry"
---

<objective>
Move the two DetectorSkills (HeuristicDetector, LLMDetector) into `src/skills/library/heuristic/` and `src/skills/library/llm/` folders with SKILL.md manifests, and place a thin `src/skills/library/selector-registry/` folder (SKILL.md + re-export wrapper) so that NO skill definition remains outside `skills/library/` (D-02). The selector registry's canonical singleton does NOT move — only a re-export wrapper is added, preserving CLAUDE.md constraint #1 (only SelectorRegistry writes selector strings to storage).

Purpose: Complete the "every skill is a library folder" requirement (SKILL-05 success criterion 2) with zero behavior change. Detectors are instantiated in index.ts/eval.ts, not stored in a registry array — so the generated module emits detector metadata for completeness but the runtime wiring is unchanged.

Output: heuristic, llm, and selector-registry library folders; thin re-export barrels left at the old detector paths so index.ts/eval.ts imports keep working; codegen extended to emit `GENERATED_DETECTOR_SKILLS` metadata. SKILL-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-skill-library-alignment/31-RESEARCH.md
@.planning/phases/31-skill-library-alignment/31-PATTERNS.md
@CLAUDE.md
@.planning/phases/31-skill-library-alignment/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move HeuristicDetector and LLMDetector into library folders with re-export barrels</name>
  <files>src/skills/library/heuristic/SKILL.md, src/skills/library/heuristic/heuristic.skill.ts, src/skills/library/llm/SKILL.md, src/skills/library/llm/llm.skill.ts, src/content/detector/heuristic.ts, src/content/detector/llm.ts</files>
  <read_first>
    - src/content/detector/heuristic.ts (full file — imports getSignalSkills from '../skill-registry', detectionConfig, runPatternSkill; the class body to move byte-identical)
    - src/content/detector/llm.ts (full file — imports only types; no internal relative deps beyond shared)
    - src/content/index.ts (lines 5-6 import HeuristicDetector + LLMDetector from './detector/heuristic' and './detector/llm')
    - scripts/eval.ts (line 30 imports HeuristicDetector from '../src/content/detector/heuristic.js')
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (heuristic.skill.ts and llm.skill.ts move sections — exact path fixups)
    - src/content/detector/heuristic.test.ts (golden snapshot — must stay byte-identical; confirm where it imports HeuristicDetector from)
  </read_first>
  <action>
    Move the HeuristicDetector class to `src/skills/library/heuristic/heuristic.skill.ts` with the class body byte-identical. Apply the ONLY path fixup: change `import { getSignalSkills } from '../skill-registry'` to `'../../content/skill-registry'`. The `'../../shared/...'` imports (types, detectionConfig, pattern-runner) stay unchanged at the new depth.
    Move the LLMDetector class to `src/skills/library/llm/llm.skill.ts` byte-identical; its `'../../shared/...'` type imports resolve identically at the new depth (no fixup needed). `chrome.runtime.sendMessage` is an ambient global — unaffected.
    To avoid touching `src/content/index.ts`, `scripts/eval.ts`, and `heuristic.test.ts` import sites, leave thin re-export BARRELS at the old paths: `src/content/detector/heuristic.ts` becomes `export { HeuristicDetector } from '../../skills/library/heuristic/heuristic.skill'` (plus re-export any named types it previously exported, e.g. `HeuristicDetectorOptions`), and `src/content/detector/llm.ts` becomes `export { LLMDetector } from '../../skills/library/llm/llm.skill'`. These barrels carry NO skill definition — they are pure re-exports — so D-02 ("no skill definition remains outside skills/library/") holds: the definitions live in the library; the barrels only forward.
    Create `src/skills/library/heuristic/SKILL.md` and `src/skills/library/llm/SKILL.md`, each with `name`, non-empty `description`, and `metadata.kind: detector` (no runtime fields, D-01).
  </action>
  <verify>
    <automated>npm test -- src/content/detector/heuristic.test.ts && npx tsc --noEmit 2>&1 | head -5 ; grep -q "skills/library/heuristic/heuristic.skill" src/content/detector/heuristic.ts && grep -q "skills/library/llm/llm.skill" src/content/detector/llm.ts && grep -q "kind: detector" src/skills/library/heuristic/SKILL.md && echo DETECTORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/library/heuristic/heuristic.skill.ts` exports `HeuristicDetector` and imports `getSignalSkills` from `'../../content/skill-registry'`.
    - `src/skills/library/llm/llm.skill.ts` exports `LLMDetector`.
    - `src/content/detector/heuristic.ts` and `src/content/detector/llm.ts` are pure re-export barrels (each contains only `export { ... } from '...skills/library/...'` lines and no class body).
    - Both detector SKILL.md files have `metadata.kind: detector` and no runtime fields.
    - `npm test -- src/content/detector/heuristic.test.ts` exits 0 with byte-identical golden snapshot.
    - `npx tsc --noEmit` clean — index.ts and eval.ts compile unchanged via the barrels.
  </acceptance_criteria>
  <done>Both detectors live in library folders; old paths are thin barrels; golden snapshot byte-identical; callers untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the selector-registry library folder (thin re-export, single-writer preserved)</name>
  <files>src/skills/library/selector-registry/SKILL.md, src/skills/library/selector-registry/selector-registry.skill.ts</files>
  <read_first>
    - src/content/selector-registry.ts (the canonical singleton — confirm its exact public exports: resolve, load, seedIfNeeded, updateCandidate, recordMiss, and any others actually exported; do NOT move it)
    - .planning/phases/31-skill-library-alignment/31-PATTERNS.md (selector-registry thin re-export section)
    - .planning/phases/31-skill-library-alignment/31-RESEARCH.md (Q6 selector registry as library skill)
    - CLAUDE.md (constraint #1 — only SelectorRegistry writes selector strings to storage)
  </read_first>
  <action>
    Create `src/skills/library/selector-registry/selector-registry.skill.ts` as a thin re-export of the canonical singleton's PUBLIC API from `'../../content/selector-registry'`. Re-export exactly the names that `src/content/selector-registry.ts` actually exports (read the file first to get the precise set — do not invent names). Add a header comment stating: the canonical singleton stays at src/content/selector-registry.ts; this file is only a representation satisfying D-02; CLAUDE.md constraint #1 — ONLY the canonical file writes selector strings to storage. This wrapper MUST contain NO `storageSet` call and NO selector string literal.
    Create `src/skills/library/selector-registry/SKILL.md` with `name: selector-registry`, a description noting it is the runtime source of truth for DOM selector lookups and that only SelectorRegistry writes selector storage, and `metadata.kind: exclusion` (closest existing discriminant; the folder is NOT wired into any skill array — cosmetic, per RESEARCH A4).
    Do NOT add selector-registry to any `skill-order.json` array and do NOT import this wrapper anywhere — it exists for Anthropic Agent Skills convention completeness only.
  </action>
  <verify>
    <automated>test -f src/skills/library/selector-registry/SKILL.md && grep -q "from '../../content/selector-registry'" src/skills/library/selector-registry/selector-registry.skill.ts && ! grep -E "storageSet" src/skills/library/selector-registry/selector-registry.skill.ts && npx tsc --noEmit 2>&1 | head -5 ; echo SELECTOR_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/library/selector-registry/selector-registry.skill.ts` re-exports only names actually exported by `src/content/selector-registry.ts`, from `'../../content/selector-registry'`.
    - The wrapper contains NO `storageSet` call and NO selector string literal (constraint #1 preserved).
    - `src/skills/library/selector-registry/SKILL.md` has `metadata.kind` set and a description referencing the single-writer rule.
    - selector-registry is NOT present in any `scripts/skill-order.json` array.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The selector registry has a library-folder representation; the single-writer invariant and all existing import sites are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Extend codegen to emit detector metadata and regenerate</name>
  <files>scripts/generate-skill-registry.ts, src/content/generated-skill-registry.ts</files>
  <read_first>
    - scripts/generate-skill-registry.ts (current codegen — extend, do not rewrite)
    - scripts/skill-order.json (the `detectors` array: heuristic, llm)
    - src/skills/library/heuristic/SKILL.md and src/skills/library/llm/SKILL.md (now present)
    - .planning/phases/31-skill-library-alignment/31-RESEARCH.md (Q5 metadata-only hydration; Open Question 2 — detectors emitted for completeness, not consumed by registry)
  </read_first>
  <action>
    Extend `scripts/generate-skill-registry.ts` so it also reads + validates the `detectors` folders' SKILL.md (heuristic, llm) and emits their descriptive metadata into `GENERATED_SKILL_METADATA` (and optionally a `GENERATED_DETECTOR_SKILLS` metadata-only export). Do NOT add detector skill objects to `GENERATED_SIGNAL_SKILLS` or `GENERATED_EXCLUSION_SKILLS`, and do NOT make skill-registry.ts consume detectors — detectors are instantiated in index.ts/eval.ts (Open Question 2 resolution: metadata for completeness only). Keep the static-import-only constraint (no dynamic import/eval). The selector-registry folder is NOT in skill-order.json, so the codegen does not process it.
    Regenerate with `npm run generate-skill-registry` and commit the updated `src/content/generated-skill-registry.ts` (D-05).
  </action>
  <verify>
    <automated>npm run generate-skill-registry && npx tsc --noEmit 2>&1 | head -5 ; grep -q "heuristic" src/content/generated-skill-registry.ts && grep -q "detector" src/content/generated-skill-registry.ts && ! grep -E "import\(|import\.meta\.glob|eval\(" src/content/generated-skill-registry.ts && npm test -- src/content/detector/heuristic.test.ts src/content/exclusions/exclusions.test.ts && echo CODEGEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/generated-skill-registry.ts` includes heuristic + llm descriptive metadata (in GENERATED_SKILL_METADATA and/or GENERATED_DETECTOR_SKILLS) with `kind: 'detector'`.
    - Detectors are NOT added to GENERATED_SIGNAL_SKILLS or GENERATED_EXCLUSION_SKILLS (those still have 8 + 4 entries).
    - The generated module contains no `import(`, `import.meta.glob`, or `eval(`.
    - `npm run generate-skill-registry` is idempotent.
    - `npm test -- heuristic.test.ts exclusions.test.ts` exits 0; golden snapshot byte-identical.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Codegen covers all skill kinds; detector metadata is emitted without altering runtime detector wiring; behavior byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selector-registry wrapper → chrome.storage.local | Wrapper re-exports only; the single-writer remains the canonical selector-registry.ts |
| detector library impls → browser bundle | HeuristicDetector/LLMDetector bundled via the old-path barrels into content.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03-01 | Tampering | selector-registry wrapper introducing a second storage writer (breaks constraint #1) | mitigate | Wrapper is re-export only; Task 2 verify greps for `storageSet` and fails if present; no selector string literal added |
| T-31-03-02 | Spoofing | re-export barrels masking a behavior change in moved detectors | mitigate | heuristic.test.ts golden snapshot runs through the barrel path and fails on any score/breakdown change (D-09) |
| T-31-03-03 | Elevation | extended codegen emitting dynamic import for detectors | mitigate | Detectors emitted as metadata only (no executable wiring in registry); Task 3 verify greps for `import(`/`eval(` |
| T-31-03-04 | Information disclosure | detector/selector SKILL.md entering the bundle | mitigate | SKILL.md read via fs only; never imported |
</threat_model>

<verification>
- `npm test` full suite green; golden snapshot byte-identical.
- No skill definition outside src/skills/library/ (old detector files are pure re-export barrels).
- selector-registry wrapper has no storageSet and no selector literal.
- `npm run generate-skill-registry` idempotent; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Both detectors and the selector registry have `src/skills/library/<name>/` folders (SKILL.md + impl/wrapper); no skill definition lives outside the library; the single-writer invariant holds; detector metadata is emitted; and the golden snapshot plus exclusion parity are byte-identical.
</success_criteria>

<output>
Create `.planning/phases/31-skill-library-alignment/31-03-SUMMARY.md` when done.
</output>
