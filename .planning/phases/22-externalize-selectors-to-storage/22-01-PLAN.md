---
phase: 22-externalize-selectors-to-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
autonomous: true
requirements: [SELECTOR-01]
must_haves:
  truths:
    - "The SelectorRegistry schema types compile and are importable from src/shared/types.ts"
    - "StorageSchema accepts selectorRegistry and selectorSessionMisses keys without type errors"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "SelectorTarget, CandidateSource, SelectorCandidate, TargetEntry, SelectorRegistrySchema types + StorageSchema extension"
      contains: "export interface SelectorRegistrySchema"
  key_links:
    - from: "src/shared/types.ts (StorageSchema)"
      to: "selectorRegistry / selectorSessionMisses keys"
      via: "optional schema fields"
      pattern: "selectorRegistry\\?: SelectorRegistrySchema"
---

<objective>
Define the storage-backed selector registry type contracts in `src/shared/types.ts`. This is the foundation all other Phase 22 plans build against: the registry module, the consumer migrations, and the dashboard health view all import these types.

Purpose: Interface-first ordering — every downstream plan consumes these contracts, so they land first (Wave 1) to eliminate scavenger-hunt exploration.
Output: `SelectorTarget`, `CandidateSource`, `SelectorCandidate`, `TargetEntry`, `SelectorRegistrySchema` types plus two new optional `StorageSchema` keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-externalize-selectors-to-storage/22-RESEARCH.md
@.planning/phases/22-externalize-selectors-to-storage/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector-registry type definitions and extend StorageSchema</name>
  <files>src/shared/types.ts</files>
  <read_first>
    - src/shared/types.ts — read the WHOLE file; observe the existing JSDoc + interface block style (e.g. the FlaggedAccount interface ~lines 79–112) and the existing `StorageSchema` interface (~lines 189–209) so new fields follow the exact optional-field pattern.
    - .planning/phases/22-externalize-selectors-to-storage/22-RESEARCH.md — section "Schema Definition for types.ts" (the authoritative type shapes) and Open Questions #1 and #2 (attribute-name targets, POST_URN_ATTR_FALLBACK).
    - .planning/phases/22-externalize-selectors-to-storage/22-PATTERNS.md — section "src/shared/types.ts (MODIFIED — additive type extensions)".
    - src/content/selectors.ts — confirm the complete set of 15 exported selector constants (excluding SELECTORS_VERSION) so the SelectorTarget union is exhaustive.
  </read_first>
  <action>
    Add these exports to src/shared/types.ts following the existing JSDoc+interface style.

    Define `SelectorTarget` as a string-literal union covering ALL 15 selector targets (every constant exported from selectors.ts EXCEPT SELECTORS_VERSION): FEED_CONTAINER, FEED_CONTAINER_FALLBACK, POST_CARD, POST_URN_ATTR, POST_URN_ATTR_FALLBACK, POST_BODY_TEXT, POST_AUTHOR_NAME, POST_AUTHOR_LINK, SPONSORED_MARKER, COMPANY_PAGE_MARKER, RESHARE_INDICATOR, COMMENT_EXPAND_BUTTON, OPEN_TO_WORK_MARKER, COMMENT_TEXT, AUTHOR_HEADLINE, CONNECTION_DEGREE. Include POST_URN_ATTR_FALLBACK and POST_AUTHOR_NAME in the union for completeness even though grep confirms no consumer imports POST_URN_ATTR_FALLBACK and POST_AUTHOR_NAME is a dead import (planner decision, baked in).

    Define `CandidateSource` as the union 'seed' | 'heuristic' | 'llm' | 'user'.

    Define `SelectorCandidate` interface with fields: value: string (selector string OR attribute-name OR URL-pattern — add a JSDoc note that attribute-name/URL-pattern targets such as POST_URN_ATTR, POST_URN_ATTR_FALLBACK, COMPANY_PAGE_MARKER store a literal string returned as-is, not a querySelector), source: CandidateSource, lastMatchedAt: string | null (ISO 8601), addedAt: string (ISO 8601), failCount: number. (Note: SELECTOR-01 requirement text mentions lastVerifiedAt and matchCount — add lastVerifiedAt: string | null and matchCount: number to satisfy the requirement metadata fields, per SELECTOR-01.)

    Define `TargetEntry` interface: candidates: SelectorCandidate[] with JSDoc "index 0 = active".

    Define `SelectorRegistrySchema` interface: version: string (mirrors SELECTORS_VERSION), targets: Record<SelectorTarget, TargetEntry>, lastAdaptedAt: string | null.

    Extend the existing `StorageSchema` interface with two optional fields (do NOT remove or reorder existing fields): selectorRegistry?: SelectorRegistrySchema and selectorSessionMisses?: SelectorTarget[] (the latter written by the content script, read by the dashboard).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` (tsc --noEmit) exits 0.
    - src/shared/types.ts contains `export type SelectorTarget =` with all 16 members listed in the action (15 selector constants + the two completeness entries are within that 16-target set: FEED_CONTAINER, FEED_CONTAINER_FALLBACK, POST_CARD, POST_URN_ATTR, POST_URN_ATTR_FALLBACK, POST_BODY_TEXT, POST_AUTHOR_NAME, POST_AUTHOR_LINK, SPONSORED_MARKER, COMPANY_PAGE_MARKER, RESHARE_INDICATOR, COMMENT_EXPAND_BUTTON, OPEN_TO_WORK_MARKER, COMMENT_TEXT, AUTHOR_HEADLINE, CONNECTION_DEGREE).
    - src/shared/types.ts contains `export interface SelectorRegistrySchema` with `targets: Record<SelectorTarget, TargetEntry>`.
    - src/shared/types.ts contains `export interface SelectorCandidate` with fields value, source, lastMatchedAt, lastVerifiedAt, addedAt, failCount, matchCount.
    - StorageSchema contains `selectorRegistry?: SelectorRegistrySchema` and `selectorSessionMisses?: SelectorTarget[]`.
    - No existing StorageSchema field was removed (grep confirms flaggedAccounts, dismissedAccounts, anthropicApiKey, settings, dailyStats, storedPosts still present).
  </acceptance_criteria>
  <done>
    All five new type exports exist, StorageSchema carries the two new optional keys, and `tsc --noEmit` is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → content/dashboard read | Stored registry data is read back and (for selector targets) used as DOM query strings. Storage is same-extension sandboxed but can hold corrupted/tampered data from a prior version or a buggy write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | SelectorRegistrySchema type definition | accept | Types are compile-time only; runtime validation of stored shape is enforced in Plan 02 (migrate/load must tolerate malformed stored data). This plan adds no runtime surface. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "No New Packages"); slopcheck not applicable. |
</threat_model>

<verification>
- `npm run type-check` exits 0 with the new types present.
- Types are exported (not local) so Plans 02–05 can import them.
</verification>

<success_criteria>
- SelectorTarget, CandidateSource, SelectorCandidate, TargetEntry, SelectorRegistrySchema all exported from src/shared/types.ts.
- StorageSchema extended with selectorRegistry and selectorSessionMisses optional keys.
- tsc --noEmit clean (SELECTOR-01 schema foundation in place).
</success_criteria>

<output>
Create `.planning/phases/22-externalize-selectors-to-storage/22-01-SUMMARY.md` when done.
</output>
