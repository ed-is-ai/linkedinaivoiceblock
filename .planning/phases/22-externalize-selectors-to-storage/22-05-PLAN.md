---
phase: 22-externalize-selectors-to-storage
plan: 05
type: execute
wave: 4
depends_on: ["22-03", "22-04"]
files_modified:
  - CLAUDE.md
  - src/content/selectors.ts
autonomous: true
requirements: [SELECTOR-09, SELECTOR-10]
must_haves:
  truths:
    - "CLAUDE.md constraint #1 describes the seed-vs-runtime selector model (selectors.ts = defaults; SelectorRegistry = runtime source; only the registry writes selectors to storage)"
    - "The selectors.ts header comment describes the seed-vs-runtime model and that runtime reads go through SelectorRegistry"
    - "selectors.ts still exports all 16 constants unchanged (only the header comment changes)"
    - "The full test suite and type-check pass — final regression gate confirms v6.1-identical behavior"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated critical constraint #1 wording (seed-vs-runtime model)"
      contains: "SelectorRegistry"
    - path: "src/content/selectors.ts"
      provides: "Updated header comment; all selector constants intact"
      contains: "seed"
  key_links:
    - from: "CLAUDE.md constraint #1"
      to: "selectors.ts header"
      via: "consistent seed-vs-runtime wording (D-08)"
      pattern: "SelectorRegistry"
---

<objective>
Finalize Phase 22: update the project documentation to the seed-vs-runtime selector model (SELECTOR-10, D-08), and run the final regression gate proving the extension behaves identically to v6.1 (SELECTOR-09). This plan runs last (Wave 4) so the docs reflect the completed migration and the regression check covers the whole phase.

Purpose: Reconcile CLAUDE.md constraint #1 and the selectors.ts header with the new runtime model now that the registry is the source of truth; lock the regression-safety success criterion.
Output: updated CLAUDE.md + selectors.ts header + green full-suite verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-externalize-selectors-to-storage/22-CONTEXT.md
@.planning/phases/22-externalize-selectors-to-storage/22-RESEARCH.md
@.planning/phases/22-externalize-selectors-to-storage/22-03-SUMMARY.md
@.planning/phases/22-externalize-selectors-to-storage/22-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md constraint #1 and selectors.ts header to the seed-vs-runtime model</name>
  <files>CLAUDE.md, src/content/selectors.ts</files>
  <read_first>
    - CLAUDE.md — the "Critical Constraints" section, specifically constraint #1 ("No CSS class names as selectors … All selectors live in one file: the selector registry"). This is the wording to extend.
    - src/content/selectors.ts — the header comment block (lines 1–17), specifically the "THIS IS THE ONLY FILE …" lines and the selector-value rules.
    - .planning/phases/22-externalize-selectors-to-storage/22-CONTEXT.md — D-08, which gives the EXACT replacement wording to use.
    - .planning/phases/22-externalize-selectors-to-storage/22-RESEARCH.md — "State of the Art" section, the CLAUDE.md constraint #1 updated wording (D-08) block.
  </read_first>
  <action>
    In CLAUDE.md constraint #1, preserve the no-CSS-class-names rule and add the seed-vs-runtime model using the exact D-08 wording: "All hard-coded LinkedIn selector strings live in `selectors.ts` and only there. At runtime the content script reads selectors exclusively through `SelectorRegistry`, which hydrates from `chrome.storage.local` and falls back to the `selectors.ts` seed. Only `SelectorRegistry` may write selector strings to storage." Keep the data-*/aria-label/role/semantic-only requirement and the single-source-of-truth intent intact.

    In src/content/selectors.ts, update ONLY the header comment block (lines 1–17) to describe the seed-vs-runtime model: selectors.ts holds the seed/default values and is the only place selector strings are hard-coded; at runtime consumers read through SelectorRegistry which hydrates from chrome.storage.local and falls back to this seed; only SelectorRegistry writes selectors to storage. Retain the existing selector-value rules (data-*/aria-*/role/semantic/URL-pattern only; CSS class names forbidden) and the inspection-date reference. Do NOT change any exported constant value, the SELECTORS_VERSION, or remove any export.
  </action>
  <verify>
    <automated>npm run type-check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md constraint #1 contains the strings "SelectorRegistry", "hydrates from", and "Only `SelectorRegistry` may write selector strings to storage" (D-08 wording), AND still contains the data-*/aria/role/semantic-only rule.
    - src/content/selectors.ts header comment references SelectorRegistry and the seed/fallback model and retains the CSS-class-forbidden rule.
    - All 16 selector constant exports in selectors.ts are unchanged in value (grep confirms FEED_CONTAINER === '[data-component-type="LazyColumn"]', POST_URN_ATTR === 'componentkey', COMPANY_PAGE_MARKER === '/company/', SELECTORS_VERSION === '1.3.0', etc. are still present and unmodified).
    - `npm run type-check` exits 0 and `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    CLAUDE.md and the selectors.ts header both describe the seed-vs-runtime model per D-08; no selector constant value changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final regression gate — full suite green, no direct selector imports remain</name>
  <files>src/content/observer.ts, src/content/exclusions.ts</files>
  <read_first>
    - .planning/phases/22-externalize-selectors-to-storage/22-03-SUMMARY.md — what was migrated, to confirm the regression scope.
    - src/content/observer.ts and src/content/exclusions.ts — confirm post-migration state (no selector-string imports) for success criterion 1.
  </read_first>
  <action>
    Run the full verification gate proving SELECTOR-09 (behaves identically to v6.1) and success criterion 1 (no direct selector imports in observer.ts / exclusions.ts). Run `npm run type-check`, `npm run build`, and `npx vitest run` and confirm all exit 0. Grep observer.ts and exclusions.ts to confirm no selector-string constants are imported from './selectors' (only SELECTORS_VERSION may remain in observer.ts as version metadata). Do not modify code in this task unless the gate reveals a regression; if it does, fix the offending consumer migration to restore identical behavior and re-run.
  </action>
  <verify>
    <automated>npm run type-check && npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0.
    - `npm run build` exits 0.
    - `npx vitest run` exits 0 with no failing tests (existing suites unchanged — regression-safe).
    - grep of observer.ts shows no imported selector-string constant from './selectors' other than SELECTORS_VERSION.
    - grep of exclusions.ts shows no import from any selectors module (resolve() is imported from './selector-registry' instead).
  </acceptance_criteria>
  <done>
    The whole phase type-checks, builds, and passes the full test suite; no runtime consumer imports a selector string directly — the extension is confirmed regression-safe vs v6.1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → developer behavior | Constraint wording governs future selector handling; an inaccurate constraint could lead to re-introduced direct imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-12 | Repudiation | doc/runtime drift (docs say registry-only but a direct import sneaks back) | mitigate | Task 2 grep gate asserts no direct selector-string imports remain in observer.ts/exclusions.ts, keeping the documented constraint and the code in sync. |
| T-22-13 | Tampering | accidental change to a selector constant value during header edit | mitigate | Acceptance criteria assert all selector constant values are unchanged (grep specific values); only the comment block changes. |
| T-22-SC | Tampering | npm installs | accept | Zero package installs this phase. |
</threat_model>

<verification>
- `npm run type-check`, `npm run build`, `npx vitest run` all exit 0.
- CLAUDE.md constraint #1 and selectors.ts header carry the D-08 seed-vs-runtime wording (SELECTOR-10).
- No direct selector-string imports remain in observer.ts/exclusions.ts (success criterion 1).
</verification>

<success_criteria>
- Documentation updated to the seed-vs-runtime model per D-08 (SELECTOR-10).
- Final regression gate green — extension behaves identically to v6.1 (SELECTOR-09).
- No selector constant values changed.
</success_criteria>

<output>
Create `.planning/phases/22-externalize-selectors-to-storage/22-05-SUMMARY.md` when done.
</output>
