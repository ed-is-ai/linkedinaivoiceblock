---
phase: 29-config-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/detector/heuristic.test.ts
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "Running npm test produces a green golden-score snapshot that pins the exact score + signalBreakdown for every representative fixture post"
    - "The snapshot captures the listicle-cta composite (all three tiers), buzzword, em-dash, ai-vocab, hook-story, motivational, impersonal, and generic-comments signals so any weight drift in Phase 29 Plan 02 is caught"
  artifacts:
    - path: "src/content/detector/heuristic.test.ts"
      provides: "Golden-score snapshot describe block asserting exact score + breakdown via toStrictEqual (D-06)"
      contains: "golden-score snapshot"
  key_links:
    - from: "src/content/detector/heuristic.test.ts"
      to: "src/content/detector/heuristic.ts"
      via: "new HeuristicDetector().detect(fixture)"
      pattern: "detector\\.detect"
---

<objective>
Establish the D-06 golden-score snapshot safety net BEFORE any literal is moved into the config module. This plan adds a snapshot test that pins the exact `score` and `signalBreakdown` produced by `HeuristicDetector` over a representative set of posts, so the Plan 02 refactor can be proven byte-identical (zero behavior change).

Purpose: Phase 29's hard constraint is zero behavior change. A pinned golden-score snapshot turns "did the refactor change any value?" into a deterministic green/red test. This plan must complete and be committed green BEFORE Plan 02 touches any source file.
Output: An expanded `heuristic.test.ts` containing a golden-score `describe` block that asserts exact score + breakdown for fixtures covering every signal whose literal moves in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/29-config-foundation/29-CONTEXT.md
@.planning/phases/29-config-foundation/29-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add golden-score snapshot covering all signals whose literals move in Plan 02</name>
  <read_first>
    - src/content/detector/heuristic.test.ts (the file being modified — reuse its existing fixtures and import style: `import { describe, it, expect } from 'vitest'`, `import { HeuristicDetector } from './heuristic'`)
    - src/content/detector/heuristic.ts (the detect() pipeline whose output is being pinned — lines 73-162; confirms which breakdown keys are produced: listicle-cta, buzzword, em-dash, ai-vocab, hook-story, motivational, impersonal, generic-comments)
    - .planning/phases/29-config-foundation/29-PATTERNS.md (section "src/content/detector/heuristic.test.ts — MODIFY" — fixture table with URNs urn:li:activity:1..voice001/human001, and the golden-score snapshot pattern note that toStrictEqual is preferred over toMatchSnapshot)
  </read_first>
  <behavior>
    - The clean-prose fixture (urn:li:activity:1) snapshots to score 0 with an empty breakdown.
    - The listicle+CTA fixture (urn:li:activity:2) snapshots with breakdown['listicle-cta'] === 25 (the listicleCta.both tier).
    - The heavy-buzzword fixture (urn:li:activity:3) snapshots with breakdown['buzzword'] === 15.
    - The em-dash fixture (urn:li:activity:4) snapshots with breakdown['em-dash'] at its exact current value.
    - The AI-voice fixture (urn:li:activity:voice001) snapshots with the exact hook-story + motivational + impersonal breakdown values and exact total score.
    - The genuine-human fixture (urn:li:activity:human001) snapshots with its exact (low) score and breakdown.
    - Every snapshotted value is the value the CURRENT code produces — captured by running the suite, never hand-guessed.
  </behavior>
  <action>
    Add a new describe block named to include the phrase "golden-score snapshot" (e.g. "HeuristicDetector — golden-score snapshot (D-06 zero-behavior-change)") to src/content/detector/heuristic.test.ts. Reuse the EXISTING fixture posts already defined in this file (urn:li:activity:1 clean prose, urn:li:activity:2 listicle+CTA, urn:li:activity:3 heavy buzzwords, urn:li:activity:4 em-dash, urn:li:activity:voice001 AI voice, urn:li:activity:human001 genuine human) — do NOT invent new post text. Instantiate `new HeuristicDetector()` with NO fetchComments (matches eval + most existing tests; generic-comments will not fire, which is the intended runtime-without-comments baseline). For each fixture, call `detect()` and assert the result with `toStrictEqual` (NOT toMatchSnapshot — explicit inline pins per PATTERNS.md, no separate snapshot file to commit) against an object of the EXACT shape `{ score: <N>, breakdown: <result.signalBreakdown> }`. To get the exact N and breakdown values: first write the assertions with placeholder values, run `npm test`, read the actual produced values from the failing-diff output, then paste those exact values in. Cover at minimum the listicle-cta composite (the 25 tier via fixture 2), buzzword (15), em-dash, and the AI-voice fixture's hook-story/motivational/impersonal contributions. Do NOT modify any production file in this plan — only heuristic.test.ts changes. Do NOT change any existing test in the file.
  </action>
  <verify>
    <automated>cd /c/Git/linkedin.blocker && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (all tests green, including the new golden-score block).
    - The new describe block's name contains the substring "golden-score snapshot".
    - At least 4 distinct fixtures are pinned with `toStrictEqual` on an object containing both `score` and the breakdown (verifiable: `grep -c "toStrictEqual" src/content/detector/heuristic.test.ts` returns >= 4).
    - No `toMatchSnapshot` call is introduced (verifiable: `grep -c "toMatchSnapshot" src/content/detector/heuristic.test.ts` returns 0).
    - No production source file (heuristic.ts, content/index.ts, eval.ts) is modified by this plan.
  </acceptance_criteria>
  <done>npm test passes with a committed golden-score snapshot that pins exact score + breakdown for all signals whose literals move in Plan 02; the snapshot is the byte-identical baseline Plan 02 must preserve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds a test file change only. No new input crosses any trust boundary; no network, storage write, or DOM/host API is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | golden-score test fixtures | accept | Test-only change; no production code path or trust boundary touched. A wrong pinned value fails closed (red test) rather than shipping bad behavior. |

**Assessment:** Phase 29 is a pure internal constant-relocation refactor with no new I/O, network, storage-write, or trust boundary. This plan adds a test only. No high-severity threats are introduced; the threat model is intentionally minimal per the phase security note.
</threat_model>

<verification>
- `npm test` green with the new golden-score block present.
- The snapshot covers listicle-cta (25 tier), buzzword (15), em-dash, and AI-voice composite signals so Plan 02 weight drift is detectable.
</verification>

<success_criteria>
- Golden-score snapshot exists, is green, and pins exact score + breakdown for representative fixtures (D-06).
- No production source changed — this is the pre-refactor safety net only.
</success_criteria>

<output>
Create `.planning/phases/29-config-foundation/29-01-SUMMARY.md` when done.
</output>
