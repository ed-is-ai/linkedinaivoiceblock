---
phase: 29-config-foundation
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/shared/detectionConfig.ts
  - src/content/index.ts
  - src/content/detector/heuristic.ts
  - scripts/eval.ts
autonomous: true
requirements: [CFG-01]
must_haves:
  truths:
    - "src/shared/detectionConfig.ts exists and exports a single nested `detectionConfig` object declared `as const` holding thresholds (flag, openToWorkPenalty, autoHideDefault), weights (listicleCta tiers + per-signal caps), and maxPostsPerSession"
    - "content/index.ts, heuristic.ts, and scripts/eval.ts import their threshold/weight values from detectionConfig.ts — no numeric detection literal remains at those call sites"
    - "npm test and npm run type-check both pass green; the Plan 01 golden-score snapshot is byte-identical (same score + breakdown for every fixture) — zero behavior change"
    - "scripts/eval.ts references detectionConfig.thresholds.flag so the eval CLI operating point can never drift from the runtime flag threshold (no manual sync step)"
  artifacts:
    - path: "src/shared/detectionConfig.ts"
      provides: "Single source of truth for all detection constants (D-01 nested as const, D-04 full weight extraction, D-05 semantic tier keys)"
      exports: ["detectionConfig"]
      contains: "as const"
      min_lines: 25
    - path: "src/content/index.ts"
      provides: "Content-script consumer importing detectionConfig for flag/openToWorkPenalty/autoHideDefault"
      contains: "detectionConfig"
    - path: "src/content/detector/heuristic.ts"
      provides: "Detector consumer importing detectionConfig.weights for listicle-cta tiers + generic-comments gate"
      contains: "detectionConfig.weights"
    - path: "scripts/eval.ts"
      provides: "Eval CLI consumer importing detectionConfig.thresholds.flag (no-drift tie-in)"
      contains: "detectionConfig"
  key_links:
    - from: "src/content/index.ts"
      to: "src/shared/detectionConfig.ts"
      via: "import { detectionConfig } from '../shared/detectionConfig'"
      pattern: "from '\\.\\./shared/detectionConfig'"
    - from: "src/content/detector/heuristic.ts"
      to: "src/shared/detectionConfig.ts"
      via: "import { detectionConfig } from '../../shared/detectionConfig'"
      pattern: "from '\\.\\./\\.\\./shared/detectionConfig'"
    - from: "scripts/eval.ts"
      to: "src/shared/detectionConfig.ts"
      via: "import { detectionConfig } from '../src/shared/detectionConfig.js'"
      pattern: "from '\\.\\./src/shared/detectionConfig\\.js'"
---

<objective>
Create the single-source `src/shared/detectionConfig.ts` module and relocate every detection threshold/weight literal from `content/index.ts`, `heuristic.ts`, and `scripts/eval.ts` into it — with ZERO behavior change, proven by the Plan 01 golden-score snapshot staying byte-identical.

Purpose: Satisfies CFG-01 — one committed detection-config module is the sole source of detection constants, imported by both runtime (content script) and the eval CLI, so the runtime operating point and the eval operating point can never drift. This is the foundation Phases 31 (reads maxPostsPerSession) and 32/33 (tune threshold + weights) build on.
Output: detectionConfig.ts (new) + three refactored consumers; npm test + npm run type-check green; snapshot unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/29-config-foundation/29-CONTEXT.md
@.planning/phases/29-config-foundation/29-PATTERNS.md
@src/shared/eval/metrics.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/shared/detectionConfig.ts (single nested as-const config)</name>
  <read_first>
    - src/shared/eval/metrics.ts (lines 1-31 — the analog: host-agnostic module JSDoc contract + the `export const THRESHOLDS … ` single-source "no drift" pattern to mirror exactly: section divider comment with decision ref, JSDoc explaining who imports and why no drift, then the export)
    - .planning/phases/29-config-foundation/29-PATTERNS.md (section "src/shared/detectionConfig.ts — NEW" — the exact target object shape with provenance comments mapping every key to its source line, and the "Note on signal max values" guidance that Phase 29 must NOT add a Math.min cap that did not exist before)
    - .planning/phases/29-config-foundation/29-CONTEXT.md (D-01 single nested object as const; D-02 autoHideDefault 60; D-04 full extraction; D-05 semantic nested tier keys; Claude's Discretion: maxPostsPerSession seeded now as 50)
  </read_first>
  <action>
    Create src/shared/detectionConfig.ts. Begin with a module-level JSDoc stating the host-agnostic contract (no chrome.*, no document.*, no fs, no process — mirror metrics.ts lines 1-16) and listing the consumers (content script content/index.ts + heuristic.ts, and the eval CLI scripts/eval.ts). The file needs ZERO runtime imports (it is a pure literal object). Export a SINGLE nested object `export const detectionConfig = { … } as const;` (D-01 — not flat named exports). Structure:
    - `thresholds`: `flag: 35` (from content/index.ts L26 FLAG_THRESHOLD), `openToWorkPenalty: 20` (content/index.ts L27 OPEN_TO_WORK_PENALTY), `autoHideDefault: 60` (content/index.ts L78/L170/L217 `?? 60` fallbacks) — D-02.
    - `weights.listicleCta`: `{ both: 25, listicleOnly: 12, ctaOnly: 8 }` (heuristic.ts L85/L89/L92) — D-05 semantic tier keys.
    - `weights`: per-signal cap keys documenting the JSDoc-stated caps (buzzword max 15, emDash max 10, aiVocab max 12, hookStory max 20, motivational max 20, impersonal max 15) and `genericComments: { gate: 20, max: 15 }` (gate from heuristic.ts L144 `score > 20`) — D-04.
    - `maxPostsPerSession: 50` (seeded now for Phase 31; no consumer yet — no-op for behavior).
    Carry provenance comments `// (D-04)` / `// (D-05)` and per-key source-line comments into the file so rationale is not lost. Add a section divider comment + JSDoc explaining "single source, no drift" exactly as metrics.ts does. Do NOT change any value — every literal must equal the value currently at its source line. Do NOT add a Math.min cap anywhere (the `max` keys are documentation for Phase 32, not behavior in Phase 29).
  </action>
  <verify>
    <automated>cd /c/Git/linkedin.blocker && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - File src/shared/detectionConfig.ts exists.
    - Exports exactly one symbol `detectionConfig` declared with `as const` (verifiable: `grep -c "as const" src/shared/detectionConfig.ts` >= 1; `grep -c "export const detectionConfig" src/shared/detectionConfig.ts` === 1).
    - Contains keys: thresholds.flag (value 35), thresholds.openToWorkPenalty (20), thresholds.autoHideDefault (60), weights.listicleCta.both (25), weights.listicleCta.listicleOnly (12), weights.listicleCta.ctaOnly (8), weights.genericComments.gate (20), maxPostsPerSession (50).
    - No `chrome.`, `document.`, `import ... 'fs'`, or `process.` token appears (verifiable: `grep -vE '^\s*(//|\*)' src/shared/detectionConfig.ts | grep -cE 'chrome\.|document\.|process\.' ` === 0).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>detectionConfig.ts exists as a single nested as-const object with all detection constants and provenance comments; type-check is green; no value differs from the current source literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor content/index.ts + heuristic.ts to import from detectionConfig</name>
  <read_first>
    - src/content/index.ts (lines 22-27 the FLAG_THRESHOLD=35 / OPEN_TO_WORK_PENALTY=20 declarations; L78 `let currentThreshold = 60`; L170 `?? 60`; L217 `?? 60`; L304 OPEN_TO_WORK_PENALTY use; L324 + L327 FLAG_THRESHOLD uses — the exact call sites being rewired)
    - src/content/detector/heuristic.ts (lines 83-95 listicle-cta composite 25/12/8 literals; L144 `if (score > 20 …` generic-comments gate; lines 13-22 import block where the new import goes)
    - src/shared/detectionConfig.ts (the module created in Task 1 — the source of truth being imported)
    - .planning/phases/29-config-foundation/29-PATTERNS.md (sections "src/content/index.ts — MODIFY" and "src/content/detector/heuristic.ts — MODIFY" — exact import paths: content uses `'../shared/detectionConfig'` no .js; heuristic uses `'../../shared/detectionConfig'` no .js; and the note that buzzword/em-dash/ai-vocab/hook/motivational/impersonal have NO literal in heuristic.ts so nothing to replace there; confidence-band 60/35 on L159 are heuristic-internal, NOT detection thresholds — leave them)
  </read_first>
  <action>
    In src/content/index.ts: add `import { detectionConfig } from '../shared/detectionConfig';` (after existing imports, before the DEBUG constant). Delete the `const FLAG_THRESHOLD = 35;` (L26) and `const OPEN_TO_WORK_PENALTY = 20;` (L27) declarations. Replace the 2 FLAG_THRESHOLD uses (L324, L327) with `detectionConfig.thresholds.flag`. Replace the 1 OPEN_TO_WORK_PENALTY use (L304) with `detectionConfig.thresholds.openToWorkPenalty`. Replace `let currentThreshold = 60;` (L78) with `let currentThreshold = detectionConfig.thresholds.autoHideDefault;`. Replace `?? 60` at L170 and L217 with `?? detectionConfig.thresholds.autoHideDefault`. The user-override read from chrome.storage.local stays exactly as-is (D-02 — only the default literal moves).
    In src/content/detector/heuristic.ts: add `import { detectionConfig } from '../../shared/detectionConfig';` (after existing imports, before the HeuristicDetectorOptions interface). In the listicle-cta composite (L83-95) replace the two `25` with `detectionConfig.weights.listicleCta.both`, the two `12` with `detectionConfig.weights.listicleCta.listicleOnly`, the two `8` with `detectionConfig.weights.listicleCta.ctaOnly`. Replace the `20` in `if (score > 20 …` (L144) with `detectionConfig.weights.genericComments.gate`. Do NOT touch the confidence-band literals `60`/`35` on L159 (they are heuristic-internal confidence bands, not detection thresholds, and are out of D-04 scope). Do NOT add any Math.min cap for buzzword/em-dash/ai-vocab/hook/motivational/impersonal — those signals have no literal in this file; leave their `score += xxxScore` lines unchanged.
  </action>
  <verify>
    <automated>cd /c/Git/linkedin.blocker && npm test && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - The Plan 01 golden-score snapshot is byte-identical: `npm test` exits 0 with no snapshot/toStrictEqual diff (any score or breakdown diff is a BUG, not tuning — fix the refactor, never the snapshot).
    - `npm run type-check` exits 0.
    - No bare detection literal remains at the rewired call sites: in src/content/index.ts the tokens `FLAG_THRESHOLD` and `OPEN_TO_WORK_PENALTY` no longer appear (verifiable: `grep -c "FLAG_THRESHOLD\|OPEN_TO_WORK_PENALTY" src/content/index.ts` === 0); no `?? 60` remains (verifiable: `grep -c "?? 60" src/content/index.ts` === 0).
    - In src/content/detector/heuristic.ts the listicle-cta block references `detectionConfig.weights.listicleCta` (verifiable: `grep -c "detectionConfig.weights.listicleCta" src/content/detector/heuristic.ts` >= 3) and the gate references `detectionConfig.weights.genericComments.gate` (verifiable: `grep -c "genericComments.gate" src/content/detector/heuristic.ts` === 1); the bare literals `breakdown['listicle-cta'] = 25` no longer appear.
    - Both files contain a `detectionConfig` import (verifiable: `grep -c "from '../shared/detectionConfig'" src/content/index.ts` === 1 and `grep -c "from '../../shared/detectionConfig'" src/content/detector/heuristic.ts` === 1).
  </acceptance_criteria>
  <done>Both content-script consumers import every threshold/weight from detectionConfig; no detection literal remains at their call sites; npm test (snapshot byte-identical) and npm run type-check are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire detectionConfig.thresholds.flag into scripts/eval.ts (no-drift tie-in)</name>
  <read_first>
    - scripts/eval.ts (lines 10-31 the import block — note the `.js`-extension ESM import convention used for src imports like `'../src/shared/pricing.js'` and `'../src/content/detector/heuristic.js'`; line 16 THRESHOLDS imported from '../src/shared/eval/index.js'; line 262 `let bestF1Threshold = THRESHOLDS[0]!;`)
    - src/shared/eval/metrics.ts (line 31 — confirms THRESHOLDS sweep array STAYS here per D-03; do NOT relocate it to detectionConfig)
    - .planning/phases/29-config-foundation/29-PATTERNS.md (section "scripts/eval.ts — MODIFY" — import path is `'../src/shared/detectionConfig.js'` WITH .js extension; D-03 keeps THRESHOLDS in metrics.ts; criterion #4 = eval references the SAME flag value as runtime so they cannot drift)
    - .planning/phases/29-config-foundation/29-CONTEXT.md (D-03 sweep array stays in metrics.ts; success criterion #4 — eval CLI uses the same threshold value as the running extension with no manual sync)
  </read_first>
  <action>
    In scripts/eval.ts: add `import { detectionConfig } from '../src/shared/detectionConfig.js';` (WITH .js extension — ESM/Node resolution, after the existing imports). Satisfy success criterion #4 (eval and runtime never drift on the operating-point flag) by referencing `detectionConfig.thresholds.flag` as the operating-point constant. Concretely: replace the bestF1Threshold seed `let bestF1Threshold = THRESHOLDS[0]!;` (L262) with `let bestF1Threshold = detectionConfig.thresholds.flag;` — this makes the eval's default/fallback operating point the SAME single source as the runtime flag threshold, so they cannot drift, while leaving the THRESHOLDS sweep array untouched in metrics.ts (D-03). Do NOT relocate or modify THRESHOLDS. Do NOT change the sweep logic (`thresholdRows = THRESHOLDS.map(...)`) — only the fallback seed value changes its source from the sweep array's first element to the shared config. This is a zero-behavior-change wiring: 35 (THRESHOLDS[0]) and detectionConfig.thresholds.flag (35) are the same value, but now sourced from the single config (criterion #4).
  </action>
  <verify>
    <automated>cd /c/Git/linkedin.blocker && npm test && npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - scripts/eval.ts imports detectionConfig with the .js extension (verifiable: `grep -c "from '../src/shared/detectionConfig.js'" scripts/eval.ts` === 1).
    - The bestF1Threshold seed references the shared config (verifiable: `grep -c "detectionConfig.thresholds.flag" scripts/eval.ts` >= 1).
    - THRESHOLDS still lives in and is imported from metrics.ts / eval index — not relocated (verifiable: `grep -c "export const THRESHOLDS" src/shared/eval/metrics.ts` === 1).
    - `npm test` exits 0 (golden-score snapshot byte-identical) and `npm run type-check` exits 0.
    - The eval operating-point flag (detectionConfig.thresholds.flag) and the runtime flag (content/index.ts via detectionConfig.thresholds.flag) resolve to the SAME config key — no manual sync exists (criterion #4 satisfied by construction).
  </acceptance_criteria>
  <done>scripts/eval.ts imports its operating-point flag from detectionConfig.thresholds.flag (no-drift tie-in, criterion #4); THRESHOLDS remains in metrics.ts (D-03); npm test + npm run type-check green; zero behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Pure internal relocation of compile-time numeric constants. detectionConfig.ts is host-agnostic (no chrome.*, document.*, fs, process). No new input crosses any boundary; no network, storage write, or DOM/host API is added or removed. The user-override read from chrome.storage.local is unchanged (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | Tampering | detection constant values during relocation | mitigate | Plan 01 golden-score snapshot must stay byte-identical; any value change fails npm test (fails closed). No value is permitted to change in Phase 29. |
| T-29-03 | Information Disclosure | detectionConfig.ts host-agnostic contract | accept | Module is a pure literal object with no I/O; the contract is enforced by the no-chrome/document/fs/process acceptance grep in Task 1. Nothing sensitive is present. |

**Assessment:** Phase 29 introduces no new I/O, network, storage-write, or trust boundary — it relocates existing numeric literals into one typed module. No package installs occur (no Package Legitimacy Gate needed). No high-severity threats are introduced; the threat model is intentionally minimal per the phase security note. The single real risk (accidental value drift) is mitigated by the Plan 01 snapshot acting as a fail-closed gate.
</threat_model>

<verification>
- `npm test` green AND the Plan 01 golden-score snapshot byte-identical (same score + breakdown for every fixture) — the operational definition of zero behavior change.
- `npm run type-check` green (the `as const` config type-narrows correctly at all consumer call sites).
- No detection numeric literal remains at any rewired call site in content/index.ts or heuristic.ts.
- THRESHOLDS sweep array still in metrics.ts (D-03 honored).
- background/index.ts unchanged (verified-no-change per PATTERNS.md: holds only REDERIVE_* scraper constants, no detection literal — do NOT add an import there).
</verification>

<success_criteria>
- Criterion 1: detectionConfig.ts exists and exports all detection constants (autoHideDefault, flag, openToWorkPenalty, listicle-cta weights, per-signal cap keys, maxPostsPerSession).
- Criterion 2: content/index.ts, heuristic.ts, and scripts/eval.ts import threshold/weight values from detectionConfig.ts; no detection numeric literal remains at those call sites (background/index.ts correctly unchanged — no detection literal exists there).
- Criterion 3: npm test && npm run type-check pass green with zero behavior change (golden-score snapshot byte-identical).
- Criterion 4: eval CLI uses detectionConfig.thresholds.flag — the same value as the running extension, no manual synchronization step.
</success_criteria>

<output>
Create `.planning/phases/29-config-foundation/29-02-SUMMARY.md` when done.
</output>
