---
phase: 30-skill-registry-architecture
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/content/exclusions/sponsored.skill.ts
  - src/content/exclusions/company-page.skill.ts
  - src/content/exclusions/non-english.skill.ts
  - src/content/exclusions/open-to-work.skill.ts
autonomous: true
requirements: [SKILL-01, SKILL-03]
must_haves:
  truths:
    - "Four ExclusionSkill modules exist (sponsored, company-page, non-english, open-to-work), each extracting one branch of the current checkExclusions() unchanged"
    - "Each exclusion skill returns the same ExclusionResult shape the corresponding checkExclusions branch returns today"
    - "open-to-work always returns excluded:false and passes openToWork metadata through (not an exclusion)"
  artifacts:
    - path: "src/content/exclusions/sponsored.skill.ts"
      provides: "sponsoredExclusionSkill — SPONSORED_MARKER check"
      contains: "id: 'sponsored'"
    - path: "src/content/exclusions/non-english.skill.ts"
      provides: "nonEnglishExclusionSkill — isNonEnglish check"
      contains: "id: 'non-english'"
    - path: "src/content/exclusions/open-to-work.skill.ts"
      provides: "openToWorkExclusionSkill — metadata passthrough"
      contains: "openToWork"
  key_links:
    - from: "src/content/exclusions/sponsored.skill.ts"
      to: "src/content/selector-registry.ts"
      via: "resolve('SPONSORED_MARKER')"
      pattern: "resolve\\('SPONSORED_MARKER'\\)"
    - from: "src/content/exclusions/non-english.skill.ts"
      to: "src/content/detector/language.ts"
      via: "isNonEnglish import"
      pattern: "isNonEnglish"
---

<objective>
Model the four hard-exclusion branches (sponsored, company-page, non-English, open-to-work) as `ExclusionSkill` modules (D-03), extracting each branch of `checkExclusions()` byte-for-byte into its own skill. The registration order these will be listed in (Plan 04) and run in (Plan 05) MUST match the current strict priority order so exclusion parity (D-09, SKILL-03) holds.

Purpose: Decompose the inline exclusion logic into discrete, registry-runnable skills without changing which posts are excluded or the short-circuit ordering (CLAUDE.md constraint #5).

Output: Four `*.skill.ts` modules under a new `src/content/exclusions/` directory. (This plan creates only the skill modules — wiring into the runner and the parity test live in Plan 05.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-skill-registry-architecture/30-CONTEXT.md
@.planning/phases/30-skill-registry-architecture/30-RESEARCH.md
@.planning/phases/30-skill-registry-architecture/30-PATTERNS.md
@src/content/exclusions.ts
@src/shared/skills/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract sponsored, company-page, and non-english ExclusionSkills</name>
  <files>src/content/exclusions/sponsored.skill.ts, src/content/exclusions/company-page.skill.ts, src/content/exclusions/non-english.skill.ts</files>
  <read_first>
    - src/content/exclusions.ts (checkExclusions L60-78 — the three exclusion branches in priority order; ExclusionResult is now re-exported from '../shared/types' after Plan 01)
    - src/shared/skills/types.ts (ExclusionSkill interface — check(postData, postNode): ExclusionResult)
    - src/content/selector-registry.ts (resolve() export — the same resolver these skills call)
    - src/content/detector/language.ts (isNonEnglish signature — (postNode, postText) => boolean)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§sponsored.skill.ts, §company-page.skill.ts, §non-english.skill.ts — exact branch mappings and import paths for the new directory)
  </read_first>
  <action>
    Create directory src/content/exclusions/ and three skill modules. Each exports a `const <name>ExclusionSkill: ExclusionSkill` extracting one branch of checkExclusions() unchanged. Import paths adjust for the deeper directory: `resolve` from '../selector-registry', `isNonEnglish` from '../detector/language', `ExclusionSkill` type from '../../shared/skills/types', `PostData` type from '../../shared/types'.

    - sponsored.skill.ts → sponsoredExclusionSkill, id 'sponsored'. check(_postData, postNode): if `postNode.querySelector(resolve('SPONSORED_MARKER'))` return `{ excluded: true, reason: 'sponsored' }` else `{ excluded: false }`. Use `reason: 'sponsored' as const` so the literal type is preserved.
    - company-page.skill.ts → companyPageExclusionSkill, id 'company-page'. check(postData, _postNode): if `postData.authorProfileUrl.includes(resolve('COMPANY_PAGE_MARKER'))` return `{ excluded: true, reason: 'company-page' as const }` else `{ excluded: false }`.
    - non-english.skill.ts → nonEnglishExclusionSkill, id 'non-english'. check(postData, postNode): if `isNonEnglish(postNode, postData.postText)` return `{ excluded: true, reason: 'non-english' as const }` else `{ excluded: false }`.

    Each branch must be a verbatim behavioral copy of the corresponding checkExclusions branch (same selector keys, same isNonEnglish call) so parity holds (D-09). Do NOT change the order of checks within a skill (each skill is one check). Add a one-line JSDoc per file naming the checkExclusions branch it was extracted from.
  </action>
  <acceptance_criteria>
    - Three files exist under src/content/exclusions/: sponsored.skill.ts, company-page.skill.ts, non-english.skill.ts.
    - sponsored.skill.ts calls `resolve('SPONSORED_MARKER')` and returns reason 'sponsored'; company-page.skill.ts checks `authorProfileUrl.includes(resolve('COMPANY_PAGE_MARKER'))` and returns reason 'company-page'; non-english.skill.ts calls `isNonEnglish(postNode, postData.postText)` and returns reason 'non-english'.
    - No selector string literal appears inline (all via resolve(), per CLAUDE.md constraint #1).
    - `npm run type-check` passes — each module satisfies the ExclusionSkill contract.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>The three hard-exclusion branches are independent ExclusionSkills with byte-identical checks to checkExclusions().</done>
</task>

<task type="auto">
  <name>Task 2: Extract open-to-work metadata-passthrough ExclusionSkill</name>
  <files>src/content/exclusions/open-to-work.skill.ts</files>
  <read_first>
    - src/content/exclusions.ts (L80-83 — the openToWork passthrough branch: `const openToWork = !!postNode.querySelector(resolve('OPEN_TO_WORK_MARKER')); return { excluded: false, openToWork };`)
    - src/shared/skills/types.ts (ExclusionSkill; ExclusionResult.openToWork field)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§open-to-work.skill.ts; RESEARCH note A2 — open-to-work always returns excluded:false)
  </read_first>
  <action>
    Create src/content/exclusions/open-to-work.skill.ts → openToWorkExclusionSkill: ExclusionSkill, id 'open-to-work'. check(_postData, postNode) returns `{ excluded: false, openToWork: !!postNode.querySelector(resolve('OPEN_TO_WORK_MARKER')) }`. This is NOT an exclusion — it always returns excluded:false and only exposes the openToWork metadata so the content-script runner (Plan 05) can apply the +20 threshold penalty unchanged. Same import paths as Task 1. Add a JSDoc note that this skill must always return excluded:false (parity-critical — it must never short-circuit detection).
  </action>
  <acceptance_criteria>
    - src/content/exclusions/open-to-work.skill.ts exists with id 'open-to-work'.
    - check() always returns excluded:false (grep confirms no `excluded: true` in the file) and sets openToWork from `resolve('OPEN_TO_WORK_MARKER')`.
    - `npm run type-check` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>open-to-work is a passthrough ExclusionSkill that never excludes and forwards the openToWork metadata, preserving the +20 threshold path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn DOM (postNode) → exclusion check | Untrusted page DOM is queried via resolve()'d selector strings; no page-supplied string is executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Tampering | ExclusionSkill selector usage | mitigate | Selector strings come exclusively from `resolve()` (SelectorRegistry), never inlined — upholds CLAUDE.md constraint #1. No page-derived string reaches a code-execution path; `querySelector` receives a trusted resolved selector only |
| T-30-05 | Spoofing | open-to-work passthrough | accept | A spoofed open-to-work marker only raises the auto-hide threshold by +20 (makes hiding harder, fail-safe toward showing content); it cannot bypass a real exclusion because excluded:false never short-circuits |
| T-30-SC | Tampering | npm installs | mitigate | Zero new packages (pure TypeScript). No install task; no slopcheck checkpoint required |
</threat_model>

<verification>
- `npm run type-check` passes — all four ExclusionSkills satisfy the contract.
- `npm test` passes — checkExclusions() is still intact (untouched this plan; replaced in Plan 05), so no behavior change yet.
- grep: each skill references only resolve()'d selector keys; open-to-work.skill.ts contains no `excluded: true`.
</verification>

<success_criteria>
- Four ExclusionSkills extract the four checkExclusions branches verbatim (SKILL-01, SKILL-03).
- Each returns the same ExclusionResult shape as today, preserving the inputs to the parity test in Plan 05 (D-09).
</success_criteria>

<output>
Create `.planning/phases/30-skill-registry-architecture/30-03-SUMMARY.md` when done.
</output>
