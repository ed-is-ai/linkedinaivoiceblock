---
phase: 30-skill-registry-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/skills/types.ts
  - src/shared/types.ts
autonomous: true
requirements: [SKILL-01, SKILL-02]
must_haves:
  truths:
    - "Skill type contracts (DetectorSkill, SignalSkill, CodeSkill, PatternSkill, ExclusionSkill, SignalContext) exist as a host-agnostic module"
    - "SkillRegistrySchema storage shape is defined and added to StorageSchema as a new optional skillRegistry key"
    - "Detector interface remains usable unchanged (alias or extension) so existing call sites do not break"
  artifacts:
    - path: "src/shared/skills/types.ts"
      provides: "All skill interfaces + SkillRegistrySchema + SKILL_REGISTRY_VERSION"
      contains: "export interface CodeSkill"
    - path: "src/shared/types.ts"
      provides: "StorageSchema with skillRegistry field + SkillRegistrySchema import"
      contains: "skillRegistry?: SkillRegistrySchema"
  key_links:
    - from: "src/shared/types.ts"
      to: "src/shared/skills/types.ts"
      via: "import type { SkillRegistrySchema }"
      pattern: "import.*SkillRegistrySchema.*skills/types"
---

<objective>
Define the host-agnostic skill type system (D-01, D-02, D-03, D-04) and wire the new `skillRegistry` storage key into `StorageSchema` (SKILL-02, Landmine 7). This is the contract foundation that every downstream plan imports — no behavior, pure type declarations.

Purpose: Establish the two-level skill model (`DetectorSkill` / `SignalSkill` / `ExclusionSkill`) as importable contracts so signal-skill wrappers, exclusion skills, and the registry all build against one source of truth. Interface-first ordering prevents a scavenger hunt in later waves.

Output: `src/shared/skills/types.ts` (new) and a one-field addition to `StorageSchema` in `src/shared/types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-skill-registry-architecture/30-CONTEXT.md
@.planning/phases/30-skill-registry-architecture/30-RESEARCH.md
@.planning/phases/30-skill-registry-architecture/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/shared/skills/types.ts skill contract module</name>
  <files>src/shared/skills/types.ts</files>
  <read_first>
    - src/shared/types.ts (existing Detector interface L66-71, SelectorRegistrySchema L398, ExclusionResult is in src/content/exclusions.ts L29-45 — note ExclusionResult currently lives in content, not shared)
    - src/content/exclusions.ts (ExclusionResult interface L29-45 — the shape ExclusionSkill.check must return)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§src/shared/skills/types.ts — full interface list to declare)
    - .planning/phases/30-skill-registry-architecture/30-RESEARCH.md (§Skill Type System Design)
  </read_first>
  <action>
    Create new file src/shared/skills/types.ts. It MUST be host-agnostic: no chrome.*, no DOM API imports except the `Element` ambient type (lib.dom is fine for the `postNode: Element` param on ExclusionSkill — it is a type-only reference, not a runtime DOM call). Import only `PostData`, `DetectionResult`, and `ExclusionResult` as types from '../types'.

    First, in src/content/exclusions.ts, the `ExclusionResult` interface (L29-45) must be re-homed so it is importable by the host-agnostic skills module without creating a content→shared dependency inversion. Move `ExclusionResult` to src/shared/types.ts (export it there) and re-export it from src/content/exclusions.ts (`export type { ExclusionResult } from '../shared/types';`) so existing importers of `./exclusions` keep working. Do this re-home in Task 2 alongside the StorageSchema edit; in this task import `ExclusionResult` from '../types' assuming it will live there.

    Declare exactly these exports (shapes per 30-PATTERNS.md §types.ts and 30-RESEARCH.md §Skill Type System Design):
    - `export const SKILL_REGISTRY_VERSION = '1.0.0';`
    - `export type SignalInput = 'text' | 'profile' | 'comments';`
    - `SignalSkillBase` interface: `kind: 'signal'`, `id: string` (matches signalBreakdown key AND detectionConfig weight key per D-04), `inputs: SignalInput[]`, `sync: boolean`.
    - `CodeSkill extends SignalSkillBase`: `flavor: 'code'`, `run(ctx: SignalContext): number | Promise<number>`.
    - `PatternSkill extends SignalSkillBase`: `flavor: 'pattern'`, `weightKey: string`, `rule: PatternRule`. NO `run()` method (executed by the pattern-runner, not the skill — MV3-CSP-safe, D-02).
    - `PatternRule` discriminated union with three members: `keyword-set` ({ kind, keywords: string[], matchMode: 'any' | 'density', densityThreshold?: number }), `regex` ({ kind, patterns: string[], minHits: number } — patterns are STRINGS, never compiled functions, per D-02 MV3 CSP), `numeric-threshold` ({ kind, extractFn: 'em-dash-density' | 'word-count', operator: '>', value: number }).
    - `ExclusionSkill` interface: `kind: 'exclusion'`, `id: string`, `check(postData: PostData, postNode: Element): ExclusionResult`.
    - `DetectorSkill` interface: `kind: 'detector'`, `name: string`, `detect(post: PostData): Promise<DetectionResult>`.
    - `SignalContext` interface: `postData: PostData`, `fetchComments?: (post: PostData) => Promise<string[]>`.
    - `export type SignalSkill = CodeSkill | PatternSkill;`
    - `export type AnySkill = DetectorSkill | SignalSkill | ExclusionSkill;`
    - `SkillRegistrySchema` interface: `version: string`, `declarativeSignalSkills: PatternSkill[]`, `declarativeExclusionSkills: ExclusionSkill[]`, `lastModifiedAt: string | null`.

    Add a JSDoc file header documenting: host-agnostic contract (no chrome.*/no runtime DOM), and that PatternSkill is the only LLM-authorable flavor because MV3 CSP forbids eval/new Function (D-02). Do NOT declare any runtime code or const objects other than SKILL_REGISTRY_VERSION.
  </action>
  <acceptance_criteria>
    - src/shared/skills/types.ts exists and exports SKILL_REGISTRY_VERSION, SignalInput, SignalSkillBase, CodeSkill, PatternSkill, PatternRule, ExclusionSkill, DetectorSkill, SignalContext, SignalSkill, AnySkill, SkillRegistrySchema.
    - PatternSkill has NO `run` member (grep: `run` does not appear inside the PatternSkill interface body).
    - File contains no `chrome.` substring and no runtime DOM call (no `document.`, `querySelector`); `Element` appears only as a type annotation.
    - `npm run type-check` passes (the file compiles against the imported PostData/DetectionResult/ExclusionResult types).
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>The skill contract module compiles and exports every interface the downstream waves import; PatternSkill carries declarative rule data only (no run()).</done>
</task>

<task type="auto">
  <name>Task 2: Re-home ExclusionResult to shared/types.ts and add skillRegistry to StorageSchema</name>
  <files>src/shared/types.ts, src/content/exclusions.ts</files>
  <read_first>
    - src/shared/types.ts (StorageSchema L417-465, SelectorRegistrySchema L398, selectorRegistry field L444, evalRuns field L464 — add skillRegistry after evalRuns)
    - src/content/exclusions.ts (ExclusionResult L29-45 — the interface to move out)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§StorageSchema addition; Landmine 7 — key must be NEW, not a rename)
  </read_first>
  <action>
    Two edits to src/shared/types.ts:
    1. Move the `ExclusionResult` interface (currently src/content/exclusions.ts L29-45) into src/shared/types.ts as an exported interface — copy it verbatim (excluded: boolean; reason?: 'sponsored' | 'company-page' | 'non-english'; openToWork?: boolean) with its JSDoc. Then in src/content/exclusions.ts, delete the local interface declaration and add `export type { ExclusionResult } from '../shared/types';` so every existing importer of `./exclusions` (e.g. content/index.ts) continues to resolve `ExclusionResult` unchanged.
    2. Import `SkillRegistrySchema` as a type at the top of src/shared/types.ts: `import type { SkillRegistrySchema } from './skills/types';`. Add a new optional field to `StorageSchema` immediately after `evalRuns?: EvalRun[];` (L464): `skillRegistry?: SkillRegistrySchema;`. This is a NEW key (Landmine 7 — verify no existing `skillRegistry` field exists before adding; only `selectorRegistry` is present).

    Do NOT change selectorRegistry, evalRuns, or any other StorageSchema field. The skillRegistry key string is camelCase to match the selectorRegistry convention.
  </action>
  <acceptance_criteria>
    - src/shared/types.ts exports `ExclusionResult` and declares `skillRegistry?: SkillRegistrySchema;` in StorageSchema.
    - src/content/exclusions.ts no longer declares `interface ExclusionResult` locally and instead re-exports it from '../shared/types'; `checkExclusions` still returns the same shape.
    - grep confirms exactly one `skillRegistry?:` field in StorageSchema and `selectorRegistry?:` is unchanged.
    - `npm run type-check` passes; `npm test` passes (no behavior change — exclusions.ts still compiles and its consumers resolve ExclusionResult).
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && npm test</automated>
  </verify>
  <done>ExclusionResult lives in shared/types.ts (importable host-agnostically), exclusions.ts re-exports it for backward compat, and StorageSchema has a typed skillRegistry slot with no collision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → content script | Declarative skill defs (PatternSkill data) read from storage are untrusted data; they must remain DATA only, never executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | PatternSkill.rule schema (types.ts) | mitigate | `regex` rule stores patterns as `string[]` only — the type system forbids storing functions; no `eval`/`new Function` path is introduced. Compilation to RegExp happens in the pattern-runner (Plan 02) from validated string sources, mirroring how SelectorRegistry compiles selector strings to querySelector calls |
| T-30-02 | Elevation of Privilege | SkillRegistrySchema storage shape | accept | A future LLM-authored declarative skill is data, not code. This phase seeds ZERO declarative skills, so no untrusted data is loaded; the surface exists but is empty. MV3 CSP forbids runtime code execution regardless |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages this phase (pure TypeScript refactor — RESEARCH §Package Legitimacy Audit: N/A). No install task exists, so no slopcheck checkpoint required |
</threat_model>

<verification>
- `npm run type-check` passes — all skill interfaces compile against existing PostData/DetectionResult.
- `npm test` passes — the Phase 29 golden-score snapshot is untouched (no runtime code added this plan).
- grep: src/shared/skills/types.ts contains no `chrome.` and no runtime DOM call.
- grep: StorageSchema contains exactly one new `skillRegistry?:` field.
</verification>

<success_criteria>
- The full skill type system (SKILL-01, SKILL-02 contracts) is declared in a host-agnostic module.
- StorageSchema carries a typed, collision-free `skillRegistry` slot (Landmine 7 avoided).
- Zero behavior change: type-check and the golden-score snapshot stay green.
</success_criteria>

<output>
Create `.planning/phases/30-skill-registry-architecture/30-01-SUMMARY.md` when done.
</output>
