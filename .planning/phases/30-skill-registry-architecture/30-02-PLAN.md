---
phase: 30-skill-registry-architecture
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/content/detector/signals/listicle-cta.skill.ts
  - src/content/detector/signals/buzzword.skill.ts
  - src/content/detector/signals/em-dash.skill.ts
  - src/content/detector/signals/ai-vocab.skill.ts
  - src/content/detector/signals/hook-story.skill.ts
  - src/content/detector/signals/motivational.skill.ts
  - src/content/detector/signals/impersonal.skill.ts
  - src/content/detector/signals/generic-comments.skill.ts
  - src/shared/skills/pattern-runner.ts
autonomous: true
requirements: [SKILL-01, SKILL-02]
must_haves:
  truths:
    - "Eight CodeSkill wrappers exist, each wrapping its existing signal function unchanged"
    - "listicle-cta is a SINGLE composite CodeSkill calling both checkListicle and checkCta, returning the detectionConfig.weights.listicleCta tier"
    - "generic-comments is the only sync:false skill; it fetches comments and scores but does NOT check the score>20 gate (gate stays in the runner)"
    - "A pattern-runner exists that executes PatternSkill rules without eval/new Function (supported-but-unused this phase)"
  artifacts:
    - path: "src/content/detector/signals/listicle-cta.skill.ts"
      provides: "Composite listicle-cta CodeSkill (id matches snapshot key)"
      contains: "id: 'listicle-cta'"
    - path: "src/content/detector/signals/generic-comments.skill.ts"
      provides: "Async generic-comments CodeSkill (sync: false)"
      contains: "sync: false"
    - path: "src/shared/skills/pattern-runner.ts"
      provides: "PatternSkillRunner — MV3-CSP-safe declarative rule executor"
      contains: "runPatternSkill"
  key_links:
    - from: "src/content/detector/signals/listicle-cta.skill.ts"
      to: "src/shared/detectionConfig.ts"
      via: "detectionConfig.weights.listicleCta tier read"
      pattern: "detectionConfig\\.weights\\.listicleCta"
    - from: "src/content/detector/signals/em-dash.skill.ts"
      to: "src/content/detector/signals/em-dash.ts"
      via: "import { checkEmDash }"
      pattern: "import.*checkEmDash"
---

<objective>
Migrate the 8 heuristic scoring signals into `CodeSkill` wrappers (D-02, D-05) and ship the supported-but-unused `PatternSkillRunner` (D-02 declarative flavor). Every wrapper imports its existing signal function unchanged — this is wrapping, not rewriting (RESEARCH §CodeSkill Wrappers). The composite `listicle-cta` skill and the async-gated `generic-comments` skill are the two landmines this plan must defuse.

Purpose: Provide the ordered set of `CodeSkill` modules the registry (Plan 04) imports and the runner (Plan 05) iterates, preserving the exact scoring behavior the Phase 29 golden-score snapshot pins (SKILL-04 / D-08).

Output: 8 `*.skill.ts` wrapper modules + `src/shared/skills/pattern-runner.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-skill-registry-architecture/30-CONTEXT.md
@.planning/phases/30-skill-registry-architecture/30-RESEARCH.md
@.planning/phases/30-skill-registry-architecture/30-PATTERNS.md
@src/shared/skills/types.ts
@src/shared/detectionConfig.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the 6 simple text CodeSkill wrappers + the listicle-cta composite</name>
  <files>src/content/detector/signals/listicle-cta.skill.ts, src/content/detector/signals/buzzword.skill.ts, src/content/detector/signals/em-dash.skill.ts, src/content/detector/signals/ai-vocab.skill.ts, src/content/detector/signals/hook-story.skill.ts, src/content/detector/signals/motivational.skill.ts, src/content/detector/signals/impersonal.skill.ts</files>
  <read_first>
    - src/shared/skills/types.ts (CodeSkill interface, SignalContext — the contract each wrapper implements)
    - src/content/detector/heuristic.ts (L78-140 — the EXACT current call order and per-signal return handling these wrappers must reproduce; note checkBuzzwords/checkEmDash/checkAiVocab/checkHookStory/checkMotivational/checkImpersonalVoice each already return their bounded score)
    - src/content/detector/signals/em-dash.ts (checkEmDash — canonical wrapper example)
    - src/content/detector/signals/listicle.ts and src/content/detector/signals/cta.ts (checkListicle / checkCta — invoked together by the composite)
    - src/shared/detectionConfig.ts (weights.listicleCta.{both|listicleOnly|ctaOnly} = 25/12/8 — the ONLY weights a wrapper reads directly)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§em-dash.skill.ts canonical example; §listicle-cta.skill.ts composite; Landmine 1)
  </read_first>
  <action>
    Create 7 new wrapper modules under src/content/detector/signals/. Each is a thin CodeSkill exporting a `const <name>Skill: CodeSkill` that imports the existing signal function unchanged. Do NOT modify any existing signal .ts file. Do NOT reintroduce any weight literal in a wrapper (D-05, Landmine 6) — the simple wrappers return the underlying function's value directly; only listicle-cta reads detectionConfig.

    Simple text wrappers (each: kind 'signal', flavor 'code', inputs ['text'], sync true, id matching the breakdown key, run({ postData }) returns the underlying function on postData.postText):
    - em-dash.skill.ts → emDashSkill, id 'em-dash', wraps checkEmDash.
    - buzzword.skill.ts → buzzwordSkill, id 'buzzword', wraps checkBuzzwords.
    - ai-vocab.skill.ts → aiVocabSkill, id 'ai-vocab', wraps checkAiVocab.
    - hook-story.skill.ts → hookStorySkill, id 'hook-story', wraps checkHookStory.
    - motivational.skill.ts → motivationalSkill, id 'motivational', wraps checkMotivational.
    - impersonal.skill.ts → impersonalSkill, id 'impersonal', wraps checkImpersonalVoice.

    Composite (Landmine 1 — MUST be ONE skill with id 'listicle-cta', NEVER two):
    - listicle-cta.skill.ts → listicleCtaSkill, id 'listicle-cta', inputs ['text'], sync true. run({ postData }) calls checkListicle(postData.postText) once and checkCta(postData.postText) once, then returns the composite tier exactly as heuristic.ts L84-96: both>0 → detectionConfig.weights.listicleCta.both; listicle only → .listicleOnly; cta only → .ctaOnly; else 0. Import detectionConfig from '../../../shared/detectionConfig' and listicle/cta from './listicle' and './cta'. The id MUST be the single string 'listicle-cta' to keep the snapshot key.

    Each wrapper file gets a one-line JSDoc header naming what it wraps and noting weights are NOT redeclared here (they live in the underlying function / detectionConfig).
  </action>
  <acceptance_criteria>
    - Seven new files exist: listicle-cta.skill.ts, buzzword.skill.ts, em-dash.skill.ts, ai-vocab.skill.ts, hook-story.skill.ts, motivational.skill.ts, impersonal.skill.ts.
    - listicle-cta.skill.ts contains exactly one `id: 'listicle-cta'` and references detectionConfig.weights.listicleCta.{both,listicleOnly,ctaOnly}; it does NOT contain numeric literals 25/12/8 (grep -v '^ *\*' for the body shows the weights come from detectionConfig, not inline).
    - No new file contains a `25`, `15`, `12`, `10`, `8`, `6`, `5` weight literal in a `return` statement (simple wrappers delegate to their function; composite reads detectionConfig).
    - No existing signal .ts file was modified (git diff shows only new *.skill.ts files added).
    - `npm run type-check` passes — every wrapper satisfies the CodeSkill contract.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Six simple text CodeSkills delegate to their unchanged signal functions, and the listicle-cta composite is a single skill reading its tier from detectionConfig — no weight literal reintroduced.</done>
</task>

<task type="auto">
  <name>Task 2: Create generic-comments async CodeSkill + pattern-runner</name>
  <files>src/content/detector/signals/generic-comments.skill.ts, src/shared/skills/pattern-runner.ts</files>
  <read_first>
    - src/shared/skills/types.ts (CodeSkill async run signature returns number | Promise<number>; PatternRule union; SignalContext.fetchComments)
    - src/content/detector/heuristic.ts (L142-152 — the generic-comments gate `score > detectionConfig.weights.genericComments.gate && fetchComments !== undefined`; note the GATE stays in the runner, NOT in the skill — Landmine 3+5)
    - src/content/detector/signals/comments.ts (checkGenericComments — the function the skill wraps)
    - src/content/detector/signals/buzzwords.ts (L39-42 — compile-RegExp-once-at-module-scope pattern the pattern-runner mirrors)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§generic-comments.skill.ts; §pattern-runner.ts; Landmines 3 and 5)
  </read_first>
  <action>
    Create two files.

    1. src/content/detector/signals/generic-comments.skill.ts → genericCommentsSkill: CodeSkill. kind 'signal', flavor 'code', id 'generic-comments', inputs ['comments'], sync FALSE (this is the ONLY sync:false skill — Landmine 5). `async run({ postData, fetchComments })`: if `!fetchComments` return 0; otherwise `const comments = await fetchComments(postData); return checkGenericComments(comments);`. The skill MUST NOT check the `score > 20` gate — that gate is the runner's job (Landmine 3). Import checkGenericComments from './comments'.

    2. src/shared/skills/pattern-runner.ts → host-agnostic (no chrome.*, no DOM). Export `runPatternSkill(skill: PatternSkill, ctx: SignalContext): number`. It executes the three PatternRule kinds WITHOUT eval/new Function (D-02, MV3 CSP, T-30-01):
       - `keyword-set`: count keyword occurrences in ctx.postData.postText; for matchMode 'any' return a hit/no-hit; for 'density' compare against densityThreshold. Return the weight resolved from detectionConfig via skill.weightKey (use a small dotted-path resolver over detectionConfig.weights; if the key is absent return 0).
       - `regex`: compile skill.rule.patterns (strings) to RegExp ONCE, cached in a module-scope Map keyed by skill.id (mirrors buzzwords.ts compile-once, RESEARCH §Don't Hand-Roll), never recompiling per call; return the resolved weight if hits >= minHits else 0.
       - `numeric-threshold`: implement the two extractFns ('em-dash-density', 'word-count') as pure functions over postText; return resolved weight if operator/value satisfied else 0.
       This runner is supported-but-unused this phase (zero declarative skills seeded). It exists to prove the declarative flavor is expressible and MV3-CSP-safe. Add a JSDoc header stating it must never introduce a code-execution path (patterns are strings compiled via new RegExp from validated sources — same trust model as SelectorRegistry selector strings).
  </action>
  <acceptance_criteria>
    - generic-comments.skill.ts exists with `sync: false` and contains no `score > 20` / `genericComments.gate` reference (gate is NOT in the skill — Landmine 3).
    - generic-comments.skill.ts is the only new skill file with `sync: false` (grep across all *.skill.ts).
    - pattern-runner.ts contains no `eval`, no `new Function`, and no `chrome.` substring; regex patterns are compiled via `new RegExp` from string inputs and cached in a module-scope Map.
    - pattern-runner.ts handles all three PatternRule kinds (keyword-set, regex, numeric-threshold).
    - `npm run type-check` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>The async generic-comments skill fetches+scores without owning the gate, and the pattern-runner executes declarative rules with zero code-execution path (MV3-CSP-safe).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PatternSkill.rule (data) → pattern-runner (execution) | Declarative rule data is compiled/evaluated by the runner; the runner must never treat rule fields as executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | pattern-runner.ts regex compilation | mitigate | `regex` patterns are compiled with `new RegExp(stringPattern, 'gi')` from string data only — NO `eval`/`new Function`. This is the same trust model SelectorRegistry uses for selector strings. Verified by grep: pattern-runner.ts contains neither `eval` nor `new Function` |
| T-30-03 | Tampering | weightKey dotted-path resolver | accept | weightKey resolves into the trusted, code-committed `detectionConfig` object; an unknown key returns 0 (fail-closed, no points awarded). No untrusted key can escalate score because declarative skills are not seeded this phase |
| T-30-SC | Tampering | npm installs | mitigate | Zero new packages (pure TypeScript). No install task; no slopcheck checkpoint required |
</threat_model>

<verification>
- `npm run type-check` passes — all 8 wrappers + pattern-runner satisfy their contracts.
- `npm test` passes — no existing signal function modified, so the golden-score snapshot is untouched at this stage (wrappers are not yet wired into the runner; that is Plan 05).
- grep: listicle-cta.skill.ts has exactly one `id: 'listicle-cta'`; generic-comments.skill.ts is the only `sync: false` skill; pattern-runner.ts has no `eval`/`new Function`.
</verification>

<success_criteria>
- Eight CodeSkill wrappers exist in step-order-ready form, each delegating to its unchanged signal function (SKILL-01).
- The composite listicle-cta is a single skill (Landmine 1 avoided) and generic-comments is the only async skill with the gate left to the runner (Landmines 3 + 5 avoided).
- The PatternSkillRunner proves the declarative flavor with zero code-execution path (SKILL-02, D-02).
</success_criteria>

<output>
Create `.planning/phases/30-skill-registry-architecture/30-02-SUMMARY.md` when done.
</output>
