---
phase: 30-skill-registry-architecture
plan: 05
type: execute
wave: 4
depends_on: ["30-04"]
files_modified:
  - src/content/detector/heuristic.ts
  - src/content/detector/llm.ts
  - src/content/index.ts
  - src/content/exclusions/exclusions.test.ts
autonomous: true
requirements: [SKILL-01, SKILL-03, SKILL-04]
must_haves:
  truths:
    - "HeuristicDetector.detect() runs registered signal skills via getSignalSkills() and produces byte-identical score + signalBreakdown to the hand-wired pipeline"
    - "The generic-comments gate (score > genericComments.gate) is enforced in the runner between the sync pass and the async await"
    - "Profile signals remain merged in content/index.ts AFTER detect() — NOT moved into the registry (no double-count)"
    - "Exclusions run via getExclusionSkills() in content/index.ts, short-circuiting on the first excluded:true before any detector runs (CLAUDE.md #5 ordering preserved)"
    - "LLMDetector implements DetectorSkill (kind discriminant added) with no behavior change"
    - "The Phase 29 golden-score snapshot stays byte-identical and a new exclusion parity test passes over the 5-fixture set"
  artifacts:
    - path: "src/content/detector/heuristic.ts"
      provides: "Registry-runner HeuristicDetector"
      contains: "getSignalSkills"
    - path: "src/content/exclusions/exclusions.test.ts"
      provides: "Exclusion parity test over the runner path (5 fixtures)"
      contains: "getExclusionSkills"
  key_links:
    - from: "src/content/detector/heuristic.ts"
      to: "src/content/skill-registry.ts"
      via: "getSignalSkills() runner iteration"
      pattern: "getSignalSkills\\(\\)"
    - from: "src/content/index.ts"
      to: "src/content/skill-registry.ts"
      via: "getExclusionSkills() loop + skillRegistry init (aliased seedIfNeeded/load)"
      pattern: "getExclusionSkills\\(\\)"
---

<objective>
Wire the registry into the live path and prove zero behavior change (SKILL-04 / D-08 / D-09). Refactor `HeuristicDetector.detect()` into a registry runner, add the `DetectorSkill` discriminant to `LLMDetector`, replace the inline `checkExclusions()` call with the exclusion-skill runner loop in `content/index.ts`, initialize the skill registry alongside the selector registry, and add an exclusion-parity test that exercises the NEW runner path. The Phase 29 golden-score snapshot is the operational guard for the signal migration; the new parity test is the guard for the exclusion migration.

Purpose: This is the wave where the architecture becomes the running behavior — and where the two byte-identical guards (snapshot + exclusion parity) must stay green.

Output: refactored `heuristic.ts`, one-line-discriminant `llm.ts`, rewired `content/index.ts`, and new `exclusions.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-skill-registry-architecture/30-CONTEXT.md
@.planning/phases/30-skill-registry-architecture/30-RESEARCH.md
@.planning/phases/30-skill-registry-architecture/30-PATTERNS.md
@src/content/detector/heuristic.ts
@src/content/detector/heuristic.test.ts
@src/content/index.ts
@src/content/skill-registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor HeuristicDetector.detect() into the registry runner</name>
  <files>src/content/detector/heuristic.ts</files>
  <read_first>
    - src/content/detector/heuristic.ts (FULL file — the exact hand-wired pipeline L74-163 to replace; the return shape L156-162 to preserve byte-identically; class declaration L42)
    - src/content/detector/heuristic.test.ts (the golden-score snapshot: clean=0/{}, listicle+CTA=25/{listicle-cta:25}, buzzword=15/{buzzword:15}, em-dash=10/{em-dash:10}, AI-voice=63/{listicle-cta:8,hook-story:20,motivational:20,impersonal:15}, human=0/{}; AND the assertion result.signals === Object.keys(result.signalBreakdown))
    - src/content/skill-registry.ts (getSignalSkills() — returns ordered skills; CodeSkill.run signature)
    - src/shared/skills/types.ts (CodeSkill, DetectorSkill, SignalContext)
    - src/shared/detectionConfig.ts (weights.genericComments.gate = 20 — the runner-owned gate)
    - .planning/phases/30-skill-registry-architecture/30-RESEARCH.md (§HeuristicDetector as Registry Runner — the two-pass loop; Landmines 1-5)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§heuristic.ts modify — class declaration + runner loop)
  </read_first>
  <behavior>
    - The existing golden-score snapshot in heuristic.test.ts MUST pass unchanged after the refactor (this IS the test for this task — run it RED-free; do not edit the snapshot expectations).
    - signals[] insertion order equals Object.keys(signalBreakdown), which equals the skill declaration order from getSignalSkills() for any post that triggers multiple signals (AI-voice fixture: listicle-cta → hook-story → motivational → impersonal).
    - generic-comments contributes only when running score (after sync skills) > genericComments.gate AND fetchComments is provided.
  </behavior>
  <action>
    Refactor src/content/detector/heuristic.ts so detect() iterates getSignalSkills() instead of calling 9 functions by hand. Replace the individual signal-function imports (checkListicle, checkBuzzwords, checkEmDash, checkCta, checkAiVocab, checkHookStory, checkMotivational, checkImpersonalVoice) with `import { getSignalSkills } from '../skill-registry';` and the CodeSkill/DetectorSkill/SignalContext types. Keep `import { checkGenericComments } from './signals/comments'` ONLY if still referenced; the generic-comments skill already wraps it, so the runner no longer calls checkGenericComments directly — remove that import if unused.

    Runner body (two passes, preserving Landmines 1-5):
    1. `const breakdown: Record<string, number> = {}; let score = 0;`
    2. `const skills = getSignalSkills();`
    3. Sync pass: for each skill where skill.sync === true, call `(skill as CodeSkill).run({ postData: post })` (synchronous return), and if result > 0 set `breakdown[skill.id] = result; score += result;`. Iterate in array order (Landmine 2) — this reproduces listicle-cta → buzzword → em-dash → ai-vocab → hook-story → motivational → impersonal exactly.
    4. Async gated pass: for each skill where skill.sync === false: if skill.id === 'generic-comments', check the gate `score > detectionConfig.weights.genericComments.gate && this.options.fetchComments` (Landmine 3 + 5 — gate uses the post-sync score, gate lives in the runner not the skill); if it passes, `const r = await (skill as CodeSkill).run({ postData: post, fetchComments: this.options.fetchComments });` and if r > 0 add to breakdown/score. Build the SignalContext with fetchComments from this.options.
    5. Preserve the tail verbatim: `const finalScore = Math.min(score, 100); return { score: finalScore, signals: Object.keys(breakdown), signalBreakdown: { ...breakdown }, confidence: finalScore >= 60 ? 'high' : finalScore >= 35 ? 'medium' : 'low', engineUsed: 'heuristic' };`.

    Class declaration: change `implements Detector` to `implements Detector, DetectorSkill` and add `readonly kind = 'detector' as const;` alongside `readonly name = 'heuristic';`. Keep HeuristicDetectorOptions and the constructor unchanged. Do NOT touch the existing JSDoc's intent but update the step-by-step comment to reflect "runs registered signal skills via the registry runner."

    Do NOT move profile signals into this file (Landmine 4 — they stay in content/index.ts). Do NOT add a declarative-skill pass (zero seeded; getSignalSkills already returns only code skills at launch — the spread is empty).
  </action>
  <acceptance_criteria>
    - heuristic.ts detect() calls getSignalSkills() and no longer imports/calls checkListicle/checkBuzzwords/checkEmDash/checkCta/checkAiVocab/checkHookStory/checkMotivational/checkImpersonalVoice directly.
    - HeuristicDetector implements DetectorSkill (kind: 'detector' present).
    - The generic-comments gate `score > detectionConfig.weights.genericComments.gate` is present in the runner (not removed, not moved to the skill).
    - `npm test` passes with the Phase 29 golden-score snapshot BYTE-IDENTICAL — specifically the AI-voice fixture still yields 63 with breakdown {listicle-cta:8, hook-story:20, motivational:20, impersonal:15} in that key order, and `result.signals === Object.keys(result.signalBreakdown)` holds (SKILL-04 / D-08).
    - `npm run type-check` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run type-check</automated>
  </verify>
  <done>HeuristicDetector is a registry runner producing byte-identical score + breakdown; the golden-score snapshot stays green and the gate/order landmines are intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add DetectorSkill discriminant to LLMDetector + wire exclusion runner and registry init in content/index.ts</name>
  <files>src/content/detector/llm.ts, src/content/index.ts</files>
  <read_first>
    - src/content/detector/llm.ts (FULL file — class declaration L11-14; the rest stays byte-identical)
    - src/content/index.ts (imports L1-15 — seedIfNeeded/load from './selector-registry' L3, checkExclusions from './exclusions' L6; init registry calls L208-209; exclusion call L291-292; openToWork usage L297; profile merge L307-311 stays UNCHANGED)
    - src/content/skill-registry.ts (getExclusionSkills, and seedIfNeeded/load exports — same names as selector-registry, MUST be aliased on import)
    - src/shared/types.ts (ExclusionResult — now re-exported; DetectorSkill via skills/types)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§llm.ts modify; §index.ts modify — registry init + exclusion runner loop)
    - .planning/phases/30-skill-registry-architecture/30-RESEARCH.md (§Exclusion Short-Circuit Ordering — the loop that preserves CLAUDE.md #5 + D-09)
  </read_first>
  <action>
    Three coordinated edits.

    1. src/content/detector/llm.ts: change `export class LLMDetector implements Detector {` to `implements Detector, DetectorSkill {` and add `readonly kind = 'detector' as const;` above `readonly name = 'llm';`. Import DetectorSkill type from '../../shared/skills/types'. All other lines byte-identical. (D-01 — LLMDetector stays a DetectorSkill that does not consume SignalSkills; the detector selection at content/index.ts L233-235 is preserved.)

    2. src/content/index.ts imports: add `import { seedIfNeeded as skillRegistrySeedIfNeeded, load as skillRegistryLoad, getExclusionSkills } from './skill-registry';` (alias to avoid colliding with the selector-registry seedIfNeeded/load already imported on L3). Add `import type { ExclusionResult } from '../shared/types';` if not already resolvable. Keep the existing `import { checkExclusions } from './exclusions';` ONLY if checkExclusions is still used elsewhere; otherwise remove it (the runner replaces it — RESEARCH Open Question 2 recommends replacing the call site directly).

    3. src/content/index.ts init (alongside L208-209 selector-registry seedIfNeeded()/load()): add `await skillRegistrySeedIfNeeded(); await skillRegistryLoad();` so the skill cache is warm before observation starts.

    4. src/content/index.ts exclusion call (replace L291-292): replace `const exclusion = checkExclusions(postData, postNode); if (exclusion.excluded) return;` with the runner loop (preserves CLAUDE.md #5 short-circuit + D-09 parity):
       - `let exclusionResult: ExclusionResult = { excluded: false };`
       - `for (const skill of getExclusionSkills()) { const r = skill.check(postData, postNode); if (r.excluded) { exclusionResult = r; break; } if (r.openToWork) exclusionResult = { ...exclusionResult, openToWork: true }; }`
       - `if (exclusionResult.excluded) return;`
       Then update the openToWork read on L297 from `exclusion.openToWork` to `exclusionResult.openToWork` so the +20 threshold path is unchanged.

    Do NOT change the profile-signal merge (L307-311) — profile stays merged AFTER detect() in the content script (Landmine 4). Do NOT change the detector selection (L233-235). Do NOT change any other logic.
  </action>
  <acceptance_criteria>
    - llm.ts: LLMDetector implements DetectorSkill with `kind: 'detector'`; all scoreViaBackground/detect logic byte-identical.
    - content/index.ts imports skillRegistrySeedIfNeeded/skillRegistryLoad/getExclusionSkills (aliased — no name collision with selector-registry seedIfNeeded/load) and calls both registry inits in init().
    - The inline checkExclusions() call is replaced by the getExclusionSkills() loop with first-excluded short-circuit (break) and openToWork passthrough; `exclusionResult.openToWork` feeds the +20 threshold (effectiveHideThreshold) exactly as before.
    - Profile merge (L307-311) is unchanged; detector selection (L233-235) is unchanged.
    - `npm run type-check` passes; `npm test` passes (golden-score snapshot still green from Task 1).
  </acceptance_criteria>
  <verify>
    <automated>npm run type-check && npm test</automated>
  </verify>
  <done>LLMDetector is a DetectorSkill, the skill registry is initialized at startup, and exclusions run through the registry loop with the short-circuit ordering and openToWork passthrough preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add exclusion parity test over the registry runner path</name>
  <files>src/content/exclusions/exclusions.test.ts</files>
  <read_first>
    - src/content/detector/heuristic.test.ts (vitest setup pattern: describe/it/expect imports, PostData fixture shape, JSDOM usage)
    - src/content/exclusions/*.skill.ts (the four skills under test)
    - src/content/skill-registry.ts (getExclusionSkills — the runner entry the test must exercise, NOT the old checkExclusions)
    - src/content/selector-registry.ts (resolve — must be mockable; check how existing tests stub it, e.g. via vi.mock)
    - src/content/detector/language.ts (isNonEnglish — what makes a fixture non-English: lang attr or codepoint sampling)
    - .planning/phases/30-skill-registry-architecture/30-RESEARCH.md (§Exclusion Parity Check Approach — the 5-fixture table)
    - .planning/phases/30-skill-registry-architecture/30-PATTERNS.md (§exclusions.test.ts — fixture cases and JSDOM element construction)
  </read_first>
  <behavior>
    - Iterating getExclusionSkills() and applying the same short-circuit loop as content/index.ts yields, for each fixture, the SAME ExclusionResult the legacy checkExclusions() produced:
      1. postNode with SPONSORED_MARKER child → { excluded: true, reason: 'sponsored' }
      2. authorProfileUrl containing the company-page marker → { excluded: true, reason: 'company-page' }
      3. CJK-heavy postText / non-English → { excluded: true, reason: 'non-english' }
      4. postNode with OPEN_TO_WORK_MARKER child → { excluded: false, openToWork: true }
      5. Normal English post, no markers → { excluded: false }
    - Sponsored takes priority over a simultaneously-present open-to-work marker (short-circuit order parity).
  </behavior>
  <action>
    Create src/content/exclusions/exclusions.test.ts (vitest). It MUST exercise the NEW runner path — import getExclusionSkills from '../skill-registry' and reproduce the same short-circuit loop content/index.ts uses (iterate, break on first excluded, accumulate openToWork) — NOT call the legacy checkExclusions() directly. This is what proves parity (D-09).

    Mock resolve() from '../selector-registry' (via vi.mock) to return deterministic selector strings so the JSDOM querySelector calls in the sponsored/open-to-work skills find or miss a marker element predictably. Build postNode fixtures with document.createElement and append marker children that match the mocked selectors. For the non-english fixture, supply postText that isNonEnglish classifies as non-English (per language.ts logic — CJK-heavy text or a lang attribute on the node).

    Assert all 5 fixture outcomes from the behavior block, plus the priority case: a postNode that has BOTH a sponsored marker and an open-to-work marker must return { excluded: true, reason: 'sponsored' } (sponsored short-circuits before open-to-work is reached).
  </action>
  <acceptance_criteria>
    - src/content/exclusions/exclusions.test.ts exists and imports getExclusionSkills from '../skill-registry' (exercises the runner path, not checkExclusions directly).
    - The test covers all 5 fixtures plus the sponsored-over-open-to-work priority case, asserting the exact ExclusionResult for each.
    - resolve() is mocked; JSDOM elements are built with createElement and marker children.
    - `npm test` passes including the new test file (exclusion parity verified — D-09 / SKILL-03).
    - `npm run type-check` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run type-check</automated>
  </verify>
  <done>A parity test drives the registry exclusion runner over a representative fixture set and confirms identical exclusion outcomes and short-circuit ordering to the legacy checkExclusions().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LinkedIn feed DOM → exclusion runner / detector | Untrusted page content flows through exclusion skills (querySelector) and signal skills (text scoring); no page string is executed |
| chrome.storage.local (skillRegistry) → runner | Hydrated declarative skills (empty this phase) would feed the runner; gated by the eval-free pattern-runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Tampering | Exclusion short-circuit ordering in content/index.ts | mitigate | The runner loop iterates getExclusionSkills() in registered priority order and breaks on the first excluded:true — verified by the new exclusion parity test (sponsored-over-open-to-work priority case). Preserves CLAUDE.md constraint #5 (hard exclusions before detection) |
| T-30-08 | Information Disclosure | LLMDetector unchanged behavior | accept | DetectorSkill discriminant is additive (kind only); the SCORE_POST message path and CORS-bound service-worker fetch are untouched (D-01). No new network or data-egress surface |
| T-30-02 | Elevation of Privilege | getSignalSkills() runner consumption | accept | Declarative skill list is empty at launch; the runner's spread of declarative skills adds nothing. No code-execution path (pattern-runner is eval-free). MV3 CSP enforced |
| T-30-SC | Tampering | npm installs | mitigate | Zero new packages (pure TypeScript). No install task; no slopcheck checkpoint required |
</threat_model>

<verification>
- `npm test` passes — Phase 29 golden-score snapshot BYTE-IDENTICAL (SKILL-04 / D-08) AND the new exclusion parity test green (SKILL-03 / D-09).
- `npm run type-check` passes — HeuristicDetector and LLMDetector both implement DetectorSkill; content/index.ts wiring type-checks.
- grep: heuristic.ts uses getSignalSkills() and retains the generic-comments gate; content/index.ts uses getExclusionSkills() with a short-circuit break; profile merge (L307-311) and detector selection (L233-235) unchanged.
- Manual code review: no profile signal absorbed into HeuristicDetector (Landmine 4); no listicle/cta split (Landmine 1).
</verification>

<success_criteria>
- Every scoring signal runs through a single registry runner inside HeuristicDetector with no hand-wired pipeline remaining (SKILL-01, success criterion 1).
- Hard exclusions run via ExclusionSkills and short-circuit before any detector/signal skill (SKILL-03, success criterion 2).
- Zero behavior change proven: golden-score snapshot byte-identical + exclusion parity green (SKILL-04, success criterion 4).
</success_criteria>

<output>
Create `.planning/phases/30-skill-registry-architecture/30-05-SUMMARY.md` when done.
</output>
